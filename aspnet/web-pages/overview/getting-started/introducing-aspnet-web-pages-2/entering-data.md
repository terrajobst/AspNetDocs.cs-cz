---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
title: Představení webových stránek ASP.NET – zadávání dat databáze pomocí formulářů | Microsoft Docs
author: Rick-Anderson
description: V tomto kurzu se dozvíte, jak vytvořit vstupní formulář a pak zadat data, která dostanete z formuláře do tabulky databáze při použití webových stránek ASP.NET (...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: d37c93fc-25fd-4e94-8671-0d437beef206
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
msc.type: authoredcontent
ms.openlocfilehash: b9354a7b97a7df9020a681f709e16a92650cfcf0
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/06/2020
ms.locfileid: "78624535"
---
# <a name="introducing-aspnet-web-pages---entering-database-data-by-using-forms"></a><span data-ttu-id="1460f-103">Představení webových stránek ASP.NET – zadávání dat databáze pomocí formulářů</span><span class="sxs-lookup"><span data-stu-id="1460f-103">Introducing ASP.NET Web Pages - Entering Database Data by Using Forms</span></span>

<span data-ttu-id="1460f-104">tím, že [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="1460f-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="1460f-105">V tomto kurzu se dozvíte, jak vytvořit vstupní formulář a pak zadat data, která dostanete z formuláře do tabulky databáze při použití webových stránek ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="1460f-105">This tutorial shows you how to create an entry form and then enter the data that you get from the form into a database table when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="1460f-106">Předpokládá, že jste dokončili řadu prostřednictvím [základních formulářů HTML v ASP.NET webových stránkách](https://go.microsoft.com/fwlink/?LinkId=251581).</span><span class="sxs-lookup"><span data-stu-id="1460f-106">It assumes you have completed the series through [Basics of HTML Forms in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span></span>
> 
> <span data-ttu-id="1460f-107">Naučíte se:</span><span class="sxs-lookup"><span data-stu-id="1460f-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="1460f-108">Další informace o tom, jak zpracovávat formuláře zadávání.</span><span class="sxs-lookup"><span data-stu-id="1460f-108">More about how to process entry forms.</span></span>
> - <span data-ttu-id="1460f-109">Jak přidat (vložit) data do databáze.</span><span class="sxs-lookup"><span data-stu-id="1460f-109">How to add (insert) data in a database.</span></span>
> - <span data-ttu-id="1460f-110">Jak zajistit, aby uživatelé zadali požadovanou hodnotu ve formuláři (ověření vstupu uživatele).</span><span class="sxs-lookup"><span data-stu-id="1460f-110">How to make sure that users have entered a required value in a form (how to validate user input).</span></span>
> - <span data-ttu-id="1460f-111">Jak zobrazit chyby ověřování.</span><span class="sxs-lookup"><span data-stu-id="1460f-111">How to display validation errors.</span></span>
> - <span data-ttu-id="1460f-112">Jak přejít na jinou stránku z aktuální stránky.</span><span class="sxs-lookup"><span data-stu-id="1460f-112">How to jump to another page from the current page.</span></span>
>   
> 
> <span data-ttu-id="1460f-113">Popsané funkce a technologie:</span><span class="sxs-lookup"><span data-stu-id="1460f-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="1460f-114">Metoda `Database.Execute`.</span><span class="sxs-lookup"><span data-stu-id="1460f-114">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="1460f-115">Příkaz SQL `Insert Into`</span><span class="sxs-lookup"><span data-stu-id="1460f-115">The SQL `Insert Into` statement</span></span>
> - <span data-ttu-id="1460f-116">Pomocná rutina `Validation`</span><span class="sxs-lookup"><span data-stu-id="1460f-116">The `Validation` helper.</span></span>
> - <span data-ttu-id="1460f-117">Metoda `Response.Redirect`.</span><span class="sxs-lookup"><span data-stu-id="1460f-117">The `Response.Redirect` method.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="1460f-118">Co sestavíte</span><span class="sxs-lookup"><span data-stu-id="1460f-118">What You'll Build</span></span>

<span data-ttu-id="1460f-119">V předchozím kurzu, který ukázal, jak vytvořit databázi, jste zadali data databáze tak, že upravíte databázi přímo v WebMatrixu a pracujete v pracovním prostoru **databáze** .</span><span class="sxs-lookup"><span data-stu-id="1460f-119">In the tutorial earlier that showed you how to create a database, you entered database data by editing the database directly in WebMatrix, working in the **Database** workspace.</span></span> <span data-ttu-id="1460f-120">Ve většině aplikací to není praktický způsob, jak vložit data do databáze, i když.</span><span class="sxs-lookup"><span data-stu-id="1460f-120">In most apps, that's not a practical way to put data into the database, though.</span></span> <span data-ttu-id="1460f-121">Takže v tomto kurzu vytvoříte webové rozhraní, které vám umožní nebo kdokoli zadá data a uloží je do databáze.</span><span class="sxs-lookup"><span data-stu-id="1460f-121">So in this tutorial, you'll create a web-based interface that lets you or anyone enter data and save it to the database.</span></span>

<span data-ttu-id="1460f-122">Vytvoříte stránku, kde můžete zadat nové filmy.</span><span class="sxs-lookup"><span data-stu-id="1460f-122">You'll create a page where you can enter new movies.</span></span> <span data-ttu-id="1460f-123">Stránka bude obsahovat formulář, který obsahuje pole (textová pole), kde můžete zadat název, Žánr a rok videa.</span><span class="sxs-lookup"><span data-stu-id="1460f-123">The page will contain an entry form that has fields (text boxes) where you can enter a movie title, genre, and year.</span></span> <span data-ttu-id="1460f-124">Stránka bude vypadat jako na této stránce:</span><span class="sxs-lookup"><span data-stu-id="1460f-124">The page will look like this page:</span></span>

![Stránka Přidat film v prohlížeči](entering-data/_static/image1.png)

<span data-ttu-id="1460f-126">Textová pole budou prvky HTML `<input>`, které budou vypadat jako tento kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-126">The text boxes will be HTML `<input>` elements that will look like this markup:</span></span>

`<input type="text" name="genre" value="" />`

## <a name="creating-the-basic-entry-form"></a><span data-ttu-id="1460f-127">Vytvoření základního formuláře položky</span><span class="sxs-lookup"><span data-stu-id="1460f-127">Creating the Basic Entry Form</span></span>

<span data-ttu-id="1460f-128">Vytvořte stránku s názvem *AddMovie. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="1460f-128">Create a page named *AddMovie.cshtml*.</span></span>

<span data-ttu-id="1460f-129">Nahraďte obsah souboru následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="1460f-129">Replace what's in the file with the following markup.</span></span> <span data-ttu-id="1460f-130">Přepsat vše; v krátké době přidáte blok kódu.</span><span class="sxs-lookup"><span data-stu-id="1460f-130">Overwrite everything; you'll add a code block at the top shortly.</span></span>

[!code-cshtml[Main](entering-data/samples/sample1.cshtml)]

<span data-ttu-id="1460f-131">Tento příklad ukazuje typický kód HTML pro vytvoření formuláře.</span><span class="sxs-lookup"><span data-stu-id="1460f-131">This example shows typical HTML for creating a form.</span></span> <span data-ttu-id="1460f-132">Používá prvky `<input>` pro textová pole a pro tlačítko Odeslat.</span><span class="sxs-lookup"><span data-stu-id="1460f-132">It uses `<input>` elements for the text boxes and for the submit button.</span></span> <span data-ttu-id="1460f-133">Titulky pro textová pole jsou vytvořeny pomocí standardních `<label>` prvků.</span><span class="sxs-lookup"><span data-stu-id="1460f-133">The captions for the text boxes are created by using standard `<label>` elements.</span></span> <span data-ttu-id="1460f-134">Prvky `<fieldset>` a `<legend>` vloží do formuláře Skvělé pole.</span><span class="sxs-lookup"><span data-stu-id="1460f-134">The `<fieldset>` and `<legend>` elements put a nice box around the form.</span></span>

<span data-ttu-id="1460f-135">Všimněte si, že na této stránce `<form>` element používá `post` jako hodnotu atributu `method`.</span><span class="sxs-lookup"><span data-stu-id="1460f-135">Notice that in this page, the `<form>` element uses `post` as the value for the `method` attribute.</span></span> <span data-ttu-id="1460f-136">V předchozím kurzu jste vytvořili formulář, který používal metodu `get`.</span><span class="sxs-lookup"><span data-stu-id="1460f-136">In the previous tutorial, you created a form that used the `get` method.</span></span> <span data-ttu-id="1460f-137">To bylo správné, protože ačkoliv formulář odeslal hodnoty na server, požadavek provedl žádné změny.</span><span class="sxs-lookup"><span data-stu-id="1460f-137">That was correct, because although the form submitted values to the server, the request did not make any changes.</span></span> <span data-ttu-id="1460f-138">Všechna data byla načtena různými způsoby.</span><span class="sxs-lookup"><span data-stu-id="1460f-138">All it did was fetch data in different ways.</span></span> <span data-ttu-id="1460f-139">Na této stránce ale *provedete* změny – Chystáte se přidat nové záznamy databáze.</span><span class="sxs-lookup"><span data-stu-id="1460f-139">However, in this page you *will* make changes—you're going to add new database records.</span></span> <span data-ttu-id="1460f-140">Proto by tato forma měla používat metodu `post`.</span><span class="sxs-lookup"><span data-stu-id="1460f-140">Therefore, this form should use the `post` method.</span></span> <span data-ttu-id="1460f-141">(Další informace o rozdílu mezi operacemi `GET` a `POST` najdete v předchozím kurzu na postranním panelu pro[získání, odeslání a zabezpečení operací HTTP](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) .)</span><span class="sxs-lookup"><span data-stu-id="1460f-141">(For more about the difference between `GET` and `POST` operations, see the[GET, POST, and HTTP Verb Safety](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the previous tutorial.)</span></span>

<span data-ttu-id="1460f-142">Všimněte si, že každé textové pole má element `name` (`title`, `genre`, `year`).</span><span class="sxs-lookup"><span data-stu-id="1460f-142">Note that each text box has a `name` element (`title`, `genre`, `year`).</span></span> <span data-ttu-id="1460f-143">Jak jste viděli v předchozím kurzu, tyto názvy jsou důležité, protože musíte mít tyto názvy, abyste mohli později získat vstup uživatele.</span><span class="sxs-lookup"><span data-stu-id="1460f-143">As you saw in the previous tutorial, these names are important because you must have those names so you can get the user's input later.</span></span> <span data-ttu-id="1460f-144">Můžete použít libovolný název.</span><span class="sxs-lookup"><span data-stu-id="1460f-144">You can use any names.</span></span> <span data-ttu-id="1460f-145">Je užitečné použít smysluplné názvy, které vám pomůžou zapamatovat si, se kterými daty pracujete.</span><span class="sxs-lookup"><span data-stu-id="1460f-145">It's helpful to use meaningful names that help you remember what data you're working with.</span></span>

<span data-ttu-id="1460f-146">Atribut `value` každého prvku `<input>` obsahuje bitovou kopii kódu Razor (například `Request.Form["title"]`).</span><span class="sxs-lookup"><span data-stu-id="1460f-146">The `value` attribute of each `<input>` element contains a bit of Razor code (for example, `Request.Form["title"]`).</span></span> <span data-ttu-id="1460f-147">Zjistili jste ve verzi tohoto štychu v předchozím kurzu, abyste zachovali hodnotu zadanou do textového pole (pokud existuje) po odeslání formuláře.</span><span class="sxs-lookup"><span data-stu-id="1460f-147">You learned a version of this trick in the previous tutorial to preserve the value entered into the text box (if any) after the form has been submitted.</span></span>

## <a name="getting-the-form-values"></a><span data-ttu-id="1460f-148">Získání hodnot formuláře</span><span class="sxs-lookup"><span data-stu-id="1460f-148">Getting the Form Values</span></span>

<span data-ttu-id="1460f-149">Dále přidáte kód, který zpracovává formulář.</span><span class="sxs-lookup"><span data-stu-id="1460f-149">Next, you add code that processes the form.</span></span> <span data-ttu-id="1460f-150">V osnově provedete následující akce:</span><span class="sxs-lookup"><span data-stu-id="1460f-150">In outline, you'll do the following:</span></span>

1. <span data-ttu-id="1460f-151">Ověřte, zda je stránka publikována (odeslána).</span><span class="sxs-lookup"><span data-stu-id="1460f-151">Check whether the page is being posted (was submitted).</span></span> <span data-ttu-id="1460f-152">Chcete, aby se váš kód spouštěl pouze v případě, že uživatel klikl na tlačítko, nikoli při prvním spuštění stránky.</span><span class="sxs-lookup"><span data-stu-id="1460f-152">You want to your code to run only when users have clicked the button, not when the page first runs.</span></span>
2. <span data-ttu-id="1460f-153">Načte hodnoty, které uživatel zadal do textových polí.</span><span class="sxs-lookup"><span data-stu-id="1460f-153">Get the values that the user entered into the text boxes.</span></span> <span data-ttu-id="1460f-154">V takovém případě, protože formulář používá příkaz `POST`, získáte hodnoty formuláře z kolekce `Request.Form`.</span><span class="sxs-lookup"><span data-stu-id="1460f-154">In this case, because the form is using the `POST` verb, you get the form values from the `Request.Form` collection.</span></span>
3. <span data-ttu-id="1460f-155">Vložte hodnoty jako nový záznam do tabulky databáze *filmů* .</span><span class="sxs-lookup"><span data-stu-id="1460f-155">Insert the values as a new record in the *Movies* database table.</span></span>

<span data-ttu-id="1460f-156">V horní části souboru přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-156">At the top of the file, add the following code:</span></span>

[!code-cshtml[Main](entering-data/samples/sample2.cshtml)]

<span data-ttu-id="1460f-157">První pár řádků vytvoří proměnné (`title`, `genre`a `year`), aby se hodnoty z textových polí vešly.</span><span class="sxs-lookup"><span data-stu-id="1460f-157">The first few lines create variables (`title`, `genre`, and `year`) to hold the values from the text boxes.</span></span> <span data-ttu-id="1460f-158">Řádková `if(IsPost)` zajistí, že proměnné jsou nastaveny *pouze* v případě, že uživatel klikne na tlačítko **přidat video** – to znamená, když byl formulář publikován.</span><span class="sxs-lookup"><span data-stu-id="1460f-158">The line `if(IsPost)` makes sure that the variables are set *only* when users click the **Add Movie** button — that is, when the form has been posted.</span></span>

<span data-ttu-id="1460f-159">Jak jste viděli v předchozím kurzu, získáte hodnotu textového pole pomocí výrazu, jako je `Request.Form["name"]`, kde *název* je název `<input>` elementu.</span><span class="sxs-lookup"><span data-stu-id="1460f-159">As you saw in an earlier tutorial, you get the value of a text box by using an expression like `Request.Form["name"]`, where *name* is the name of the `<input>` element.</span></span>

<span data-ttu-id="1460f-160">Názvy proměnných (`title`, `genre`a `year`) jsou libovolné.</span><span class="sxs-lookup"><span data-stu-id="1460f-160">The names of the variables (`title`, `genre`, and `year`) are arbitrary.</span></span> <span data-ttu-id="1460f-161">Podobně jako názvy, které přiřadíte `<input>` prvků, můžete je zavolat libovolným způsobem.</span><span class="sxs-lookup"><span data-stu-id="1460f-161">Like the names that you assign to `<input>` elements, you can call them anything you like.</span></span> <span data-ttu-id="1460f-162">(Názvy proměnných nemusejí odpovídat atributům názvu `<input>` prvků ve formuláři.) Ale stejně jako u elementů `<input>` je vhodné použít názvy proměnných, které odráží data, která obsahují.</span><span class="sxs-lookup"><span data-stu-id="1460f-162">(The names of the variables don't have to match the name attributes of `<input>` elements on the form.) But as with the `<input>` elements, it's a good idea to use variable names that reflect the data that they contain.</span></span> <span data-ttu-id="1460f-163">Když píšete kód, konzistentní názvy usnadňují zapamatování, se kterými daty pracujete.</span><span class="sxs-lookup"><span data-stu-id="1460f-163">When you write code, consistent names make it easier for you to remember what data you're working with.</span></span>

## <a name="adding-data-to-the-database"></a><span data-ttu-id="1460f-164">Přidání dat do databáze</span><span class="sxs-lookup"><span data-stu-id="1460f-164">Adding Data to the Database</span></span>

<span data-ttu-id="1460f-165">V bloku kódu, který jste právě přidali, těsně *uvnitř* uzavírací složené závorky (`}`) bloku `if` (ne pouze uvnitř bloku kódu) přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-165">In the code block you just added, just *inside* the closing brace ( `}` ) of the `if` block (not just inside the code block), add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample3.cs)]

<span data-ttu-id="1460f-166">Tento příklad je podobný kódu, který jste použili v předchozím kurzu pro načtení a zobrazení dat.</span><span class="sxs-lookup"><span data-stu-id="1460f-166">This example is similar to the code you used in a previous tutorial to fetch and display data.</span></span> <span data-ttu-id="1460f-167">Řádek, který začíná `db =` otevře databázi, například dřív, a další řádek definuje příkaz SQL znovu, jak jste viděli předtím.</span><span class="sxs-lookup"><span data-stu-id="1460f-167">The line that starts with `db =` opens the database, like before, and the next line defines a SQL statement, again as you saw before.</span></span> <span data-ttu-id="1460f-168">Tentokrát ale definuje příkaz SQL `Insert Into`.</span><span class="sxs-lookup"><span data-stu-id="1460f-168">However, this time it defines a SQL `Insert Into` statement.</span></span> <span data-ttu-id="1460f-169">Následující příklad ukazuje obecnou syntaxi příkazu `Insert Into`:</span><span class="sxs-lookup"><span data-stu-id="1460f-169">The following example shows the general syntax of the `Insert Into` statement:</span></span>

`INSERT INTO table (column1, column2, column3, ...) VALUES (value1, value2, value3, ...)`

<span data-ttu-id="1460f-170">Jinými slovy, zadáte tabulku, do které se má vložit, a pak uveďte sloupce, které se mají vložit do, a potom vypište hodnoty, které chcete vložit.</span><span class="sxs-lookup"><span data-stu-id="1460f-170">In other words, you specify the table to insert into, then list the columns to insert into, and then list the values to insert.</span></span> <span data-ttu-id="1460f-171">(Jak je uvedeno dříve, SQL nerozlišuje velká a malá písmena, ale někteří lidé mají na velká písmena, aby usnadnili čtení příkazu.)</span><span class="sxs-lookup"><span data-stu-id="1460f-171">(As noted before, SQL is not case sensitive but some people capitalize the keywords to make it easier to read the command.)</span></span>

<span data-ttu-id="1460f-172">Sloupce, které vkládáte do, jsou již uvedeny v příkazu – `(Title, Genre, Year)`.</span><span class="sxs-lookup"><span data-stu-id="1460f-172">The columns that you're inserting into are already listed in the command — `(Title, Genre, Year)`.</span></span> <span data-ttu-id="1460f-173">Zajímavá část je způsob, jak získat hodnoty z textových polí do `VALUES` části příkazu.</span><span class="sxs-lookup"><span data-stu-id="1460f-173">The interesting part is how you get the values from the text boxes into the `VALUES` part of the command.</span></span> <span data-ttu-id="1460f-174">Místo skutečných hodnot vidíte `@0`, `@1`a `@2`, což jsou zástupné symboly kurzů.</span><span class="sxs-lookup"><span data-stu-id="1460f-174">Instead of actual values, you see `@0`, `@1`, and `@2`, which are of course placeholders.</span></span> <span data-ttu-id="1460f-175">Při spuštění příkazu (na řádku `db.Execute`) předáte hodnoty, které jste získali z textových polí.</span><span class="sxs-lookup"><span data-stu-id="1460f-175">When you run the command (on the `db.Execute` line), you pass the values that you got from the text boxes.</span></span>

<span data-ttu-id="1460f-176">**Významná!**</span><span class="sxs-lookup"><span data-stu-id="1460f-176">**Important!**</span></span> <span data-ttu-id="1460f-177">Mějte na paměti, že jediným způsobem, jak byste měli v příkazu SQL zahrnout data zadaná online uživatelem, je použít zástupné symboly, jak vidíte tady (`VALUES(@0, @1, @2)`).</span><span class="sxs-lookup"><span data-stu-id="1460f-177">Remember that the only way you should ever include data entered online by a user in a SQL statement is to use placeholders, as you see here (`VALUES(@0, @1, @2)`).</span></span> <span data-ttu-id="1460f-178">Pokud do příkazu SQL zřetězete vstup uživatele, otevřete sami sebe s útokem na injektáže SQL, jak je vysvětleno v tématu [základy formulářů na webových stránkách ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581) (předchozí kurz).</span><span class="sxs-lookup"><span data-stu-id="1460f-178">If you concatenate user input into a SQL statement, you open yourself to a SQL injection attack, as explained in [Form Basics in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (the previous tutorial).</span></span>

<span data-ttu-id="1460f-179">V rámci `if` bloku přidejte následující řádek za `db.Execute` řádek:</span><span class="sxs-lookup"><span data-stu-id="1460f-179">Still inside the `if` block, add the following line after the `db.Execute` line:</span></span>

[!code-css[Main](entering-data/samples/sample4.css)]

<span data-ttu-id="1460f-180">Po vložení nového videa do databáze se tento řádek přeskočí (přesměrovává) na stránku *filmy* , abyste viděli film, který jste právě zadali.</span><span class="sxs-lookup"><span data-stu-id="1460f-180">After the new movie has been inserted into the database, this line jumps you (redirects) to the *Movies* page so you can see the movie you just entered.</span></span> <span data-ttu-id="1460f-181">Operátor `~` znamená "kořen webu".</span><span class="sxs-lookup"><span data-stu-id="1460f-181">The `~` operator means "root of the website."</span></span> <span data-ttu-id="1460f-182">(Operátor `~` funguje pouze na stránkách ASP.NET, ne v HTML všeobecně.)</span><span class="sxs-lookup"><span data-stu-id="1460f-182">(The `~` operator works only in ASP.NET pages, not in HTML generally.)</span></span>

<span data-ttu-id="1460f-183">Úplný blok kódu vypadá jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="1460f-183">The complete code block looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample5.cshtml)]

## <a name="testing-the-insert-command-so-far"></a><span data-ttu-id="1460f-184">Testování příkazu INSERT (zatím daleko)</span><span class="sxs-lookup"><span data-stu-id="1460f-184">Testing the Insert Command (So Far)</span></span>

<span data-ttu-id="1460f-185">Ještě nejste hotovi, ale teď je vhodná doba k otestování.</span><span class="sxs-lookup"><span data-stu-id="1460f-185">You're not done yet, but now is a good time to test.</span></span>

<span data-ttu-id="1460f-186">Ve stromovém zobrazení souborů ve WebMatrixu klikněte pravým tlačítkem na stránku *AddMovie. cshtml* a pak klikněte na **Spustit v prohlížeči**.</span><span class="sxs-lookup"><span data-stu-id="1460f-186">In the tree view of files in WebMatrix, right-click the *AddMovie.cshtml* page and then click **Launch in browser**.</span></span>

![Stránka Přidat film v prohlížeči](entering-data/_static/image2.png)

<span data-ttu-id="1460f-188">(Pokud skončíte s jinou stránkou v prohlížeči, ujistěte se, že adresa URL je `http://localhost:nnnnn/AddMovie`), kde *nnnnn* je číslo portu, které používáte.)</span><span class="sxs-lookup"><span data-stu-id="1460f-188">(If you end up with a different page in the browser, make sure that the URL is `http://localhost:nnnnn/AddMovie`), where *nnnnn* is the port number that you're using.)</span></span>

<span data-ttu-id="1460f-189">Zobrazila se chybová stránka?</span><span class="sxs-lookup"><span data-stu-id="1460f-189">Did you get an error page?</span></span> <span data-ttu-id="1460f-190">Pokud ano, přečtěte si je pečlivě a ujistěte se, že kód přesně vypadá, co byl uveden výše.</span><span class="sxs-lookup"><span data-stu-id="1460f-190">If so, read it carefully and make sure that the code looks exactly what was listed earlier.</span></span>

<span data-ttu-id="1460f-191">Zadejte film do formuláře &mdash; například, použijte "občanský Kane", "drama" a "1941".</span><span class="sxs-lookup"><span data-stu-id="1460f-191">Enter a movie in the form &mdash; for example, use "Citizen Kane", "Drama", and "1941".</span></span> <span data-ttu-id="1460f-192">(Nebo cokoli.) Pak klikněte na **Přidat film**.</span><span class="sxs-lookup"><span data-stu-id="1460f-192">(Or whatever.) Then click **Add Movie**.</span></span>

<span data-ttu-id="1460f-193">Pokud vše proběhne správně, budete přesměrováni na stránku *filmy* .</span><span class="sxs-lookup"><span data-stu-id="1460f-193">If all goes well, you're redirected to the *Movies* page.</span></span> <span data-ttu-id="1460f-194">Ujistěte se, že je uvedený nový film.</span><span class="sxs-lookup"><span data-stu-id="1460f-194">Make sure that your new movie is listed.</span></span>

![Stránka filmy zobrazující nově přidaný film](entering-data/_static/image3.png)

## <a name="validating-user-input"></a><span data-ttu-id="1460f-196">Ověřování vstupu uživatele</span><span class="sxs-lookup"><span data-stu-id="1460f-196">Validating User Input</span></span>

<span data-ttu-id="1460f-197">Vraťte se na stránku *AddMovie* nebo ji znovu spusťte.</span><span class="sxs-lookup"><span data-stu-id="1460f-197">Go back to the *AddMovie* page, or run it again.</span></span> <span data-ttu-id="1460f-198">Zadejte jiný film, ale tentokrát zadejte jenom nadpis &mdash; například, do pole deště zadejte "singin'".</span><span class="sxs-lookup"><span data-stu-id="1460f-198">Enter another movie, but this time, enter only the title &mdash; for example, enter "Singin' in the Rain".</span></span> <span data-ttu-id="1460f-199">Pak klikněte na **Přidat film**.</span><span class="sxs-lookup"><span data-stu-id="1460f-199">Then click **Add Movie**.</span></span>

<span data-ttu-id="1460f-200">Budete přesměrováni na stránku *filmy* znovu.</span><span class="sxs-lookup"><span data-stu-id="1460f-200">You're redirected to the *Movies* page again.</span></span> <span data-ttu-id="1460f-201">Nový film můžete najít, ale neúplný.</span><span class="sxs-lookup"><span data-stu-id="1460f-201">You can find the new movie, but it's incomplete.</span></span>

![Stránka filmy s informacemi o novém videu, u kterých chybí některé hodnoty](entering-data/_static/image4.png)

<span data-ttu-id="1460f-203">Při vytváření tabulky *filmů* jste výslovně uvedli, že žádná pole by mohla mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1460f-203">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="1460f-204">Tady máte vstupní formulář pro nové filmy a zanecháváte si pole prázdné.</span><span class="sxs-lookup"><span data-stu-id="1460f-204">Here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="1460f-205">To je chyba.</span><span class="sxs-lookup"><span data-stu-id="1460f-205">That's an error.</span></span>

<span data-ttu-id="1460f-206">V tomto případě databáze ve skutečnosti nevyvolává (nebo *vyvolává*) chybu.</span><span class="sxs-lookup"><span data-stu-id="1460f-206">In this case, the database didn't actually raise (or *throw*) an error.</span></span> <span data-ttu-id="1460f-207">Nezadali jste Žánr nebo rok, takže kód na stránce *AddMovie* tyto hodnoty považuje za vyvolané *prázdné řetězce*.</span><span class="sxs-lookup"><span data-stu-id="1460f-207">You didn't supply a genre or year, so the code in the *AddMovie* page treated those values as so-called *empty strings*.</span></span> <span data-ttu-id="1460f-208">Když jste spustili příkaz SQL `Insert Into`, pole žánru a roku neobsahovala v nich užitečná data, ale nemají hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1460f-208">When the SQL `Insert Into` command ran, the genre and year fields didn't have useful data in them, but they weren't null.</span></span>

<span data-ttu-id="1460f-209">Zjevně nechcete uživatelům umožnit zadávat do databáze informace o polovičním vyprázdnění filmů.</span><span class="sxs-lookup"><span data-stu-id="1460f-209">Obviously, you don't want to let users enter half-empty movie information into the database.</span></span> <span data-ttu-id="1460f-210">Řešením je ověřit vstup uživatele.</span><span class="sxs-lookup"><span data-stu-id="1460f-210">The solution is to validate the user's input.</span></span> <span data-ttu-id="1460f-211">Ve výchozím nastavení se ověří, že uživatel zadal hodnotu pro všechna pole (to znamená, že žádná z nich neobsahuje prázdný řetězec).</span><span class="sxs-lookup"><span data-stu-id="1460f-211">Initially, the validation will simply make sure that the user has entered a value for all of the fields (that is, that none of them contains an empty string).</span></span>

> [!TIP]
> 
> <span data-ttu-id="1460f-212">**Null a prázdné řetězce**</span><span class="sxs-lookup"><span data-stu-id="1460f-212">**Null and Empty Strings**</span></span>
> 
> <span data-ttu-id="1460f-213">V programování existuje rozdíl mezi různými pojmy "žádná hodnota".</span><span class="sxs-lookup"><span data-stu-id="1460f-213">In programming, there's a distinction between different notions of "no value."</span></span> <span data-ttu-id="1460f-214">Obecně je hodnota *null* , pokud nebyla nikdy nastavena nebo inicializována jakýmkoli způsobem.</span><span class="sxs-lookup"><span data-stu-id="1460f-214">In general, a value is *null* if it has never been set or initialized in any way.</span></span> <span data-ttu-id="1460f-215">Naproti tomu proměnná, která očekává znaková data (řetězce), může být nastavena na *prázdný řetězec*.</span><span class="sxs-lookup"><span data-stu-id="1460f-215">In contrast, a variable that expects character data (strings) can be set to an *empty string*.</span></span> <span data-ttu-id="1460f-216">V takovém případě hodnota není null; je pouze explicitně nastaveno na řetězec znaků, jejichž délka je nula.</span><span class="sxs-lookup"><span data-stu-id="1460f-216">In that case, the value is not null; it's just been explicitly set to a string of characters whose length is zero.</span></span> <span data-ttu-id="1460f-217">Tyto dva příkazy znázorňují rozdíl:</span><span class="sxs-lookup"><span data-stu-id="1460f-217">These two statements show the difference:</span></span>
> 
> [!code-csharp[Main](entering-data/samples/sample6.cs)]
> 
> <span data-ttu-id="1460f-218">Je to poněkud složitější než to, ale důležité je, že `null` představuje řazení neurčitého stavu.</span><span class="sxs-lookup"><span data-stu-id="1460f-218">It's a little more complicated than that, but the important point is that `null` represents a sort of undetermined state.</span></span>
> 
> <span data-ttu-id="1460f-219">Nyní je důležité pochopit přesně, pokud je hodnota null, a když je pouze prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="1460f-219">Now and then it's important to understand exactly when a value is null and when it's just an empty string.</span></span> <span data-ttu-id="1460f-220">V kódu stránky *AddMovie* získáte hodnoty textových polí pomocí `Request.Form["title"]` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="1460f-220">In the code for the *AddMovie* page, you get the values of the text boxes by using `Request.Form["title"]` and so on.</span></span> <span data-ttu-id="1460f-221">Při prvním spuštění stránky (před kliknutím na tlačítko) je hodnota `Request.Form["title"]` null.</span><span class="sxs-lookup"><span data-stu-id="1460f-221">When the page first runs (before you click the button), the value of `Request.Form["title"]` is null.</span></span> <span data-ttu-id="1460f-222">Ale při odeslání formuláře `Request.Form["title"]` Získá hodnotu `title` textového pole.</span><span class="sxs-lookup"><span data-stu-id="1460f-222">But when you submit the form, `Request.Form["title"]` gets the value of the `title` text box.</span></span> <span data-ttu-id="1460f-223">Není zřejmé, ale prázdné textové pole není null. obsahuje pouze prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="1460f-223">It's not obvious, but an empty text box is not null; it just has an empty string in it.</span></span> <span data-ttu-id="1460f-224">Takže když se kód spustí jako odpověď na kliknutí na tlačítko, `Request.Form["title"]` obsahuje prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="1460f-224">So when the code runs in response to the button click, `Request.Form["title"]` has an empty string in it.</span></span>
> 
> <span data-ttu-id="1460f-225">Proč je tento rozdíl důležitý?</span><span class="sxs-lookup"><span data-stu-id="1460f-225">Why is this distinction important?</span></span> <span data-ttu-id="1460f-226">Při vytváření tabulky *filmů* jste výslovně uvedli, že žádná pole by mohla mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1460f-226">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="1460f-227">Ale tady máte vstupní formulář pro nové filmy a necháte pole prázdná.</span><span class="sxs-lookup"><span data-stu-id="1460f-227">But here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="1460f-228">Měli byste očekávat, že databáze bude stěžovatelem při pokusu o uložení nových filmů, které neobsahovaly hodnoty pro žánr nebo rok.</span><span class="sxs-lookup"><span data-stu-id="1460f-228">You would reasonably expect the database to complain when you tried to save new movies that didn't have values for genre or year.</span></span> <span data-ttu-id="1460f-229">Ale to je &mdash;, i když necháte tato textová pole prázdná, hodnoty nemůžou být null. Jedná se o prázdné řetězce.</span><span class="sxs-lookup"><span data-stu-id="1460f-229">But that's the point &mdash; even if you leave those text boxes blank, the values aren't null; they're empty strings.</span></span> <span data-ttu-id="1460f-230">V důsledku toho můžete uložit nové filmy do databáze s prázdnými sloupci &mdash;, ale ne null!</span><span class="sxs-lookup"><span data-stu-id="1460f-230">As a result, you're able to save new movies to the database with these columns empty &mdash; but not null!</span></span> <span data-ttu-id="1460f-231">&mdash; hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1460f-231">&mdash; values.</span></span> <span data-ttu-id="1460f-232">Proto je nutné se ujistit, že uživatelé neodesílají prázdný řetězec, který můžete provést ověřením vstupu uživatele.</span><span class="sxs-lookup"><span data-stu-id="1460f-232">Therefore, you have to make sure that users don't submit an empty string, which you can do by validating the user's input.</span></span>

### <a name="the-validation-helper"></a><span data-ttu-id="1460f-233">Pomocník pro ověřování</span><span class="sxs-lookup"><span data-stu-id="1460f-233">The Validation Helper</span></span>

<span data-ttu-id="1460f-234">Webové stránky ASP.NET obsahují nápovědu &mdash; `Validation` pomocníka &mdash;, pomocí kterých se můžete ujistit, že uživatelé zadávají data vyhovující vašim požadavkům.</span><span class="sxs-lookup"><span data-stu-id="1460f-234">ASP.NET Web Pages includes a helper &mdash; the `Validation` helper &mdash; that you can use to make sure that users enter data that meets your requirements.</span></span> <span data-ttu-id="1460f-235">Pomocník pro `Validation` je jedním z pomocných rutin, které jsou integrované pro ASP.NET webových stránek, takže je nemusíte instalovat jako balíček pomocí NuGet, jak jste nainstalovali pomocníka Gravatar v předchozím kurzu.</span><span class="sxs-lookup"><span data-stu-id="1460f-235">The `Validation` helper is one of the helpers that's built in to ASP.NET Web Pages, so you don't have to install it as a package by using NuGet, the way you installed the Gravatar helper in an earlier tutorial.</span></span>

<span data-ttu-id="1460f-236">Chcete-li ověřit vstup uživatele, proveďte následující akce:</span><span class="sxs-lookup"><span data-stu-id="1460f-236">To validate the user's input, you'll do the following:</span></span>

- <span data-ttu-id="1460f-237">Pomocí kódu určete, že chcete v textových polích na stránce vyžadovat hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1460f-237">Use code to specify that you want to require values in the text boxes on the page.</span></span>
- <span data-ttu-id="1460f-238">Vložte do kódu test, aby byly informace o videu přidány do databáze pouze v případě, že vše ověřuje správně.</span><span class="sxs-lookup"><span data-stu-id="1460f-238">Put a test into the code so that the movie information is added to the database only if everything validates properly.</span></span>
- <span data-ttu-id="1460f-239">Přidejte kód do značky pro zobrazení chybových zpráv.</span><span class="sxs-lookup"><span data-stu-id="1460f-239">Add code into the markup to display error messages.</span></span>

<span data-ttu-id="1460f-240">V bloku kódu na stránce *AddMovie* vpravo nahoře před deklaracemi proměnné přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-240">In the code block in the *AddMovie* page, right up at the top before the variable declarations, add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample7.cs)]

<span data-ttu-id="1460f-241">Zavoláte `Validation.RequireField` jednou pro každé pole (`<input>` element), kde chcete vyžadovat zadání.</span><span class="sxs-lookup"><span data-stu-id="1460f-241">You call `Validation.RequireField` once for each field (`<input>` element) where you want to require an entry.</span></span> <span data-ttu-id="1460f-242">Můžete také přidat vlastní chybovou zprávu pro každé volání, jako tady vidíte.</span><span class="sxs-lookup"><span data-stu-id="1460f-242">You can also add a custom error message for each call, like you see here.</span></span> <span data-ttu-id="1460f-243">(Tyto zprávy jsme rozlišili tak, aby ukázaly, že zde můžete nic dělat.)</span><span class="sxs-lookup"><span data-stu-id="1460f-243">(We varied the messages just to show that you can put anything you like there.)</span></span>

<span data-ttu-id="1460f-244">Pokud dojde k potížím, chcete zabránit vložení nových informací o videu do databáze.</span><span class="sxs-lookup"><span data-stu-id="1460f-244">If there's a problem, you want to prevent the new movie information from being inserted into the database.</span></span> <span data-ttu-id="1460f-245">V bloku `if(IsPost)` použijte `&&` (Logical AND) k přidání další podmínky, která testuje `Validation.IsValid()`.</span><span class="sxs-lookup"><span data-stu-id="1460f-245">In the `if(IsPost)` block, use `&&` (logical AND) to add another condition that tests `Validation.IsValid()`.</span></span> <span data-ttu-id="1460f-246">Až skončíte, celý `if(IsPost)` blok vypadá jako tento kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-246">When you're done, the whole `if(IsPost)` block looks like this code:</span></span>

[!code-csharp[Main](entering-data/samples/sample8.cs)]

<span data-ttu-id="1460f-247">Pokud dojde k chybě ověřování u libovolného pole, které jste zaregistrovali pomocí pomocné rutiny `Validation`, vrátí metoda `Validation.IsValid` hodnotu false.</span><span class="sxs-lookup"><span data-stu-id="1460f-247">If there's a validation error with any of the fields that you registered by using the `Validation` helper, the `Validation.IsValid` method returns false.</span></span> <span data-ttu-id="1460f-248">A v takovém případě žádný kód v tomto bloku nebude spuštěn, takže do databáze nebudou vloženy žádné neplatné záznamy filmů.</span><span class="sxs-lookup"><span data-stu-id="1460f-248">And in that case, none of the code in that block will run, so no invalid movie entries will be inserted into the database.</span></span> <span data-ttu-id="1460f-249">A samozřejmě nejste přesměrováni na stránku *filmy* .</span><span class="sxs-lookup"><span data-stu-id="1460f-249">And of course you're not redirected to the *Movies* page.</span></span>

<span data-ttu-id="1460f-250">Kompletní blok kódu, včetně ověřovacího kódu, teď vypadá jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="1460f-250">The complete code block, including the validation code, now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample9.cshtml?highlight=10)]

## <a name="displaying-validation-errors"></a><span data-ttu-id="1460f-251">Zobrazení chyb ověřování</span><span class="sxs-lookup"><span data-stu-id="1460f-251">Displaying Validation Errors</span></span>

<span data-ttu-id="1460f-252">Posledním krokem je zobrazení všech chybových zpráv.</span><span class="sxs-lookup"><span data-stu-id="1460f-252">The last step is to display any error messages.</span></span> <span data-ttu-id="1460f-253">Můžete zobrazit jednotlivé zprávy pro každou chybu ověřování, nebo můžete zobrazit souhrn nebo obojí.</span><span class="sxs-lookup"><span data-stu-id="1460f-253">You can display individual messages for each validation error, or you can display a summary, or both.</span></span> <span data-ttu-id="1460f-254">V tomto kurzu provedete obojí, abyste viděli, jak funguje.</span><span class="sxs-lookup"><span data-stu-id="1460f-254">For this tutorial, you'll do both so that you can see how it works.</span></span>

<span data-ttu-id="1460f-255">Vedle každého prvku `<input>`, který ověřujete, zavolejte metodu `Html.ValidationMessage` a předejte jí název `<input>`ho prvku, který ověřujete.</span><span class="sxs-lookup"><span data-stu-id="1460f-255">Next to each `<input>` element that you're validating, call the `Html.ValidationMessage` method and pass it the name of the `<input>` element you're validating.</span></span> <span data-ttu-id="1460f-256">Metodu `Html.ValidationMessage` vložíte přímo tam, kde chcete, aby se zobrazila chybová zpráva.</span><span class="sxs-lookup"><span data-stu-id="1460f-256">You put the `Html.ValidationMessage` method right where you want the error message to appear.</span></span> <span data-ttu-id="1460f-257">Při spuštění stránky vykreslí metoda `Html.ValidationMessage` `<span>` elementu, kde se zobrazí chyba ověřování.</span><span class="sxs-lookup"><span data-stu-id="1460f-257">When the page runs, the `Html.ValidationMessage` method renders a `<span>` element where the validation error will go.</span></span> <span data-ttu-id="1460f-258">(Pokud není k dispozici žádná chyba, element `<span>` je vykreslen, ale v něm není žádný text.)</span><span class="sxs-lookup"><span data-stu-id="1460f-258">(If there's no error, the `<span>` element is rendered, but there's no text in it.)</span></span>

<span data-ttu-id="1460f-259">Změňte značku na stránce tak, aby obsahovala metodu `Html.ValidationMessage` pro každý ze tří `<input>` prvků na stránce, jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="1460f-259">Change the markup in the page so that it includes an `Html.ValidationMessage` method for each of the three `<input>` elements on the page, like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample10.cshtml?highlight=3,8,13)]

<span data-ttu-id="1460f-260">Chcete-li zjistit, jak souhrn funguje, přidejte také následující značku a kód hned za `<h1>Add a Movie</h1>` element na stránce:</span><span class="sxs-lookup"><span data-stu-id="1460f-260">To see how the summary works, also add the following markup and code right after the `<h1>Add a Movie</h1>` element on the page:</span></span>

[!code-cshtml[Main](entering-data/samples/sample11.cshtml)]

<span data-ttu-id="1460f-261">Ve výchozím nastavení metoda `Html.ValidationSummary` zobrazuje všechny zprávy o ověřování v seznamu (`<ul>` element, který je uvnitř elementu `<div>`).</span><span class="sxs-lookup"><span data-stu-id="1460f-261">By default, the `Html.ValidationSummary` method displays all the validation messages in a list (a `<ul>` element that's inside a `<div>` element).</span></span> <span data-ttu-id="1460f-262">Stejně jako u metody `Html.ValidationMessage` se kód pro Shrnutí ověření vždy vykreslí; Pokud nejsou žádné chyby, nejsou vykresleny žádné položky seznamu.</span><span class="sxs-lookup"><span data-stu-id="1460f-262">As with the `Html.ValidationMessage` method, the markup for the validation summary is always rendered; if there are no errors, no list items are rendered.</span></span>

<span data-ttu-id="1460f-263">Souhrn může být alternativní způsob, jak zobrazit ověřovací zprávy místo pomocí metody `Html.ValidationMessage` k zobrazení každé chyby specifické pro každé pole.</span><span class="sxs-lookup"><span data-stu-id="1460f-263">The summary can be an alternative way to display validation messages instead of by using the `Html.ValidationMessage` method to display each field-specific error.</span></span> <span data-ttu-id="1460f-264">Můžete také použít souhrn i podrobnosti.</span><span class="sxs-lookup"><span data-stu-id="1460f-264">Or you can use both a summary and the details.</span></span> <span data-ttu-id="1460f-265">Nebo můžete použít metodu `Html.ValidationSummary` k zobrazení obecné chyby a potom použít jednotlivá volání `Html.ValidationMessage` k zobrazení podrobností.</span><span class="sxs-lookup"><span data-stu-id="1460f-265">Or you can use the `Html.ValidationSummary` method to display a generic error and then use individual `Html.ValidationMessage` calls to display details.</span></span>

<span data-ttu-id="1460f-266">Stránka dokončení teď vypadá jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="1460f-266">The complete page now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample12.cshtml)]

<span data-ttu-id="1460f-267">A to je vše.</span><span class="sxs-lookup"><span data-stu-id="1460f-267">That's it.</span></span> <span data-ttu-id="1460f-268">Nyní můžete stránku otestovat přidáním videa, ale ponechání jednoho nebo více polí.</span><span class="sxs-lookup"><span data-stu-id="1460f-268">You can now test the page by adding a movie but leaving out one or more of the fields.</span></span> <span data-ttu-id="1460f-269">Když to uděláte, zobrazí se následující chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="1460f-269">When you do, you see the following error display:</span></span>

![Přidat stránku videa zobrazující chybové zprávy ověřování](entering-data/_static/image5.png)

## <a name="styling-the-validation-error-messages"></a><span data-ttu-id="1460f-271">Určení stylu chybových zpráv ověřování</span><span class="sxs-lookup"><span data-stu-id="1460f-271">Styling the Validation Error Messages</span></span>

<span data-ttu-id="1460f-272">Vidíte, že se zobrazují chybové zprávy, ale ve skutečnosti se neukládají velmi dobře.</span><span class="sxs-lookup"><span data-stu-id="1460f-272">You can see that there are error messages, but they don't really stand out very well.</span></span> <span data-ttu-id="1460f-273">K dispozici je jednoduchý způsob, jak tyto chybové zprávy naformátovat, ale.</span><span class="sxs-lookup"><span data-stu-id="1460f-273">There's an easy way to style the error messages, though.</span></span>

<span data-ttu-id="1460f-274">Chcete-li naformátovat jednotlivé chybové zprávy, které jsou zobrazeny `Html.ValidationMessage`, vytvořte třídu stylu CSS s názvem `field-validation-error`.</span><span class="sxs-lookup"><span data-stu-id="1460f-274">To style the individual error messages that are displayed by `Html.ValidationMessage`, create a CSS style class named `field-validation-error`.</span></span> <span data-ttu-id="1460f-275">Chcete-li definovat hledání souhrnu ověření, vytvořte třídu stylu CSS s názvem `validation-summary-errors`.</span><span class="sxs-lookup"><span data-stu-id="1460f-275">To define the look for the validation summary, create a CSS style class named `validation-summary-errors`.</span></span>

<span data-ttu-id="1460f-276">Chcete-li zjistit, jak tento postup funguje, přidejte prvek `<style>` do části `<head>` stránky.</span><span class="sxs-lookup"><span data-stu-id="1460f-276">To see how this technique works, add a `<style>` element inside the `<head>` section of the page.</span></span> <span data-ttu-id="1460f-277">Pak definujte třídy stylů s názvem `field-validation-error` a `validation-summary-errors`, které obsahují následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="1460f-277">Then define style classes named `field-validation-error` and `validation-summary-errors` that contain the following rules:</span></span>

[!code-cshtml[Main](entering-data/samples/sample13.cshtml?highlight=4-17)]

<span data-ttu-id="1460f-278">Normálně byste pravděpodobně umístili informace o stylu do samostatného souboru *. CSS* , ale pro zjednodušení můžete je umístit na stránku.</span><span class="sxs-lookup"><span data-stu-id="1460f-278">Normally you'd probably put style information into a separate *.css* file, but for simplicity you can put them in the page for now.</span></span> <span data-ttu-id="1460f-279">(Později v tomto kurzu nastavíte pravidla šablony stylů CSS do samostatného souboru *. CSS* .)</span><span class="sxs-lookup"><span data-stu-id="1460f-279">(Later in this tutorial set, you'll move the CSS rules to a separate *.css* file.)</span></span>

<span data-ttu-id="1460f-280">Pokud dojde k chybě ověřování, metoda `Html.ValidationMessage` vykreslí `<span>` prvek, který obsahuje `class="field-validation-error"`.</span><span class="sxs-lookup"><span data-stu-id="1460f-280">If there's a validation error, the `Html.ValidationMessage` method renders a `<span>` element that includes `class="field-validation-error"`.</span></span> <span data-ttu-id="1460f-281">Přidáním definice stylu pro tuto třídu můžete nakonfigurovat, jak bude zpráva vypadat.</span><span class="sxs-lookup"><span data-stu-id="1460f-281">By adding a style definition for that class, you can configure what the message looks like.</span></span> <span data-ttu-id="1460f-282">Pokud dojde k chybám, metoda `ValidationSummary` podobně dynamicky vykresluje `class="validation-summary-errors"`atributu.</span><span class="sxs-lookup"><span data-stu-id="1460f-282">If there are errors, the `ValidationSummary` method likewise dynamically renders the attribute `class="validation-summary-errors"`.</span></span>

<span data-ttu-id="1460f-283">Spusťte znovu stránku a záměrně ponechte několik polí.</span><span class="sxs-lookup"><span data-stu-id="1460f-283">Run the page again and deliberately leave out a couple of the fields.</span></span> <span data-ttu-id="1460f-284">Chyby jsou nyní mnohem patrné.</span><span class="sxs-lookup"><span data-stu-id="1460f-284">The errors are now more noticeable.</span></span> <span data-ttu-id="1460f-285">(Ve skutečnosti se overdone, ale to je jenom k tomu, abyste viděli, co můžete dělat.)</span><span class="sxs-lookup"><span data-stu-id="1460f-285">(In fact, they're overdone, but that's just to show what you can do.)</span></span>

![Přidat stránku videa ukazující chyby ověřování, které byly ve stylu](entering-data/_static/image6.png)

## <a name="adding-a-link-to-the-movies-page"></a><span data-ttu-id="1460f-287">Přidání odkazu na stránku filmů</span><span class="sxs-lookup"><span data-stu-id="1460f-287">Adding a Link to the Movies Page</span></span>

<span data-ttu-id="1460f-288">Prvním krokem je to, aby bylo možné se dostat na stránku *AddMovie* z původního seznamu filmů.</span><span class="sxs-lookup"><span data-stu-id="1460f-288">One final step is to make it convenient to get to the *AddMovie* page from the original movie listing.</span></span>

<span data-ttu-id="1460f-289">Otevřete stránku *filmy* znovu.</span><span class="sxs-lookup"><span data-stu-id="1460f-289">Open the *Movies* page again.</span></span> <span data-ttu-id="1460f-290">Po uzavírací značce `</div>`, která následuje po `WebGrid` pomocné rutiny, přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="1460f-290">After the closing `</div>` tag that follows the `WebGrid` helper, add the following markup:</span></span>

[!code-cshtml[Main](entering-data/samples/sample14.cshtml)]

<span data-ttu-id="1460f-291">Jak jste viděli dřív, ASP.NET interpretuje operátor `~` jako kořen webu.</span><span class="sxs-lookup"><span data-stu-id="1460f-291">As you saw before, ASP.NET interprets the `~` operator as the root of the website.</span></span> <span data-ttu-id="1460f-292">Nemusíte používat operátor `~`; můžete použít `<a href="./AddMovie">Add a movie</a>` značek nebo nějaký jiný způsob, jak definovat cestu, kterou jazyk HTML rozumí.</span><span class="sxs-lookup"><span data-stu-id="1460f-292">You don't have to use the `~` operator; you could use the markup `<a href="./AddMovie">Add a movie</a>` or some other way to define the path that HTML understands.</span></span> <span data-ttu-id="1460f-293">Ale operátor `~` je dobrým obecným přístupem při vytváření odkazů na stránky Razor, protože je web flexibilnější – Pokud přesunete aktuální stránku do podsložky, bude odkaz pořád přejít na stránku *AddMovie* .</span><span class="sxs-lookup"><span data-stu-id="1460f-293">But the `~` operator is a good general approach when you create links for Razor pages, because it makes the site more flexible — if you move the current page to a subfolder, the link will still go to the *AddMovie* page.</span></span> <span data-ttu-id="1460f-294">(Nezapomeňte, že operátor `~` funguje pouze na stránkách *. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="1460f-294">(Remember that the `~` operator only works in *.cshtml* pages.</span></span> <span data-ttu-id="1460f-295">ASP.NET je srozumitelná, ale nejedná se o standardní HTML.)</span><span class="sxs-lookup"><span data-stu-id="1460f-295">ASP.NET understands it, but it's not standard HTML.)</span></span>

<span data-ttu-id="1460f-296">Až skončíte, spusťte stránku *filmy* .</span><span class="sxs-lookup"><span data-stu-id="1460f-296">When you're done, run the *Movies* page.</span></span> <span data-ttu-id="1460f-297">Bude vypadat jako na této stránce:</span><span class="sxs-lookup"><span data-stu-id="1460f-297">It will look like this page:</span></span>

![Stránka filmy s odkazem na stránku přidat filmy](entering-data/_static/image7.png)

<span data-ttu-id="1460f-299">Klikněte na odkaz **Přidat film** , abyste se ujistili, že přejde na stránku *AddMovie* .</span><span class="sxs-lookup"><span data-stu-id="1460f-299">Click the **Add a movie** link to make sure that it goes to the *AddMovie* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="1460f-300">Připravujeme další</span><span class="sxs-lookup"><span data-stu-id="1460f-300">Coming Up Next</span></span>

<span data-ttu-id="1460f-301">V dalším kurzu se dozvíte, jak umožnit uživatelům upravovat data, která už jsou v databázi.</span><span class="sxs-lookup"><span data-stu-id="1460f-301">In the next tutorial, you'll learn how to let users edit data that's already in the database.</span></span>

## <a name="complete-listing-for-addmovie-page"></a><span data-ttu-id="1460f-302">Úplný výpis pro stránku AddMovie</span><span class="sxs-lookup"><span data-stu-id="1460f-302">Complete Listing for AddMovie Page</span></span>

[!code-cshtml[Main](entering-data/samples/sample15.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="1460f-303">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="1460f-303">Additional Resources</span></span>

- [<span data-ttu-id="1460f-304">Seznámení s ASP.NET webovým programováním pomocí syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="1460f-304">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="1460f-305">[Příkaz SQL INSERT INTO](http://www.w3schools.com/sql/sql_insert.asp) na webu w3schools</span><span class="sxs-lookup"><span data-stu-id="1460f-305">[SQL INSERT INTO Statement](http://www.w3schools.com/sql/sql_insert.asp) on the W3Schools site</span></span>
- <span data-ttu-id="1460f-306">[Ověřování vstupu uživatele na webech webových stránek ASP.NET](https://go.microsoft.com/fwlink/?LinkId=253002).</span><span class="sxs-lookup"><span data-stu-id="1460f-306">[Validating User Input in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span></span> <span data-ttu-id="1460f-307">Další informace o práci s pomocníkem `Validation`.</span><span class="sxs-lookup"><span data-stu-id="1460f-307">More information about working with the `Validation` helper.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="1460f-308">[Předchozí](form-basics.md)
> [Další](updating-data.md)</span><span class="sxs-lookup"><span data-stu-id="1460f-308">[Previous](form-basics.md)
[Next](updating-data.md)</span></span>
