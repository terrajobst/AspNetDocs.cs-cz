---
uid: web-api/overview/security/preventing-cross-site-request-forgery-csrf-attacks
title: Prevence útoků na ASP.NET (CSRF) žádostí mezi weby v MVC
author: MikeWasson
description: Popisuje útok proti padělání požadavků (CSRF) mezi weby a postup implementace antiCSRF měr v ASP.NET Web MVC.
ms.author: riande
ms.date: 12/12/2012
ms.assetid: 81d46f14-8f48-4d8c-830d-cc8d594dc11b
msc.legacyurl: /web-api/overview/security/preventing-cross-site-request-forgery-csrf-attacks
msc.type: authoredcontent
ms.openlocfilehash: 5fb0f8bcc9e587ba4fbbf2b857d3bf7adcaafb94
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/06/2020
ms.locfileid: "78555116"
---
# <a name="preventing-cross-site-request-forgery-csrf-attacks-in-aspnet-mvc-application"></a><span data-ttu-id="ebad8-103">Prevence útoků prostřednictvím CSRF (site-to Request) v aplikaci ASP.NET MVC</span><span class="sxs-lookup"><span data-stu-id="ebad8-103">Preventing Cross-Site Request Forgery (CSRF) Attacks in ASP.NET MVC Application</span></span>

<span data-ttu-id="ebad8-104">o [Jan Wasson](https://github.com/MikeWasson)</span><span class="sxs-lookup"><span data-stu-id="ebad8-104">by [Mike Wasson](https://github.com/MikeWasson)</span></span>

<span data-ttu-id="ebad8-105">Padělání žádostí mezi weby (CSRF) je útok, kdy škodlivý web pošle požadavek na ohrožený web, ve kterém je aktuálně přihlášený uživatel.</span><span class="sxs-lookup"><span data-stu-id="ebad8-105">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in</span></span>

<span data-ttu-id="ebad8-106">Tady je příklad útoku CSRF:</span><span class="sxs-lookup"><span data-stu-id="ebad8-106">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="ebad8-107">Uživatel se do `www.example.com` přihlašuje pomocí ověřování pomocí formulářů.</span><span class="sxs-lookup"><span data-stu-id="ebad8-107">A user logs into `www.example.com` using forms authentication.</span></span>
2. <span data-ttu-id="ebad8-108">Server ověřuje uživatele.</span><span class="sxs-lookup"><span data-stu-id="ebad8-108">The server authenticates the user.</span></span> <span data-ttu-id="ebad8-109">Odpověď ze serveru obsahuje soubor cookie ověřování.</span><span class="sxs-lookup"><span data-stu-id="ebad8-109">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="ebad8-110">Uživatel navštíví škodlivý web, aniž by se odhlásil.</span><span class="sxs-lookup"><span data-stu-id="ebad8-110">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="ebad8-111">Tento škodlivý web obsahuje následující formulář HTML:</span><span class="sxs-lookup"><span data-stu-id="ebad8-111">This malicious site contains the following HTML form:</span></span> 

    [!code-html[Main](preventing-cross-site-request-forgery-csrf-attacks/samples/sample1.html)]

    <span data-ttu-id="ebad8-112">Všimněte si, že akce formuláře provede příspěvky na ohrožený web, nikoli na škodlivý web.</span><span class="sxs-lookup"><span data-stu-id="ebad8-112">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="ebad8-113">Toto je součást CSRF (pro různé lokality).</span><span class="sxs-lookup"><span data-stu-id="ebad8-113">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="ebad8-114">Uživatel klikne na tlačítko Odeslat.</span><span class="sxs-lookup"><span data-stu-id="ebad8-114">The user clicks the submit button.</span></span> <span data-ttu-id="ebad8-115">Prohlížeč zahrnuje ověřovací soubor cookie s požadavkem.</span><span class="sxs-lookup"><span data-stu-id="ebad8-115">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="ebad8-116">Požadavek běží na serveru s kontextem ověřování uživatele a může provádět cokoli, co může ověřený uživatel dělat.</span><span class="sxs-lookup"><span data-stu-id="ebad8-116">The request runs on the server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="ebad8-117">I když tento příklad vyžaduje, aby uživatel mohl kliknout na tlačítko formulář, škodlivá stránka může stejně snadno spustit skript, který odešle formulář automaticky.</span><span class="sxs-lookup"><span data-stu-id="ebad8-117">Although this example requires the user to click the form button, the malicious page could just as easily run a script that submits the form automatically.</span></span> <span data-ttu-id="ebad8-118">Použití protokolu SSL navíc nebrání útoku CSRF, protože škodlivý web může odeslat žádost "https://".</span><span class="sxs-lookup"><span data-stu-id="ebad8-118">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="ebad8-119">Obvykle jsou útoky CSRF na weby, které používají soubory cookie pro ověřování, protože prohlížeče odesílají všechny relevantní soubory cookie do cílového webu.</span><span class="sxs-lookup"><span data-stu-id="ebad8-119">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="ebad8-120">Nicméně útoky CSRF nejsou omezené na zneužití souborů cookie.</span><span class="sxs-lookup"><span data-stu-id="ebad8-120">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="ebad8-121">Například základní ověřování a ověřování algoritmem Digest je také zranitelné.</span><span class="sxs-lookup"><span data-stu-id="ebad8-121">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="ebad8-122">Když se uživatel přihlásí pomocí základního ověřování nebo ověřování hodnotou hash.</span><span class="sxs-lookup"><span data-stu-id="ebad8-122">After a user logs in with Basic or Digest authentication.</span></span> <span data-ttu-id="ebad8-123">prohlížeč automaticky pošle přihlašovací údaje až do ukončení relace.</span><span class="sxs-lookup"><span data-stu-id="ebad8-123">the browser automatically sends the credentials until the session ends.</span></span>

## <a name="anti-forgery-tokens"></a><span data-ttu-id="ebad8-124">Tokeny proti padělání</span><span class="sxs-lookup"><span data-stu-id="ebad8-124">Anti-Forgery Tokens</span></span>

<span data-ttu-id="ebad8-125">Aby se zabránilo útokům CSRF, používá ASP.NET MVC tokeny proti padělání, označované také jako *ověřovací tokeny žádostí*.</span><span class="sxs-lookup"><span data-stu-id="ebad8-125">To help prevent CSRF attacks, ASP.NET MVC uses anti-forgery tokens, also called *request verification tokens*.</span></span>

1. <span data-ttu-id="ebad8-126">Klient požaduje stránku HTML, která obsahuje formulář.</span><span class="sxs-lookup"><span data-stu-id="ebad8-126">The client requests an HTML page that contains a form.</span></span>
2. <span data-ttu-id="ebad8-127">Server obsahuje dvě tokeny v odpovědi.</span><span class="sxs-lookup"><span data-stu-id="ebad8-127">The server includes two tokens in the response.</span></span> <span data-ttu-id="ebad8-128">Jeden token se pošle jako soubor cookie.</span><span class="sxs-lookup"><span data-stu-id="ebad8-128">One token is sent as a cookie.</span></span> <span data-ttu-id="ebad8-129">Druhý je umístěn ve skrytém poli formuláře.</span><span class="sxs-lookup"><span data-stu-id="ebad8-129">The other is placed in a hidden form field.</span></span> <span data-ttu-id="ebad8-130">Tokeny jsou vygenerované náhodně, aby nežádoucí osoba nemohly odhadnout hodnoty.</span><span class="sxs-lookup"><span data-stu-id="ebad8-130">The tokens are generated randomly so that an adversary cannot guess the values.</span></span>
3. <span data-ttu-id="ebad8-131">Když klient formulář odešle, musí odeslat oba tokeny zpátky na server.</span><span class="sxs-lookup"><span data-stu-id="ebad8-131">When the client submits the form, it must send both tokens back to the server.</span></span> <span data-ttu-id="ebad8-132">Klient odešle token souborů cookie jako soubor cookie a odešle token formuláře do dat formuláře.</span><span class="sxs-lookup"><span data-stu-id="ebad8-132">The client sends the cookie token as a cookie, and it sends the form token inside the form data.</span></span> <span data-ttu-id="ebad8-133">(Klient prohlížeče to automaticky provede, když uživatel formulář odešle.)</span><span class="sxs-lookup"><span data-stu-id="ebad8-133">(A browser client automatically does this when the user submits the form.)</span></span>
4. <span data-ttu-id="ebad8-134">Pokud požadavek nezahrnuje obě tokeny, server žádost nepovoluje.</span><span class="sxs-lookup"><span data-stu-id="ebad8-134">If a request does not include both tokens, the server disallows the request.</span></span>

<span data-ttu-id="ebad8-135">Tady je příklad formuláře HTML s tokenem skrytého formuláře:</span><span class="sxs-lookup"><span data-stu-id="ebad8-135">Here is an example of an HTML form with a hidden form token:</span></span>

[!code-html[Main](preventing-cross-site-request-forgery-csrf-attacks/samples/sample2.html)]

<span data-ttu-id="ebad8-136">Tokeny ochrany proti padělání fungují, protože škodlivá stránka nemůže číst tokeny uživatele z důvodu zásad stejného původu.</span><span class="sxs-lookup"><span data-stu-id="ebad8-136">Anti-forgery tokens work because the malicious page cannot read the user's tokens, due to same-origin policies.</span></span> <span data-ttu-id="ebad8-137">([Zásady se stejným zdrojem](http://www.w3.org/Security/wiki/Same_Origin_Policy) brání tomu, aby se dokumenty hostované na dvou různých webech lišily k obsahu.</span><span class="sxs-lookup"><span data-stu-id="ebad8-137">([Same-origin policies](http://www.w3.org/Security/wiki/Same_Origin_Policy) prevent documents hosted on two different sites from accessing each other's content.</span></span> <span data-ttu-id="ebad8-138">Takže v předchozím příkladu může škodlivá stránka odesílat požadavky do example.com, ale nemůže přečíst odpověď.)</span><span class="sxs-lookup"><span data-stu-id="ebad8-138">So in the earlier example, the malicious page can send requests to example.com, but it cannot read the response.)</span></span>

<span data-ttu-id="ebad8-139">Aby nedocházelo k útokům CSRF, používejte k jakýmkoli ověřovacím protokolům tokeny proti padělání, kde prohlížeč po přihlášení uživatele tiše odesílá přihlašovací údaje.</span><span class="sxs-lookup"><span data-stu-id="ebad8-139">To prevent CSRF attacks, use anti-forgery tokens with any authentication protocol where the browser silently sends credentials after the user logs in.</span></span> <span data-ttu-id="ebad8-140">To zahrnuje protokoly ověřování založené na souborech cookie, jako je ověřování pomocí formulářů, a také protokoly, jako je základní ověřování a ověřování algoritmem Digest.</span><span class="sxs-lookup"><span data-stu-id="ebad8-140">This includes cookie-based authentication protocols, such as forms authentication, as well as protocols such as Basic and Digest authentication.</span></span>

<span data-ttu-id="ebad8-141">Pro jakékoli nebezpečné metody (POST, PUT, DELETE) byste měli vyžadovat tokeny pro ochranu proti padělání.</span><span class="sxs-lookup"><span data-stu-id="ebad8-141">You should require anti-forgery tokens for any nonsafe methods (POST, PUT, DELETE).</span></span> <span data-ttu-id="ebad8-142">Také se ujistěte, že bezpečné metody (GET, HEAD) nemají žádné vedlejší účinky.</span><span class="sxs-lookup"><span data-stu-id="ebad8-142">Also, make sure that safe methods (GET, HEAD) do not have any side effects.</span></span> <span data-ttu-id="ebad8-143">Kromě toho platí, že pokud povolíte podporu mezi doménami, jako je CORS nebo JSONP, pak i bezpečné metody, jako je GET, jsou potenciálně zranitelné vůči útokům CSRF, což útočníkovi umožňuje číst potenciálně citlivá data.</span><span class="sxs-lookup"><span data-stu-id="ebad8-143">Moreover, if you enable cross-domain support, such as CORS or JSONP, then even safe methods like GET are potentially vulnerable to CSRF attacks, allowing the attacker to read potentially sensitive data.</span></span>

## <a name="anti-forgery-tokens-in-aspnet-mvc"></a><span data-ttu-id="ebad8-144">Tokeny pro ochranu proti padělání ve ASP.NET MVC</span><span class="sxs-lookup"><span data-stu-id="ebad8-144">Anti-Forgery Tokens in ASP.NET MVC</span></span>

<span data-ttu-id="ebad8-145">Chcete-li přidat tokeny pro ochranu proti padělání na stránku Razor, použijte pomocnou metodu **HtmlHelper. AntiForgeryToken** :</span><span class="sxs-lookup"><span data-stu-id="ebad8-145">To add the anti-forgery tokens to a Razor page, use the **HtmlHelper.AntiForgeryToken** helper method:</span></span>

[!code-cshtml[Main](preventing-cross-site-request-forgery-csrf-attacks/samples/sample3.cshtml)]

<span data-ttu-id="ebad8-146">Tato metoda přidá skryté pole formuláře a také nastaví token souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="ebad8-146">This method adds the hidden form field and also sets the cookie token.</span></span>

## <a name="anti-csrf-and-ajax"></a><span data-ttu-id="ebad8-147">Anti-CSRF a AJAX</span><span class="sxs-lookup"><span data-stu-id="ebad8-147">Anti-CSRF and AJAX</span></span>

<span data-ttu-id="ebad8-148">Token formuláře může být problémem pro požadavky AJAX, protože požadavek AJAX může odesílat data JSON, nikoli data formuláře HTML.</span><span class="sxs-lookup"><span data-stu-id="ebad8-148">The form token can be a problem for AJAX requests, because an AJAX request might send JSON data, not HTML form data.</span></span> <span data-ttu-id="ebad8-149">Jedním z řešení je odeslat tokeny ve vlastní hlavičce protokolu HTTP.</span><span class="sxs-lookup"><span data-stu-id="ebad8-149">One solution is to send the tokens in a custom HTTP header.</span></span> <span data-ttu-id="ebad8-150">Následující kód používá syntaxe Razor k vygenerování tokenů a následně přidá tokeny do požadavku AJAX.</span><span class="sxs-lookup"><span data-stu-id="ebad8-150">The following code uses Razor syntax to generate the tokens, and then adds the tokens to an AJAX request.</span></span> <span data-ttu-id="ebad8-151">Tokeny se generují na serveru voláním metody **Antipadělání. Gettokens**.</span><span class="sxs-lookup"><span data-stu-id="ebad8-151">The tokens are generated at the server by calling **AntiForgery.GetTokens**.</span></span>

[!code-html[Main](preventing-cross-site-request-forgery-csrf-attacks/samples/sample4.html)]

<span data-ttu-id="ebad8-152">Při zpracování žádosti extrahujte tokeny z hlavičky žádosti.</span><span class="sxs-lookup"><span data-stu-id="ebad8-152">When you process the request, extract the tokens from the request header.</span></span> <span data-ttu-id="ebad8-153">Pak zavoláním metody **. Validate** ověřte tokeny.</span><span class="sxs-lookup"><span data-stu-id="ebad8-153">Then call the **AntiForgery.Validate** method to validate the tokens.</span></span> <span data-ttu-id="ebad8-154">Metoda **Validate** vyvolá výjimku, pokud tokeny nejsou platné.</span><span class="sxs-lookup"><span data-stu-id="ebad8-154">The **Validate** method throws an exception if the tokens are not valid.</span></span>

[!code-csharp[Main](preventing-cross-site-request-forgery-csrf-attacks/samples/sample5.cs)]
