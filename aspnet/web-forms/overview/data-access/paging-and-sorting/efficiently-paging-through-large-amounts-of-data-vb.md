---
uid: web-forms/overview/data-access/paging-and-sorting/efficiently-paging-through-large-amounts-of-data-vb
title: Efektivní stránkování prostřednictvím velkých objemů dat (VB) | Microsoft Docs
author: rick-anderson
description: Výchozí možnost stránkování ovládacího prvku pro prezentaci dat není vhodná při práci s velkými objemy dat, protože jeho základní ovládací prvek zdroje dat získává...
ms.author: riande
ms.date: 08/15/2006
ms.assetid: 3e20e64a-8808-4b49-88d6-014e2629d56f
msc.legacyurl: /web-forms/overview/data-access/paging-and-sorting/efficiently-paging-through-large-amounts-of-data-vb
msc.type: authoredcontent
ms.openlocfilehash: 0c788c4109d0d2839de969c628399290376a1ccd
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/06/2020
ms.locfileid: "78589941"
---
# <a name="efficiently-paging-through-large-amounts-of-data-vb"></a><span data-ttu-id="93859-103">Účinné stránkování velkých objemů dat (VB)</span><span class="sxs-lookup"><span data-stu-id="93859-103">Efficiently Paging Through Large Amounts of Data (VB)</span></span>

<span data-ttu-id="93859-104">[Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="93859-104">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="93859-105">[Stáhnout ukázkovou aplikaci](https://download.microsoft.com/download/9/c/1/9c1d03ee-29ba-4d58-aa1a-f201dcc822ea/ASPNET_Data_Tutorial_25_VB.exe) nebo [Stáhnout PDF](efficiently-paging-through-large-amounts-of-data-vb/_static/datatutorial25vb1.pdf)</span><span class="sxs-lookup"><span data-stu-id="93859-105">[Download Sample App](https://download.microsoft.com/download/9/c/1/9c1d03ee-29ba-4d58-aa1a-f201dcc822ea/ASPNET_Data_Tutorial_25_VB.exe) or [Download PDF](efficiently-paging-through-large-amounts-of-data-vb/_static/datatutorial25vb1.pdf)</span></span>

> <span data-ttu-id="93859-106">Výchozí možnost stránkování ovládacího prvku pro prezentaci dat je nevhodná při práci s velkými objemy dat, protože jeho podkladový ovládací prvek zdroje dat načte všechny záznamy, i když se zobrazí pouze podmnožina dat.</span><span class="sxs-lookup"><span data-stu-id="93859-106">The default paging option of a data presentation control is unsuitable when working with large amounts of data, as its underlying data source control retrieves all records, even though only a subset of data is displayed.</span></span> <span data-ttu-id="93859-107">V takovém případě je nutné změnit vlastní stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-107">In such circumstances, we must turn to custom paging.</span></span>

## <a name="introduction"></a><span data-ttu-id="93859-108">Úvod</span><span class="sxs-lookup"><span data-stu-id="93859-108">Introduction</span></span>

<span data-ttu-id="93859-109">Jak jsme probrali v předchozím kurzu, můžete stránkování implementovat jedním ze dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="93859-109">As we discussed in the preceding tutorial, paging can be implemented in one of two ways:</span></span>

- <span data-ttu-id="93859-110">**Výchozí stránkování** lze implementovat pouhým zaškrtnutím možnosti Povolit stránkování v inteligentní značce datová webová ovládání s. Při zobrazení stránky dat však prvek ObjectDataSource načte *všechny* záznamy, i když na stránce se zobrazí pouze podmnožina těchto záznamů.</span><span class="sxs-lookup"><span data-stu-id="93859-110">**Default Paging** can be implemented by simply checking the Enable Paging option in the data Web control s smart tag; however, whenever viewing a page of data, the ObjectDataSource retrieves *all* of the records, even though only a subset of them are displayed in the page</span></span>
- <span data-ttu-id="93859-111">**Vlastní stránkování** vylepšuje výkon výchozích stránkování tím, že načítá pouze záznamy z databáze, které je třeba zobrazit pro konkrétní stránku dat požadovanou uživatelem. vlastní stránkování ale zahrnuje trochu větší úsilí při implementaci než výchozí stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-111">**Custom Paging** improves the performance of default paging by retrieving only those records from the database that need to be displayed for the particular page of data requested by the user; however, custom paging involves a bit more effort to implement than default paging</span></span>

<span data-ttu-id="93859-112">Z důvodu snadné implementace stačí zaškrtnout políčko a znovu provést.</span><span class="sxs-lookup"><span data-stu-id="93859-112">Due to the ease of implementation just check a checkbox and you re done!</span></span> <span data-ttu-id="93859-113">výchozí stránkování je atraktivní možnost.</span><span class="sxs-lookup"><span data-stu-id="93859-113">default paging is an attractive option.</span></span> <span data-ttu-id="93859-114">Jeho přístup k přístupu k načtení všech záznamů se ale dá k implausiblemu výběru při stránkování přes dostatečně velké objemy dat nebo pro weby s mnoha souběžnými uživateli.</span><span class="sxs-lookup"><span data-stu-id="93859-114">Its na�ve approach in retrieving all of the records, though, makes it an implausible choice when paging through sufficiently large amounts of data or for sites with many concurrent users.</span></span> <span data-ttu-id="93859-115">V takovém případě je nutné změnit vlastní stránkování, aby poskytovala reagující systém.</span><span class="sxs-lookup"><span data-stu-id="93859-115">In such circumstances, we must turn to custom paging in order to provide a responsive system.</span></span>

<span data-ttu-id="93859-116">Výzvou k vlastnímu stránkování je schopnost zapisovat dotaz, který vrací přesně sadu záznamů potřebných pro konkrétní stránku dat.</span><span class="sxs-lookup"><span data-stu-id="93859-116">The challenge of custom paging is being able to write a query that returns the precise set of records needed for a particular page of data.</span></span> <span data-ttu-id="93859-117">Naštěstí Microsoft SQL Server 2005 poskytuje nové klíčové slovo pro výsledky řazení, které nám umožňuje napsat dotaz, který může efektivně načíst správnou podmnožinu záznamů.</span><span class="sxs-lookup"><span data-stu-id="93859-117">Fortunately, Microsoft SQL Server 2005 provides a new keyword for ranking results, which enables us to write a query that can efficiently retrieve the proper subset of records.</span></span> <span data-ttu-id="93859-118">V tomto kurzu se dozvíte, jak používat toto nové klíčové slovo SQL Server 2005 k implementaci vlastního stránkování v ovládacím prvku GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-118">In this tutorial we'll see how to use this new SQL Server 2005 keyword to implement custom paging in a GridView control.</span></span> <span data-ttu-id="93859-119">I když je uživatelské rozhraní pro vlastní stránkování stejné jako výchozí stránkování, krokování z jedné stránky na další s použitím vlastního stránkování může být několik objednávek rychleji než výchozí stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-119">While the user interface for custom paging is identical to that for default paging, stepping from one page to the next using custom paging can be several orders of magnitude faster than default paging.</span></span>

> [!NOTE]
> <span data-ttu-id="93859-120">Přesný nárůst výkonu vystavený vlastním stránkováním závisí na celkovém počtu záznamů, které jsou stránkovaná, a zatížení na databázovém serveru.</span><span class="sxs-lookup"><span data-stu-id="93859-120">The exact performance gain exhibited by custom paging depends on the total number of records being paged through and the load being placed on the database server.</span></span> <span data-ttu-id="93859-121">Na konci tohoto kurzu se podíváme na některé hrubé metriky, které prezentují výhody ve výkonu získané prostřednictvím vlastního stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-121">At the end of this tutorial we'll look at some rough metrics that showcase the benefits in performance obtained through custom paging.</span></span>

## <a name="step-1-understanding-the-custom-paging-process"></a><span data-ttu-id="93859-122">Krok 1: Princip procesu vlastního stránkování</span><span class="sxs-lookup"><span data-stu-id="93859-122">Step 1: Understanding the Custom Paging Process</span></span>

<span data-ttu-id="93859-123">Při stránkování přes data jsou přesné záznamy zobrazené na stránce závislé na požadované stránce dat a na počtu zobrazených záznamů na stránce.</span><span class="sxs-lookup"><span data-stu-id="93859-123">When paging through data, the precise records displayed in a page depend upon the page of data being requested and the number of records displayed per page.</span></span> <span data-ttu-id="93859-124">Představte si například, že jsme chtěli stránku procházet 81 produktů a zobrazit 10 produktů na stránku.</span><span class="sxs-lookup"><span data-stu-id="93859-124">For example, imagine that we wanted to page through the 81 products, displaying 10 products per page.</span></span> <span data-ttu-id="93859-125">Při prohlížení první stránky chceme, aby byly produkty od 1 do 10. Při prohlížení druhé stránky, které vám d zajímá produkty 11 až 20 atd.</span><span class="sxs-lookup"><span data-stu-id="93859-125">When viewing the first page, we d want products 1 through 10; when viewing the second page we d be interested in products 11 through 20, and so on.</span></span>

<span data-ttu-id="93859-126">Existují tři proměnné, které určují, jaké záznamy je potřeba načíst a jak se mají vykreslovat rozhraní stránkování:</span><span class="sxs-lookup"><span data-stu-id="93859-126">There are three variables that dictate what records need to be retrieved and how the paging interface should be rendered:</span></span>

- <span data-ttu-id="93859-127">**Začátek indexu řádku** index prvního řádku na stránce dat, která se má zobrazit; Tento index lze vypočítat vynásobením indexu stránky záznamy, které se mají zobrazit na stránce a přidáním jednoho.</span><span class="sxs-lookup"><span data-stu-id="93859-127">**Start Row Index** the index of the first row in the page of data to display; this index can be calculated by multiplying the page index by the records to display per page and adding one.</span></span> <span data-ttu-id="93859-128">Například při stránkování prostřednictvím záznamů 10 pro první stránku (jejíž index stránky je 0) je index řádku Start 0 \* 10 + 1 nebo 1; pro druhou stránku (jejíž index stránky je 1) je index počátečního řádku 1 \* 10 + 1 nebo 11.</span><span class="sxs-lookup"><span data-stu-id="93859-128">For example, when paging through records 10 at a time, for the first page (whose page index is 0), the Start Row Index is 0 \* 10 + 1, or 1; for the second page (whose page index is 1), the Start Row Index is 1 \* 10 + 1, or 11.</span></span>
- <span data-ttu-id="93859-129">**Maximální počet řádků** , které se mají zobrazit na jednu stránku (maximální počet záznamů)</span><span class="sxs-lookup"><span data-stu-id="93859-129">**Maximum Rows** the maximum number of records to display per page.</span></span> <span data-ttu-id="93859-130">Tato proměnná se označuje jako maximální počet řádků, od poslední stránky může být méně záznamů vráceno, než je velikost stránky.</span><span class="sxs-lookup"><span data-stu-id="93859-130">This variable is referred to as maximum rows since for the last page there may be fewer records returned than the page size.</span></span> <span data-ttu-id="93859-131">Například když stránkování přes 81 Products 10 záznamů na stránku, bude mít devátá a poslední stránka pouze jeden záznam.</span><span class="sxs-lookup"><span data-stu-id="93859-131">For example, when paging through the 81 products 10 records per page, the ninth and final page will have just one record.</span></span> <span data-ttu-id="93859-132">Žádná stránka, ale zobrazí více záznamů, než je hodnota maximální počet řádků.</span><span class="sxs-lookup"><span data-stu-id="93859-132">No page, though, will show more records than the Maximum Rows value.</span></span>
- <span data-ttu-id="93859-133">**Celkový** počet záznamů, celkový počet záznamů, které jsou stránkované.</span><span class="sxs-lookup"><span data-stu-id="93859-133">**Total Record Count** the total number of records being paged through.</span></span> <span data-ttu-id="93859-134">I když tato proměnná není potřebná k určení záznamů, které se mají načíst pro danou stránku, nadiktujte rozhraní stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-134">While this variable isn t needed to determine what records to retrieve for a given page, it does dictate the paging interface.</span></span> <span data-ttu-id="93859-135">Pokud se například stránkují 81 Products, rozhraní stránkování ví, že v uživatelském rozhraní stránkování zobrazí devět čísel stránek.</span><span class="sxs-lookup"><span data-stu-id="93859-135">For example, if there are 81 products being paged through, the paging interface knows to display nine page numbers in the paging UI.</span></span>

<span data-ttu-id="93859-136">S výchozím stránkováním je index řádku zahájení vypočítán jako součin indexu stránky a velikosti stránky plus jedna, zatímco maximální počet řádků je pouze velikost stránky.</span><span class="sxs-lookup"><span data-stu-id="93859-136">With default paging, the Start Row Index is computed as the product of the page index and the page size plus one, whereas the Maximum Rows is simply the page size.</span></span> <span data-ttu-id="93859-137">Vzhledem k tomu, že výchozí stránkování načítá všechny záznamy z databáze při vykreslování libovolné stránky dat, je známý index pro každý řádek, čímž se při přechodu na řádek indexu počátečního řádku jedná o triviální úlohu.</span><span class="sxs-lookup"><span data-stu-id="93859-137">Since default paging retrieves all of the records from the database when rendering any page of data, the index for each row is known, thereby making moving to Start Row Index row a trivial task.</span></span> <span data-ttu-id="93859-138">Celkový počet záznamů je navíc k dispozici okamžitě, protože je jednoduše počet záznamů v objektu DataTable (nebo jakýkoli objekt, který se používá k uchování výsledků databáze).</span><span class="sxs-lookup"><span data-stu-id="93859-138">Moreover, the Total Record Count is readily available, as it s simply the number of records in the DataTable (or whatever object is being used to hold the database results).</span></span>

<span data-ttu-id="93859-139">Když je zadaný index počátečního řádku a maximální počet řádků, musí implementace vlastního stránkování vracet jenom přesnou podmnožinu záznamů počínaje indexem počátečního řádku a až po dobu až po maximum řádků.</span><span class="sxs-lookup"><span data-stu-id="93859-139">Given the Start Row Index and Maximum Rows variables, a custom paging implementation must only return the precise subset of records starting at the Start Row Index and up to Maximum Rows number of records after that.</span></span> <span data-ttu-id="93859-140">Vlastní stránkování nabízí dva problémy:</span><span class="sxs-lookup"><span data-stu-id="93859-140">Custom paging provides two challenges:</span></span>

- <span data-ttu-id="93859-141">Musíme být schopni efektivně přidružit index řádku ke každému řádku v celých datech, která jsou stránkovaná, aby bylo možné začít vracet záznamy na zadaném indexu počátečního řádku.</span><span class="sxs-lookup"><span data-stu-id="93859-141">We must be able to efficiently associate a row index with each row in the entire data being paged through so that we can start returning records at the specified Start Row Index</span></span>
- <span data-ttu-id="93859-142">Musíme zadat celkový počet záznamů, které se stránkují.</span><span class="sxs-lookup"><span data-stu-id="93859-142">We need to provide the total number of records being paged through</span></span>

<span data-ttu-id="93859-143">V následujících dvou krocích prověříme skript SQL, který je potřeba k tomu, aby reagoval na tyto dva problémy.</span><span class="sxs-lookup"><span data-stu-id="93859-143">In the next two steps we'll examine the SQL script needed to respond to these two challenges.</span></span> <span data-ttu-id="93859-144">Kromě skriptu SQL budeme také muset implementovat metody do DAL a knihoven BLL.</span><span class="sxs-lookup"><span data-stu-id="93859-144">In addition to the SQL script, we'll also need to implement methods in the DAL and BLL.</span></span>

## <a name="step-2-returning-the-total-number-of-records-being-paged-through"></a><span data-ttu-id="93859-145">Krok 2: vrácení celkového počtu záznamů, které jsou stránkované prostřednictvím</span><span class="sxs-lookup"><span data-stu-id="93859-145">Step 2: Returning the Total Number of Records Being Paged Through</span></span>

<span data-ttu-id="93859-146">Než se podíváme, jak načíst přesnou podmnožinu záznamů pro stránku, která se zobrazuje, Podívejme se nejprve na to, jak vrátit celkový počet záznamů, které jsou stránkou.</span><span class="sxs-lookup"><span data-stu-id="93859-146">Before we examine how to retrieve the precise subset of records for the page being displayed, let s first look at how to return the total number of records being paged through.</span></span> <span data-ttu-id="93859-147">Tyto informace jsou potřebné k tomu, aby bylo možné správně nakonfigurovat stránkovací uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="93859-147">This information is needed in order to properly configure the paging user interface.</span></span> <span data-ttu-id="93859-148">Celkový počet záznamů vrácených konkrétním dotazem SQL lze získat pomocí [agregační funkce`COUNT`](https://msdn.microsoft.com/library/ms175997.aspx).</span><span class="sxs-lookup"><span data-stu-id="93859-148">The total number of records returned by a particular SQL query can be obtained by using the [`COUNT` aggregate function](https://msdn.microsoft.com/library/ms175997.aspx).</span></span> <span data-ttu-id="93859-149">Například k určení celkového počtu záznamů v `Products` tabulce můžeme použít následující dotaz:</span><span class="sxs-lookup"><span data-stu-id="93859-149">For example, to determine the total number of records in the `Products` table, we can use the following query:</span></span>

[!code-sql[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample1.sql)]

<span data-ttu-id="93859-150">Pojďme do našeho DAL přidat metodu, která tyto informace vrátí.</span><span class="sxs-lookup"><span data-stu-id="93859-150">Let s add a method to our DAL that returns this information.</span></span> <span data-ttu-id="93859-151">Konkrétně vytvoříme metodu DAL s názvem `TotalNumberOfProducts()`, která provede příkaz `SELECT` zobrazený výše.</span><span class="sxs-lookup"><span data-stu-id="93859-151">In particular, we'll create a DAL method called `TotalNumberOfProducts()` that executes the `SELECT` statement shown above.</span></span>

<span data-ttu-id="93859-152">Začněte tím, že ve složce `App_Code/DAL` otevřete soubor `Northwind.xsd` typované datové sady.</span><span class="sxs-lookup"><span data-stu-id="93859-152">Start by opening the `Northwind.xsd` Typed DataSet file in the `App_Code/DAL` folder.</span></span> <span data-ttu-id="93859-153">Potom klikněte pravým tlačítkem myši na `ProductsTableAdapter` v návrháři a vyberte možnost Přidat dotaz.</span><span class="sxs-lookup"><span data-stu-id="93859-153">Next, right-click on the `ProductsTableAdapter` in the Designer and choose Add Query.</span></span> <span data-ttu-id="93859-154">Jak jsme se seznámili v předchozích kurzech, to nám umožní přidat novou metodu na DAL, který při vyvolání spustí konkrétní příkaz SQL nebo uloženou proceduru.</span><span class="sxs-lookup"><span data-stu-id="93859-154">As we ve seen in previous tutorials, this will allow us to add a new method to the DAL that, when invoked, will execute a particular SQL statement or stored procedure.</span></span> <span data-ttu-id="93859-155">Podobně jako u našich TableAdapter metod v předchozích kurzech se tato možnost používá k použití ad-hoc SQL příkazu.</span><span class="sxs-lookup"><span data-stu-id="93859-155">As with our TableAdapter methods in previous tutorials, for this one opt to use an ad-hoc SQL statement.</span></span>

![Použití příkazu SQL ad hoc](efficiently-paging-through-large-amounts-of-data-vb/_static/image1.png)

<span data-ttu-id="93859-157">**Obrázek 1**: použití příkazu SQL ad hoc</span><span class="sxs-lookup"><span data-stu-id="93859-157">**Figure 1**: Use an Ad-Hoc SQL Statement</span></span>

<span data-ttu-id="93859-158">Na další obrazovce můžeme zadat typ dotazu, který se má vytvořit.</span><span class="sxs-lookup"><span data-stu-id="93859-158">On the next screen we can specify what type of query to create.</span></span> <span data-ttu-id="93859-159">Vzhledem k tomu, že tento dotaz vrátí jednu, skalární hodnotu, zobrazí se v `Products` tabulce celkový počet záznamů v tabulce vyberte `SELECT`, která vrací hodnotu jednotlivě.</span><span class="sxs-lookup"><span data-stu-id="93859-159">Since this query will return a single, scalar value the total number of records in the `Products` table choose the `SELECT` which returns a singe value option.</span></span>

![Nakonfigurujte dotaz tak, aby používal příkaz SELECT, který vrací jedinou hodnotu.](efficiently-paging-through-large-amounts-of-data-vb/_static/image2.png)

<span data-ttu-id="93859-161">**Obrázek 2**: Nakonfigurujte dotaz tak, aby používal příkaz SELECT, který vrací jedinou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="93859-161">**Figure 2**: Configure the Query to Use a SELECT Statement that Returns a Single Value</span></span>

<span data-ttu-id="93859-162">Po označení typu dotazu, který se má použít, je nutné zadat dotaz Next.</span><span class="sxs-lookup"><span data-stu-id="93859-162">After indicating the type of query to use, we must next specify the query.</span></span>

![Použít dotaz na výběr počtu (\*) z produktů](efficiently-paging-through-large-amounts-of-data-vb/_static/image3.png)

<span data-ttu-id="93859-164">**Obrázek 3**: použití dotazu vybrat počet (\*) z produktu</span><span class="sxs-lookup"><span data-stu-id="93859-164">**Figure 3**: Use the SELECT COUNT(\*) FROM Products Query</span></span>

<span data-ttu-id="93859-165">Nakonec zadejte název metody.</span><span class="sxs-lookup"><span data-stu-id="93859-165">Finally, specify the name for the method.</span></span> <span data-ttu-id="93859-166">Jak je uvedeno výše, můžeme použít `TotalNumberOfProducts`.</span><span class="sxs-lookup"><span data-stu-id="93859-166">As aforementioned, let s use `TotalNumberOfProducts`.</span></span>

![Pojmenujte metodu DAL TotalNumberOfProducts](efficiently-paging-through-large-amounts-of-data-vb/_static/image4.png)

<span data-ttu-id="93859-168">**Obrázek 4**: pojmenování metody dal TotalNumberOfProducts</span><span class="sxs-lookup"><span data-stu-id="93859-168">**Figure 4**: Name the DAL Method TotalNumberOfProducts</span></span>

<span data-ttu-id="93859-169">Po kliknutí na tlačítko Dokončit průvodce přidá do DAL metodu `TotalNumberOfProducts`.</span><span class="sxs-lookup"><span data-stu-id="93859-169">After clicking Finish, the wizard will add the `TotalNumberOfProducts` method to the DAL.</span></span> <span data-ttu-id="93859-170">Skalární návratové metody v DAL vrací typy s možnou hodnotou null pro případ, že je `NULL`výsledek dotazu SQL.</span><span class="sxs-lookup"><span data-stu-id="93859-170">The scalar returning methods in the DAL return nullable types, in case the result from the SQL query is `NULL`.</span></span> <span data-ttu-id="93859-171">Náš `COUNT` dotaz však vždycky vrátí hodnotu, která není`NULL`; Metoda DAL bez ohledu na ni vrací celé číslo s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="93859-171">Our `COUNT` query, however, will always return a non-`NULL` value; regardless, the DAL method returns a nullable integer.</span></span>

<span data-ttu-id="93859-172">Kromě metody DAL potřebujeme také metodu v knihoven BLL.</span><span class="sxs-lookup"><span data-stu-id="93859-172">In addition to the DAL method, we also need a method in the BLL.</span></span> <span data-ttu-id="93859-173">Otevřete soubor `ProductsBLL` třídy a přidejte metodu `TotalNumberOfProducts`, která jednoduše vyvolá volání metody DAL s `TotalNumberOfProducts`:</span><span class="sxs-lookup"><span data-stu-id="93859-173">Open the `ProductsBLL` class file and add a `TotalNumberOfProducts` method that simply calls down to the DAL s `TotalNumberOfProducts` method:</span></span>

[!code-vb[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample2.vb)]

<span data-ttu-id="93859-174">Metoda DAL s `TotalNumberOfProducts` vrací celé číslo s možnou hodnotou null; vytvořili jsme však metodu `ProductsBLL` třídy s `TotalNumberOfProducts` tak, aby vracela standardní celé číslo.</span><span class="sxs-lookup"><span data-stu-id="93859-174">The DAL s `TotalNumberOfProducts` method returns a nullable integer; however, we ve created the `ProductsBLL` class s `TotalNumberOfProducts` method so that it returns a standard integer.</span></span> <span data-ttu-id="93859-175">Proto potřebujeme, aby metoda `ProductsBLL` třídy s `TotalNumberOfProducts` vrátila hodnotu v rámci celého čísla s možnou hodnotou null vráceného metodou DAL s `TotalNumberOfProducts`.</span><span class="sxs-lookup"><span data-stu-id="93859-175">Therefore, we need to have the `ProductsBLL` class s `TotalNumberOfProducts` method return the value portion of the nullable integer returned by the DAL s `TotalNumberOfProducts` method.</span></span> <span data-ttu-id="93859-176">Volání `GetValueOrDefault()` vrátí hodnotu hodnoty null, pokud existuje; Pokud je celé číslo s možnou hodnotou null `null`, vrátí výchozí celočíselnou hodnotu 0.</span><span class="sxs-lookup"><span data-stu-id="93859-176">The call to `GetValueOrDefault()` returns the value of the nullable integer, if it exists; if the nullable integer is `null`, however, it returns the default integer value, 0.</span></span>

## <a name="step-3-returning-the-precise-subset-of-records"></a><span data-ttu-id="93859-177">Krok 3: vrácení přesné podmnožiny záznamů</span><span class="sxs-lookup"><span data-stu-id="93859-177">Step 3: Returning the Precise Subset of Records</span></span>

<span data-ttu-id="93859-178">Naším dalším úkolem je vytvořit metody v rámci DAL a knihoven BLL, které přijímají index počátečního řádku a maximální výše popsané proměnné řádků a vracejí příslušné záznamy.</span><span class="sxs-lookup"><span data-stu-id="93859-178">Our next task is to create methods in the DAL and BLL that accept the Start Row Index and Maximum Rows variables discussed earlier and return the appropriate records.</span></span> <span data-ttu-id="93859-179">Než to uděláte, Podívejme se nejprve na potřebný skript SQL.</span><span class="sxs-lookup"><span data-stu-id="93859-179">Before we do that, let s first look at the needed SQL script.</span></span> <span data-ttu-id="93859-180">K tomu čelíme, že musíme být schopni efektivně přiřadit index ke každému řádku v rámci celých výsledků, které jsou stránkovaná, aby bylo možné vracet pouze ty záznamy, které začínají indexem počátečního řádku (a až do maximálního počtu záznamů).</span><span class="sxs-lookup"><span data-stu-id="93859-180">The challenge facing us is that we must be able to efficiently assign an index to each row in the entire results being paged through so that we can return just those records starting at the Start Row Index (and up to the Maximum Records number of records).</span></span>

<span data-ttu-id="93859-181">Nejedná se o výzvu, pokud je v tabulce databáze již sloupec, který slouží jako index řádku.</span><span class="sxs-lookup"><span data-stu-id="93859-181">This is not a challenge if there is already a column in the database table that serves as a row index.</span></span> <span data-ttu-id="93859-182">Na první pohled si můžeme všimnout, že `ProductID` pole `Products` tabulky by bylo stačit, protože první produkt má `ProductID` 1, druhý a 2 atd.</span><span class="sxs-lookup"><span data-stu-id="93859-182">At first glance we might think that the `Products` table s `ProductID` field would suffice, as the first product has `ProductID` of 1, the second a 2, and so on.</span></span> <span data-ttu-id="93859-183">Odstraněním produktu se ale v sekvenci opustí mezeru, nullifying tento přístup.</span><span class="sxs-lookup"><span data-stu-id="93859-183">However, deleting a product leaves a gap in the sequence, nullifying this approach.</span></span>

<span data-ttu-id="93859-184">K dispozici jsou dvě obecné techniky, pomocí kterých můžete efektivně přidružit index řádku k datům, která se stránkují, a tím umožnit načtení přesné podmnožiny záznamů:</span><span class="sxs-lookup"><span data-stu-id="93859-184">There are two general techniques used to efficiently associate a row index with the data to page through, thereby enabling the precise subset of records to be retrieved:</span></span>

- <span data-ttu-id="93859-185">Použití klíčového slova **SQL Server 2005 s `ROW_NUMBER()`** jako nového pro SQL Server 2005, klíčové slovo `ROW_NUMBER()` přidružuje hodnocení každému vrácenému záznamu na základě určitého řazení.</span><span class="sxs-lookup"><span data-stu-id="93859-185">**Using SQL Server 2005 s `ROW_NUMBER()` Keyword** new to SQL Server 2005, the `ROW_NUMBER()` keyword associates a ranking with each returned record based on some ordering.</span></span> <span data-ttu-id="93859-186">Toto hodnocení lze použít jako index řádku pro každý řádek.</span><span class="sxs-lookup"><span data-stu-id="93859-186">This ranking can be used as a row index for each row.</span></span>
- <span data-ttu-id="93859-187">**Použití proměnné tabulky a `SET ROWCOUNT`** Příkaz SQL Server s [`SET ROWCOUNT`](https://msdn.microsoft.com/library/ms188774.aspx) lze použít k určení, kolik celkových záznamů má dotaz zpracovat před ukončením; [proměnné tabulky](http://www.sqlteam.com/item.asp?ItemID=9454) jsou lokální proměnné T-SQL, které mohou uchovávat tabulková data, podobají [dočasné tabulky](http://www.sqlteam.com/item.asp?ItemID=2029).</span><span class="sxs-lookup"><span data-stu-id="93859-187">**Using a Table Variable and `SET ROWCOUNT`** SQL Server s [`SET ROWCOUNT` statement](https://msdn.microsoft.com/library/ms188774.aspx) can be used to specify how many total records a query should process before terminating; [table variables](http://www.sqlteam.com/item.asp?ItemID=9454) are local T-SQL variables that can hold tabular data, akin to [temporary tables](http://www.sqlteam.com/item.asp?ItemID=2029).</span></span> <span data-ttu-id="93859-188">Tento přístup funguje stejně dobře Microsoft SQL Server 2005 i SQL Server 2000 (zatímco `ROW_NUMBER()` přístup funguje jenom s SQL Server 2005).</span><span class="sxs-lookup"><span data-stu-id="93859-188">This approach works equally well with both Microsoft SQL Server 2005 and SQL Server 2000 (whereas the `ROW_NUMBER()` approach only works with SQL Server 2005).</span></span>  
  
  <span data-ttu-id="93859-189">Nápad je vytvořit proměnnou tabulky, která má `IDENTITY` sloupec a sloupce pro primární klíče tabulky, jejichž data jsou stránkovaná.</span><span class="sxs-lookup"><span data-stu-id="93859-189">The idea here is to create a table variable that has an `IDENTITY` column and columns for the primary keys of the table whose data is being paged through.</span></span> <span data-ttu-id="93859-190">V dalším kroku se obsah tabulky, jejíž data se stránkuje, dohlíží do proměnné tabulky, takže se pro každý záznam v tabulce přidruží sekvenční index řádku (přes `IDENTITY` sloupec).</span><span class="sxs-lookup"><span data-stu-id="93859-190">Next, the contents of the table whose data is being paged through is dumped into the table variable, thereby associating a sequential row index (via the `IDENTITY` column) for each record in the table.</span></span> <span data-ttu-id="93859-191">Po naplnění proměnné tabulky může být proveden příkaz `SELECT` v proměnné tabulky, který je spojen s podkladovou tabulkou, a vyžádat si konkrétní záznamy.</span><span class="sxs-lookup"><span data-stu-id="93859-191">Once the table variable has been populated, a `SELECT` statement on the table variable, joined with the underlying table, can be executed to pull out the particular records.</span></span> <span data-ttu-id="93859-192">Příkaz `SET ROWCOUNT` slouží k inteligentnímu omezení počtu záznamů, které musí být do proměnné tabulky dumpingové.</span><span class="sxs-lookup"><span data-stu-id="93859-192">The `SET ROWCOUNT` statement is used to intelligently limit the number of records that need to be dumped into the table variable.</span></span>  
  
  <span data-ttu-id="93859-193">Tato metoda s účinností je založena na požadovaném číslu stránky, protože `SET ROWCOUNT` hodnotě je přiřazena hodnota indexu počátečního řádku a maximální počet řádků.</span><span class="sxs-lookup"><span data-stu-id="93859-193">This approach s efficiency is based on the page number being requested, as the `SET ROWCOUNT` value is assigned the value of Start Row Index plus the Maximum Rows.</span></span> <span data-ttu-id="93859-194">Při stránkování na stránky s nízkými čísly, například na prvních několika stránkách dat, tento přístup je velmi efektivní.</span><span class="sxs-lookup"><span data-stu-id="93859-194">When paging through low-numbered pages such as the first few pages of data this approach is very efficient.</span></span> <span data-ttu-id="93859-195">Ale při načítání stránky poblíž konce vykazuje výchozí výkon stránkování jako.</span><span class="sxs-lookup"><span data-stu-id="93859-195">However, it exhibits default paging-like performance when retrieving a page near the end.</span></span>

<span data-ttu-id="93859-196">Tento kurz implementuje vlastní stránkování pomocí klíčového slova `ROW_NUMBER()`.</span><span class="sxs-lookup"><span data-stu-id="93859-196">This tutorial implements custom paging using the `ROW_NUMBER()` keyword.</span></span> <span data-ttu-id="93859-197">Další informace o tom, jak používat proměnnou tabulky a `SET ROWCOUNT` techniku, najdete v článku [efektivnější způsob stránkování prostřednictvím rozsáhlých sad výsledků](http://www.4guysfromrolla.com/webtech/042606-1.shtml).</span><span class="sxs-lookup"><span data-stu-id="93859-197">For more information on using the table variable and `SET ROWCOUNT` technique, see [A More Efficient Method for Paging Through Large Result Sets](http://www.4guysfromrolla.com/webtech/042606-1.shtml).</span></span>

<span data-ttu-id="93859-198">Klíčové slovo `ROW_NUMBER()` přidružené hodnocení každému záznamu vrácenému v rámci určitého řazení pomocí následující syntaxe:</span><span class="sxs-lookup"><span data-stu-id="93859-198">The `ROW_NUMBER()` keyword associated a ranking with each record returned over a particular ordering using the following syntax:</span></span>

[!code-sql[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample3.sql)]

<span data-ttu-id="93859-199">`ROW_NUMBER()` vrátí číselnou hodnotu, která určuje pořadí pro každý záznam s ohledem na označené řazení.</span><span class="sxs-lookup"><span data-stu-id="93859-199">`ROW_NUMBER()` returns a numerical value that specifies the rank for each record with regards to the indicated ordering.</span></span> <span data-ttu-id="93859-200">Pokud například chcete zobrazit pořadí jednotlivých produktů seřazené od nejdražších po nejnižší, můžeme použít následující dotaz:</span><span class="sxs-lookup"><span data-stu-id="93859-200">For example, to see the rank for each product, ordered from the most expensive to the least, we could use the following query:</span></span>

[!code-sql[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample4.sql)]

<span data-ttu-id="93859-201">Obrázek 5 ukazuje tento dotaz s výsledky při spuštění prostřednictvím okna dotazu v aplikaci Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="93859-201">Figure 5 shows this query s results when run through the query window in Visual Studio.</span></span> <span data-ttu-id="93859-202">Všimněte si, že produkty jsou seřazené podle ceny a zároveň s cenovou známkou pro každý řádek.</span><span class="sxs-lookup"><span data-stu-id="93859-202">Note that the products are ordered by price, along with a price rank for each row.</span></span>

![Cenové pořadí je zahrnuté pro každý vrácený záznam.](efficiently-paging-through-large-amounts-of-data-vb/_static/image5.png)

<span data-ttu-id="93859-204">**Obrázek 5**: cenová klasifikace je zahrnutá u každého vráceného záznamu.</span><span class="sxs-lookup"><span data-stu-id="93859-204">**Figure 5**: The Price Rank is Included for Each Returned Record</span></span>

> [!NOTE]
> <span data-ttu-id="93859-205">`ROW_NUMBER()` je jenom jedna z mnoha nových funkcí hodnocení dostupných v SQL Server 2005.</span><span class="sxs-lookup"><span data-stu-id="93859-205">`ROW_NUMBER()` is just one of the many new ranking functions available in SQL Server 2005.</span></span> <span data-ttu-id="93859-206">Podrobnou diskuzi o `ROW_NUMBER()`, spolu s dalšími funkcemi hodnocení, najdete v článku o [vrácení seřazených výsledků pomocí Microsoft SQL Server 2005](http://www.4guysfromrolla.com/webtech/010406-1.shtml).</span><span class="sxs-lookup"><span data-stu-id="93859-206">For a more thorough discussion of `ROW_NUMBER()`, along with the other ranking functions, read [Returning Ranked Results with Microsoft SQL Server 2005](http://www.4guysfromrolla.com/webtech/010406-1.shtml).</span></span>

<span data-ttu-id="93859-207">Při řazení výsledků podle zadaného `ORDER BY` sloupce v klauzuli `OVER` (`UnitPrice`ve výše uvedeném příkladu) musí SQL Server seřadit výsledky.</span><span class="sxs-lookup"><span data-stu-id="93859-207">When ranking the results by the specified `ORDER BY` column in the `OVER` clause (`UnitPrice`, in the above example), SQL Server must sort the results.</span></span> <span data-ttu-id="93859-208">Jedná se o rychlou operaci, pokud se ve sloupcích nachází clusterovaný index, ve kterém jsou výsledky seřazeny, nebo v případě, že existuje pokrýváný index, ale může být levnější v jiném.</span><span class="sxs-lookup"><span data-stu-id="93859-208">This is a quick operation if there is a clustered index over the column(s) the results are being ordered by, or if there is a covering index, but can be more costly otherwise.</span></span> <span data-ttu-id="93859-209">Chcete-li zlepšit výkon pro dostatečně velké dotazy, zvažte přidání neclusterovaných indexů pro sloupec, podle kterého jsou výsledky seřazeny.</span><span class="sxs-lookup"><span data-stu-id="93859-209">To help improve performance for sufficiently large queries, consider adding a non-clustered index for the column by which the results are ordered by.</span></span> <span data-ttu-id="93859-210">Podrobnější informace najdete v tématu věnovaném [funkcím řazení a výkonu v SQL Server 2005](http://www.sql-server-performance.com/ak_ranking_functions.asp) .</span><span class="sxs-lookup"><span data-stu-id="93859-210">See [Ranking Functions and Performance in SQL Server 2005](http://www.sql-server-performance.com/ak_ranking_functions.asp) for a more detailed look at the performance considerations.</span></span>

<span data-ttu-id="93859-211">Informace o řazení vrácené funkcí `ROW_NUMBER()` nelze přímo použít v klauzuli `WHERE`.</span><span class="sxs-lookup"><span data-stu-id="93859-211">The ranking information returned by `ROW_NUMBER()` cannot directly be used in the `WHERE` clause.</span></span> <span data-ttu-id="93859-212">Odvozenou tabulku však lze použít k vrácení `ROW_NUMBER()` výsledek, který lze poté zobrazit v klauzuli `WHERE`.</span><span class="sxs-lookup"><span data-stu-id="93859-212">However, a derived table can be used to return the `ROW_NUMBER()` result, which can then appear in the `WHERE` clause.</span></span> <span data-ttu-id="93859-213">Následující dotaz například používá odvozenou tabulku pro vrácení sloupců NázevVýrobku a UnitPrice společně s výsledkem `ROW_NUMBER()` a poté používá klauzuli `WHERE`, která vrátí pouze ty produkty, jejichž cenová klasifikace je mezi 11 a 20:</span><span class="sxs-lookup"><span data-stu-id="93859-213">For example, the following query uses a derived table to return the ProductName and UnitPrice columns, along with the `ROW_NUMBER()` result, and then uses a `WHERE` clause to only return those products whose price rank is between 11 and 20:</span></span>

[!code-sql[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample5.sql)]

<span data-ttu-id="93859-214">Dalším rozšiřováním tohoto konceptu můžete využít tento přístup k načtení konkrétní stránky dat podle požadovaného indexu počátečního řádku a maximální hodnoty řádků:</span><span class="sxs-lookup"><span data-stu-id="93859-214">Extending this concept a bit further, we can utilize this approach to retrieve a specific page of data given the desired Start Row Index and Maximum Rows values:</span></span>

[!code-html[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample6.html)]

> [!NOTE]
> <span data-ttu-id="93859-215">Jak uvidíme později v tomto kurzu, *`StartRowIndex`* poskytnutý prvkem ObjectDataSource je indexován od nuly, zatímco hodnota `ROW_NUMBER()` vrácená SQL Server 2005 je indexována od 1.</span><span class="sxs-lookup"><span data-stu-id="93859-215">As we will see later on in this tutorial, the *`StartRowIndex`* supplied by the ObjectDataSource is indexed starting at zero, whereas the `ROW_NUMBER()` value returned by SQL Server 2005 is indexed starting at 1.</span></span> <span data-ttu-id="93859-216">Proto klauzule `WHERE` vrátí tyto záznamy, kde `PriceRank` je striktně větší než *`StartRowIndex`* a menší nebo rovna *`StartRowIndex`*  +  *`MaximumRows`* .</span><span class="sxs-lookup"><span data-stu-id="93859-216">Therefore, the `WHERE` clause returns those records where `PriceRank` is strictly greater than *`StartRowIndex`* and less than or equal to *`StartRowIndex`* + *`MaximumRows`*.</span></span>

<span data-ttu-id="93859-217">Teď, když jsme probrali, jak se dá `ROW_NUMBER()` použít k načtení konkrétní stránky dat z indexu počátečního řádku a maximálního počtu řádků, teď je potřeba tuto logiku implementovat jako metody v DAL a knihoven BLL.</span><span class="sxs-lookup"><span data-stu-id="93859-217">Now that we ve discussed how `ROW_NUMBER()` can be used to retrieve a particular page of data given the Start Row Index and Maximum Rows values, we now need to implement this logic as methods in the DAL and BLL.</span></span>

<span data-ttu-id="93859-218">Při vytváření tohoto dotazu musíte určit řazení, podle kterého budou výsledky seřazeny. v abecedním pořadí si můžeme produkty seřadit podle názvu.</span><span class="sxs-lookup"><span data-stu-id="93859-218">When creating this query we must decide the ordering by which the results will be ranked; let s sort the products by their name in alphabetical order.</span></span> <span data-ttu-id="93859-219">To znamená, že s implementací vlastního stránkování v tomto kurzu nebudeme moct vytvořit vlastní stránkovou sestavu, než dá se taky seřadit.</span><span class="sxs-lookup"><span data-stu-id="93859-219">This means that with the custom paging implementation in this tutorial we will not be able to create a custom paged report than can also be sorted.</span></span> <span data-ttu-id="93859-220">V dalším kurzu ale uvidíte, jak se tyto funkce dají zadat.</span><span class="sxs-lookup"><span data-stu-id="93859-220">In the next tutorial, though, we'll see how such functionality can be provided.</span></span>

<span data-ttu-id="93859-221">V předchozí části jsme vytvořili metodu DAL jako příkaz SQL ad hoc.</span><span class="sxs-lookup"><span data-stu-id="93859-221">In the previous section we created the DAL method as an ad-hoc SQL statement.</span></span> <span data-ttu-id="93859-222">Analyzátor T-SQL v aplikaci Visual Studio, který používá Průvodce TableAdapter, ale nefunguje, jako syntaxe `OVER` používaná funkcí `ROW_NUMBER()`.</span><span class="sxs-lookup"><span data-stu-id="93859-222">Unfortunately, the T-SQL parser in Visual Studio used by the TableAdapter wizard doesn t like the `OVER` syntax used by the `ROW_NUMBER()` function.</span></span> <span data-ttu-id="93859-223">Proto je nutné vytvořit tuto metodu DAL jako uloženou proceduru.</span><span class="sxs-lookup"><span data-stu-id="93859-223">Therefore, we must create this DAL method as a stored procedure.</span></span> <span data-ttu-id="93859-224">V nabídce zobrazení vyberte Průzkumník serveru (nebo stiskněte kombinaci kláves CTRL + ALT + S) a rozbalte uzel `NORTHWND.MDF`.</span><span class="sxs-lookup"><span data-stu-id="93859-224">Select the Server Explorer from the View menu (or hit Ctrl+Alt+S) and expand the `NORTHWND.MDF` node.</span></span> <span data-ttu-id="93859-225">Chcete-li přidat novou uloženou proceduru, klikněte pravým tlačítkem na uzel uložené procedury a vyberte možnost Přidat novou uloženou proceduru (viz obrázek 6).</span><span class="sxs-lookup"><span data-stu-id="93859-225">To add a new stored procedure, right-click on the Stored Procedures node and choose Add a New Stored Procedure (see Figure 6).</span></span>

![Přidat novou uloženou proceduru pro stránkování prostřednictvím produktů](efficiently-paging-through-large-amounts-of-data-vb/_static/image6.png)

<span data-ttu-id="93859-227">**Obrázek 6**: Přidání nové uložené procedury pro stránkování prostřednictvím produktů</span><span class="sxs-lookup"><span data-stu-id="93859-227">**Figure 6**: Add a New Stored Procedure for Paging Through the Products</span></span>

<span data-ttu-id="93859-228">Tato uložená procedura by měla přijmout dva celočíselné vstupní parametry – `@startRowIndex` a `@maximumRows` a používat `ROW_NUMBER()` funkce seřazené podle pole `ProductName` a vracet tak pouze řádky větší než zadané `@startRowIndex` a menší nebo rovny `@startRowIndex` + `@maximumRow` s.</span><span class="sxs-lookup"><span data-stu-id="93859-228">This stored procedure should accept two integer input parameters - `@startRowIndex` and `@maximumRows` and use the `ROW_NUMBER()` function ordered by the `ProductName` field, returning only those rows greater than the specified `@startRowIndex` and less than or equal to `@startRowIndex` + `@maximumRow` s.</span></span> <span data-ttu-id="93859-229">Do nové uložené procedury zadejte následující skript a potom kliknutím na ikonu Uložit přidejte uloženou proceduru do databáze.</span><span class="sxs-lookup"><span data-stu-id="93859-229">Enter the following script into the new stored procedure and then click the Save icon to add the stored procedure to the database.</span></span>

[!code-sql[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample7.sql)]

<span data-ttu-id="93859-230">Po vytvoření uložené procedury chvíli počkejte, než se otestuje. Pravým tlačítkem myši klikněte na název uložené procedury `GetProductsPaged` v Průzkumník serveru a vyberte možnost provést.</span><span class="sxs-lookup"><span data-stu-id="93859-230">After creating the stored procedure, take a moment to test it out. Right-click on the `GetProductsPaged` stored procedure name in the Server Explorer and choose the Execute option.</span></span> <span data-ttu-id="93859-231">Visual Studio vás pak vyzve k zadání vstupních parametrů `@startRowIndex` a `@maximumRow` s (viz obrázek 7).</span><span class="sxs-lookup"><span data-stu-id="93859-231">Visual Studio will then prompt you for the input parameters, `@startRowIndex` and `@maximumRow` s (see Figure 7).</span></span> <span data-ttu-id="93859-232">Zkuste použít jiné hodnoty a prověřte výsledky.</span><span class="sxs-lookup"><span data-stu-id="93859-232">Try different values and examine the results.</span></span>

![Zadejte hodnotu pro parametry @startRowIndex a @maximumRows](efficiently-paging-through-large-amounts-of-data-vb/_static/image7.png)

<span data-ttu-id="93859-234"><strong>Obrázek 7</strong>: zadejte hodnotu parametrů @startRowIndex a @maximumRows</span><span class="sxs-lookup"><span data-stu-id="93859-234"><strong>Figure 7</strong>: Enter a Value for the @startRowIndex and @maximumRows Parameters</span></span>

<span data-ttu-id="93859-235">Po zvolení hodnot vstupních parametrů zobrazí okno výstup výsledky.</span><span class="sxs-lookup"><span data-stu-id="93859-235">After choosing these input parameters values, the Output window will show the results.</span></span> <span data-ttu-id="93859-236">Obrázek 8 ukazuje výsledky při průchodu 10 pro parametry `@startRowIndex` a `@maximumRows`.</span><span class="sxs-lookup"><span data-stu-id="93859-236">Figure 8 shows the results when passing in 10 for both the `@startRowIndex` and `@maximumRows` parameters.</span></span>

<span data-ttu-id="93859-237">[Vrátí se ![záznamy, které by se zobrazily na druhé stránce dat.](efficiently-paging-through-large-amounts-of-data-vb/_static/image9.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image8.png)</span><span class="sxs-lookup"><span data-stu-id="93859-237">[![The Records That Would Appear in the Second Page of Data are Returned](efficiently-paging-through-large-amounts-of-data-vb/_static/image9.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image8.png)</span></span>

<span data-ttu-id="93859-238">**Obrázek 8**: vrátí se záznamy, které by se zobrazily na druhé stránce dat ([kliknutím zobrazíte obrázek v plné velikosti).](efficiently-paging-through-large-amounts-of-data-vb/_static/image10.png)</span><span class="sxs-lookup"><span data-stu-id="93859-238">**Figure 8**: The Records That Would Appear in the Second Page of Data are Returned ([Click to view full-size image](efficiently-paging-through-large-amounts-of-data-vb/_static/image10.png))</span></span>

<span data-ttu-id="93859-239">Po vytvoření této uložené procedury jsme připraveni vytvořit metodu `ProductsTableAdapter`.</span><span class="sxs-lookup"><span data-stu-id="93859-239">With this stored procedure created, we re ready to create the `ProductsTableAdapter` method.</span></span> <span data-ttu-id="93859-240">Otevřete `Northwind.xsd` typovou datovou sadu, klikněte pravým tlačítkem na `ProductsTableAdapter`a vyberte možnost Přidat dotaz.</span><span class="sxs-lookup"><span data-stu-id="93859-240">Open the `Northwind.xsd` Typed DataSet, right-click in the `ProductsTableAdapter`, and choose the Add Query option.</span></span> <span data-ttu-id="93859-241">Místo vytvoření dotazu pomocí příkazu SQL ad hoc ho vytvořte pomocí existující uložené procedury.</span><span class="sxs-lookup"><span data-stu-id="93859-241">Instead of creating the query using an ad-hoc SQL statement, create it using an existing stored procedure.</span></span>

![Vytvoření metody DAL pomocí existující uložené procedury](efficiently-paging-through-large-amounts-of-data-vb/_static/image11.png)

<span data-ttu-id="93859-243">**Obrázek 9**: vytvoření metody dal pomocí existující uložené procedury</span><span class="sxs-lookup"><span data-stu-id="93859-243">**Figure 9**: Create the DAL Method Using an Existing Stored Procedure</span></span>

<span data-ttu-id="93859-244">V dalším kroku se zobrazí výzva, abyste vybrali uloženou proceduru, která se má vyvolat.</span><span class="sxs-lookup"><span data-stu-id="93859-244">Next, we are prompted to select the stored procedure to invoke.</span></span> <span data-ttu-id="93859-245">Z rozevíracího seznamu vyberte `GetProductsPaged` uloženou proceduru.</span><span class="sxs-lookup"><span data-stu-id="93859-245">Pick the `GetProductsPaged` stored procedure from the drop-down list.</span></span>

![Z rozevíracího seznamu vyberte uloženou proceduru GetProductsPaged.](efficiently-paging-through-large-amounts-of-data-vb/_static/image12.png)

<span data-ttu-id="93859-247">**Obrázek 10**: z rozevíracího seznamu vyberte uloženou proceduru GetProductsPaged.</span><span class="sxs-lookup"><span data-stu-id="93859-247">**Figure 10**: Choose the GetProductsPaged Stored Procedure from the Drop-Down List</span></span>

<span data-ttu-id="93859-248">Na další obrazovce se zobrazí dotaz, jaký typ dat je vrácen uloženou procedurou: tabulková data, jedna hodnota nebo žádná hodnota.</span><span class="sxs-lookup"><span data-stu-id="93859-248">The next screen then asks you what kind of data is returned by the stored procedure: tabular data, a single value, or no value.</span></span> <span data-ttu-id="93859-249">Vzhledem k tomu, že uložená procedura `GetProductsPaged` může vracet více záznamů, znamená to, že vrátí tabulková data.</span><span class="sxs-lookup"><span data-stu-id="93859-249">Since the `GetProductsPaged` stored procedure can return multiple records, indicate that it returns tabular data.</span></span>

![Označení, že uložená procedura vrátí tabulková data](efficiently-paging-through-large-amounts-of-data-vb/_static/image13.png)

<span data-ttu-id="93859-251">**Obrázek 11**: označení, že uložená procedura vrátí tabulková data</span><span class="sxs-lookup"><span data-stu-id="93859-251">**Figure 11**: Indicate that the Stored Procedure Returns Tabular Data</span></span>

<span data-ttu-id="93859-252">Nakonec uveďte názvy metod, které chcete vytvořit.</span><span class="sxs-lookup"><span data-stu-id="93859-252">Finally, indicate the names of the methods you want to have created.</span></span> <span data-ttu-id="93859-253">Stejně jako u našich předchozích kurzů, pokračujte a vytvořte metody pomocí naplnit objekt DataTable a vraťte DataTable.</span><span class="sxs-lookup"><span data-stu-id="93859-253">As with our previous tutorials, go ahead and create methods using both the Fill a DataTable and Return a DataTable.</span></span> <span data-ttu-id="93859-254">Název první metody `FillPaged` a druhý `GetProductsPaged`.</span><span class="sxs-lookup"><span data-stu-id="93859-254">Name the first method `FillPaged` and the second `GetProductsPaged`.</span></span>

![Pojmenování metod FillPaged a GetProductsPaged](efficiently-paging-through-large-amounts-of-data-vb/_static/image14.png)

<span data-ttu-id="93859-256">**Obrázek 12**: pojmenování metod FillPaged a GetProductsPaged</span><span class="sxs-lookup"><span data-stu-id="93859-256">**Figure 12**: Name the Methods FillPaged and GetProductsPaged</span></span>

<span data-ttu-id="93859-257">Kromě vytvoření metody DAL pro vrácení konkrétní stránky produktů je také potřeba poskytnout tyto funkce v knihoven BLL.</span><span class="sxs-lookup"><span data-stu-id="93859-257">In addition to created a DAL method to return a particular page of products, we also need to provide such functionality in the BLL.</span></span> <span data-ttu-id="93859-258">Podobně jako u metody DAL musí metoda knihoven BLL s GetProductsPaged přijmout dva celočíselné vstupy pro zadání indexu počátečního řádku a maximálního počtu řádků a musí vracet pouze ty záznamy, které spadají do zadaného rozsahu.</span><span class="sxs-lookup"><span data-stu-id="93859-258">Like the DAL method, the BLL s GetProductsPaged method must accept two integer inputs for specifying the Start Row Index and Maximum Rows, and must return just those records that fall within the specified range.</span></span> <span data-ttu-id="93859-259">Vytvořte takovou metodu knihoven BLL ve třídě ProductsBLL, která se pouze zavolá do metody GetProductsPaged DAL s, například:</span><span class="sxs-lookup"><span data-stu-id="93859-259">Create such a BLL method in the ProductsBLL class that merely calls down into the DAL s GetProductsPaged method, like so:</span></span>

[!code-vb[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample8.vb)]

<span data-ttu-id="93859-260">Pro vstupní parametry metody knihoven BLL můžete použít libovolný název, ale v krátké době se vám zobrazí, jak se vám podaří použít `startRowIndex` a `maximumRows` při konfiguraci prvku ObjectDataSource pro použití této metody uložit nás z další funkční funkce.</span><span class="sxs-lookup"><span data-stu-id="93859-260">You can use any name for the BLL method s input parameters, but, as we will see shortly, choosing to use `startRowIndex` and `maximumRows` saves us from an extra bit of work when configuring an ObjectDataSource to use this method.</span></span>

## <a name="step-4-configuring-the-objectdatasource-to-use-custom-paging"></a><span data-ttu-id="93859-261">Krok 4: Konfigurace prvku ObjectDataSource pro použití vlastního stránkování</span><span class="sxs-lookup"><span data-stu-id="93859-261">Step 4: Configuring the ObjectDataSource to Use Custom Paging</span></span>

<span data-ttu-id="93859-262">Díky metodám knihoven BLL a DAL pro přístup k určité podmnožině záznamů je vše připraveno k vytvoření ovládacího prvku GridView, který provede stránky prostřednictvím svých podkladových záznamů pomocí vlastního stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-262">With the BLL and DAL methods for accessing a particular subset of records complete, we re ready to create a GridView control that pages through its underlying records using custom paging.</span></span> <span data-ttu-id="93859-263">Začněte tím, že otevřete stránku `EfficientPaging.aspx` ve složce `PagingAndSorting`, přidáte na stránku prvek GridView a nakonfigurujete ji tak, aby používala nový ovládací prvek ObjectDataSource.</span><span class="sxs-lookup"><span data-stu-id="93859-263">Start by opening the `EfficientPaging.aspx` page in the `PagingAndSorting` folder, add a GridView to the page, and configure it to use a new ObjectDataSource control.</span></span> <span data-ttu-id="93859-264">V předchozích kurzech máme často nakonfigurovaný prvek ObjectDataSource, aby používal metodu `ProductsBLL` třídy s `GetProducts`.</span><span class="sxs-lookup"><span data-stu-id="93859-264">In our past tutorials, we often had the ObjectDataSource configured to use the `ProductsBLL` class s `GetProducts` method.</span></span> <span data-ttu-id="93859-265">Tentokrát ale chceme místo toho použít metodu `GetProductsPaged`, protože `GetProducts` metoda vrátí *všechny* produkty v databázi, zatímco `GetProductsPaged` vrací jenom určitou podmnožinu záznamů.</span><span class="sxs-lookup"><span data-stu-id="93859-265">This time, however, we want to use the `GetProductsPaged` method instead, since the `GetProducts` method returns *all* of the products in the database whereas `GetProductsPaged` returns just a particular subset of records.</span></span>

![Konfigurace prvku ObjectDataSource pro použití metody ProductsBLL třídy s GetProductsPaged](efficiently-paging-through-large-amounts-of-data-vb/_static/image15.png)

<span data-ttu-id="93859-267">**Obrázek 13**: Konfigurace prvku ObjectDataSource pro použití metody ProductsBLL třídy s GetProductsPaged</span><span class="sxs-lookup"><span data-stu-id="93859-267">**Figure 13**: Configure the ObjectDataSource to Use the ProductsBLL Class s GetProductsPaged Method</span></span>

<span data-ttu-id="93859-268">Vzhledem k tomu, že jsme znovu vytvořili prvek GridView, který je jen pro čtení, chvíli počkejte, než nastavíte rozevírací seznam metoda na kartách vložení, aktualizace a odstranění na (žádné).</span><span class="sxs-lookup"><span data-stu-id="93859-268">Since we re creating a read-only GridView, take a moment to set the method drop-down list in the INSERT, UPDATE, and DELETE tabs to (None).</span></span>

<span data-ttu-id="93859-269">Dále Průvodce prvek ObjectDataSource vyzve pro zdroje `GetProductsPaged` metody s `startRowIndex` a `maximumRows` hodnoty vstupních parametrů.</span><span class="sxs-lookup"><span data-stu-id="93859-269">Next, the ObjectDataSource wizard prompts us for the sources of the `GetProductsPaged` method s `startRowIndex` and `maximumRows` input parameters values.</span></span> <span data-ttu-id="93859-270">Tyto vstupní parametry budou ve skutečnosti automaticky nastaveny pomocí prvku GridView, takže zdrojovou sadu jednoduše ponechte nastavenou na None a klikněte na tlačítko Dokončit.</span><span class="sxs-lookup"><span data-stu-id="93859-270">These input parameters will actually be set by the GridView automatically, so simply leave the source set to None and click Finish.</span></span>

![Ponechat zdroje vstupních parametrů jako žádné](efficiently-paging-through-large-amounts-of-data-vb/_static/image16.png)

<span data-ttu-id="93859-272">**Obrázek 14**: ponechte zdroje vstupních parametrů jako žádné.</span><span class="sxs-lookup"><span data-stu-id="93859-272">**Figure 14**: Leave the Input Parameter Sources as None</span></span>

<span data-ttu-id="93859-273">Po dokončení průvodce ObjectDataSource bude prvek GridView obsahovat vlastnost BoundField nebo třídě CheckBoxField podporována pro každé pole dat produktu.</span><span class="sxs-lookup"><span data-stu-id="93859-273">After completing the ObjectDataSource wizard, the GridView will contain a BoundField or CheckBoxField for each of the product data fields.</span></span> <span data-ttu-id="93859-274">Nebojte se přizpůsobit vzhled prvku GridView, jak je vidět.</span><span class="sxs-lookup"><span data-stu-id="93859-274">Feel free to tailor the GridView s appearance as you see fit.</span></span> <span data-ttu-id="93859-275">Zobrazuje se jenom `ProductName`, `CategoryName`, `SupplierName`, `QuantityPerUnit`a `UnitPrice` BoundFields.</span><span class="sxs-lookup"><span data-stu-id="93859-275">I ve opted to display only the `ProductName`, `CategoryName`, `SupplierName`, `QuantityPerUnit`, and `UnitPrice` BoundFields.</span></span> <span data-ttu-id="93859-276">Také nakonfigurujete prvek GridView tak, aby podporoval stránkování, zaškrtnutím políčka Povolit stránkování v jeho inteligentní značce.</span><span class="sxs-lookup"><span data-stu-id="93859-276">Also, configure the GridView to support paging by checking the Enable Paging checkbox in its smart tag.</span></span> <span data-ttu-id="93859-277">Po těchto změnách by deklarativní označení GridView a ObjectDataSource mělo vypadat podobně jako následující:</span><span class="sxs-lookup"><span data-stu-id="93859-277">After these changes, the GridView and ObjectDataSource declarative markup should look similar to the following:</span></span>

[!code-aspx[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample9.aspx)]

<span data-ttu-id="93859-278">Pokud ale navštívíte stránku prostřednictvím prohlížeče, ovládací prvek GridView však není tam, kde má být nalezen.</span><span class="sxs-lookup"><span data-stu-id="93859-278">If you visit the page through a browser, however, the GridView is no where to be found.</span></span>

![Prvek GridView není zobrazen](efficiently-paging-through-large-amounts-of-data-vb/_static/image17.png)

<span data-ttu-id="93859-280">**Obrázek 15**: prvek GridView není zobrazen.</span><span class="sxs-lookup"><span data-stu-id="93859-280">**Figure 15**: The GridView is Not Displayed</span></span>

<span data-ttu-id="93859-281">Prvek GridView chybí, protože prvek ObjectDataSource aktuálně používá hodnotu 0 jako hodnoty pro oba `GetProductsPaged` `startRowIndex` a vstupní parametry `maximumRows`.</span><span class="sxs-lookup"><span data-stu-id="93859-281">The GridView is missing because the ObjectDataSource is currently using 0 as the values for both of the `GetProductsPaged` `startRowIndex` and `maximumRows` input parameters.</span></span> <span data-ttu-id="93859-282">Proto výsledný dotaz SQL nevrací žádné záznamy, takže prvek GridView není zobrazen.</span><span class="sxs-lookup"><span data-stu-id="93859-282">Hence, the resulting SQL query is returning no records and therefore the GridView is not displayed.</span></span>

<span data-ttu-id="93859-283">Chcete-li tento problém napravit, musíme prvek ObjectDataSource nakonfigurovat tak, aby používal vlastní stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-283">To remedy this, we need to configure the ObjectDataSource to use custom paging.</span></span> <span data-ttu-id="93859-284">To lze provést v následujících krocích:</span><span class="sxs-lookup"><span data-stu-id="93859-284">This can be accomplished in the following steps:</span></span>

1. <span data-ttu-id="93859-285">**Nastavte vlastnost `EnablePaging` ObjectDataSource na `true`** označuje prvek ObjectDataSource, který musí předat do `SelectMethod` dva další parametry: jeden pro zadání indexu počátečního řádku ([`StartRowIndexParameterName`](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.startrowindexparametername.aspx)) a druhý pro určení maximálního počtu řádků ([`MaximumRowsParameterName`](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.maximumrowsparametername.aspx)).</span><span class="sxs-lookup"><span data-stu-id="93859-285">**Set the ObjectDataSource s `EnablePaging` property to `true`** this indicates to the ObjectDataSource that it must pass to the `SelectMethod` two additional parameters: one to specify the Start Row Index ([`StartRowIndexParameterName`](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.startrowindexparametername.aspx)), and one to specify the Maximum Rows ([`MaximumRowsParameterName`](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.maximumrowsparametername.aspx)).</span></span>
2. <span data-ttu-id="93859-286">**Nastavte vlastnosti ObjectDataSource s `StartRowIndexParameterName` a `MaximumRowsParameterName` odpovídajícím způsobem** `StartRowIndexParameterName` a `MaximumRowsParameterName` vlastnosti označují názvy vstupních parametrů předaných do `SelectMethod` pro vlastní stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-286">**Set the ObjectDataSource s `StartRowIndexParameterName` and `MaximumRowsParameterName` Properties Accordingly** the `StartRowIndexParameterName` and `MaximumRowsParameterName` properties indicate the names of the input parameters passed into the `SelectMethod` for custom paging purposes.</span></span> <span data-ttu-id="93859-287">Ve výchozím nastavení jsou tyto názvy parametrů `startIndexRow` a `maximumRows`, což je důvod, proč při vytváření `GetProductsPaged` metody v knihoven BLL jsem použili tyto hodnoty pro vstupní parametry.</span><span class="sxs-lookup"><span data-stu-id="93859-287">By default, these parameter names are `startIndexRow` and `maximumRows`, which is why, when creating the `GetProductsPaged` method in the BLL, I used these values for the input parameters.</span></span> <span data-ttu-id="93859-288">Pokud se rozhodnete použít jiné názvy parametrů pro metodu knihoven BLL `GetProductsPaged`, jako je například `startIndex` a `maxRows`, například byste museli nastavovat `StartRowIndexParameterName` a `MaximumRowsParameterName` vlastnosti ObjectDataSource (například startIndex pro `StartRowIndexParameterName` a maxRows pro `MaximumRowsParameterName`).</span><span class="sxs-lookup"><span data-stu-id="93859-288">If you chose to use different parameter names for the BLL s `GetProductsPaged` method such as `startIndex` and `maxRows`, for example you would need to set the ObjectDataSource s `StartRowIndexParameterName` and `MaximumRowsParameterName` properties accordingly (such as startIndex for `StartRowIndexParameterName` and maxRows for `MaximumRowsParameterName`).</span></span>
3. <span data-ttu-id="93859-289">**Nastavte vlastnost ObjectDataSource s [`SelectCountMethod`](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.selectcountmethod(VS.80).aspx) na název metody, která vrátí celkový počet záznamů, které jsou stránkované prostřednictvím (`TotalNumberOfProducts`)** odvolání, že metoda `ProductsBLL` třídy s `TotalNumberOfProducts` vrátí celkový počet záznamů, které jsou stránkou, pomocí metody dal, která provádí dotaz `SELECT COUNT(*) FROM Products`.</span><span class="sxs-lookup"><span data-stu-id="93859-289">**Set the ObjectDataSource s [`SelectCountMethod` Property](https://msdn.microsoft.com/library/system.web.ui.webcontrols.objectdatasource.selectcountmethod(VS.80).aspx) to the Name of the Method that Returns the Total Number of Records Being Paged Through (`TotalNumberOfProducts`)** recall that the `ProductsBLL` class s `TotalNumberOfProducts` method returns the total number of records being paged through using a DAL method that executes a `SELECT COUNT(*) FROM Products` query.</span></span> <span data-ttu-id="93859-290">Tyto informace vyžaduje prvek ObjectDataSource, aby bylo možné správně vykreslit rozhraní stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-290">This information is needed by the ObjectDataSource in order to correctly render the paging interface.</span></span>
4. <span data-ttu-id="93859-291">**Odebere `startRowIndex` a `maximumRows` prvky `<asp:Parameter>` z deklarativního označení ObjectDataSource s** při konfiguraci prvku ObjectDataSource prostřednictvím průvodce. Visual Studio automaticky přidalo dva prvky `<asp:Parameter>` pro vstupní parametry `GetProductsPaged` metody s.</span><span class="sxs-lookup"><span data-stu-id="93859-291">**Remove the `startRowIndex` and `maximumRows` `<asp:Parameter>` Elements from the ObjectDataSource s Declarative Markup** when configuring the ObjectDataSource through the wizard, Visual Studio automatically added two `<asp:Parameter>` elements for the `GetProductsPaged` method s input parameters.</span></span> <span data-ttu-id="93859-292">Nastavením `EnablePaging` na `true`budou tyto parametry předány automaticky; Pokud se zobrazí také v deklarativní syntaxi, prvek ObjectDataSource se pokusí předat *čtyři* parametry metodě `GetProductsPaged` a dva parametry metody `TotalNumberOfProducts`.</span><span class="sxs-lookup"><span data-stu-id="93859-292">By setting `EnablePaging` to `true`, these parameters will be passed automatically; if they also appear in the declarative syntax, the ObjectDataSource will attempt to pass *four* parameters to the `GetProductsPaged` method and two parameters to the `TotalNumberOfProducts` method.</span></span> <span data-ttu-id="93859-293">Pokud zapomenete odebrat tyto prvky `<asp:Parameter>`, zobrazí se při návštěvě stránky v prohlížeči chybová zpráva, například: *ObjectDataSource ' ObjectDataSource1 ' nemůže najít neobecnou metodu ' TotalNumberOfProducts ', která má parametry: StartRowIndex, MaximumRows*.</span><span class="sxs-lookup"><span data-stu-id="93859-293">If you forget to remove these `<asp:Parameter>` elements, when visiting the page through a browser you'll get an error message like: *ObjectDataSource 'ObjectDataSource1' could not find a non-generic method 'TotalNumberOfProducts' that has parameters: startRowIndex, maximumRows*.</span></span>

<span data-ttu-id="93859-294">Po provedení těchto změn by deklarativní syntaxe ObjectDataSource s měla vypadat takto:</span><span class="sxs-lookup"><span data-stu-id="93859-294">After making these changes, the ObjectDataSource s declarative syntax should look like the following:</span></span>

[!code-aspx[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample10.aspx)]

<span data-ttu-id="93859-295">Všimněte si, že byly nastaveny vlastnosti `EnablePaging` a `SelectCountMethod` a byly odebrány prvky `<asp:Parameter>`.</span><span class="sxs-lookup"><span data-stu-id="93859-295">Note that the `EnablePaging` and `SelectCountMethod` properties have been set and the `<asp:Parameter>` elements have been removed.</span></span> <span data-ttu-id="93859-296">Obrázek 16 ukazuje snímek obrazovky okno Vlastnosti poté, co byly provedeny tyto změny.</span><span class="sxs-lookup"><span data-stu-id="93859-296">Figure 16 shows a screen shot of the Properties window after these changes have been made.</span></span>

![Chcete-li použít vlastní stránkování, nakonfigurujte ovládací prvek ObjectDataSource](efficiently-paging-through-large-amounts-of-data-vb/_static/image18.png)

<span data-ttu-id="93859-298">**Obrázek 16**: použití vlastního stránkování, konfigurace ovládacího prvku ObjectDataSource</span><span class="sxs-lookup"><span data-stu-id="93859-298">**Figure 16**: To Use Custom Paging, Configure the ObjectDataSource Control</span></span>

<span data-ttu-id="93859-299">Po provedení těchto změn navštivte tuto stránku v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="93859-299">After making these changes, visit this page through a browser.</span></span> <span data-ttu-id="93859-300">Měli byste vidět seznam 10 produktů seřazený podle abecedy.</span><span class="sxs-lookup"><span data-stu-id="93859-300">You should see 10 products listed, ordered alphabetically.</span></span> <span data-ttu-id="93859-301">Chvíli počkejte, než budou data postupně procházet jednou stránkou.</span><span class="sxs-lookup"><span data-stu-id="93859-301">Take a moment to step through the data one page at a time.</span></span> <span data-ttu-id="93859-302">I když z pohledu koncového uživatele mezi výchozím stránkováním a vlastním stránkováním nedochází k žádnému vizuálnímu rozdílu, vlastní stránkování je efektivněji stránkou prostřednictvím velkých objemů dat, protože načítá pouze ty záznamy, které se musí zobrazit pro danou stránku.</span><span class="sxs-lookup"><span data-stu-id="93859-302">While there is no visual difference from the end user s perspective between default paging and custom paging, custom paging more efficiently pages through large amounts of data as it only retrieves those records that need to be displayed for a given page.</span></span>

<span data-ttu-id="93859-303">[![data řazená podle názvu produktu jsou stránkou s použitím vlastního stránkování](efficiently-paging-through-large-amounts-of-data-vb/_static/image20.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image19.png)</span><span class="sxs-lookup"><span data-stu-id="93859-303">[![The Data, Ordered by the Product s Name, is Paged Using Custom Paging](efficiently-paging-through-large-amounts-of-data-vb/_static/image20.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image19.png)</span></span>

<span data-ttu-id="93859-304">**Obrázek 17**: data seřazená podle názvu produktu jsou stránkovaná pomocí vlastního stránkování ([kliknutím zobrazíte obrázek v plné velikosti).](efficiently-paging-through-large-amounts-of-data-vb/_static/image21.png)</span><span class="sxs-lookup"><span data-stu-id="93859-304">**Figure 17**: The Data, Ordered by the Product s Name, is Paged Using Custom Paging ([Click to view full-size image](efficiently-paging-through-large-amounts-of-data-vb/_static/image21.png))</span></span>

> [!NOTE]
> <span data-ttu-id="93859-305">U vlastního stránkování je hodnota počtu stránek vrácená `SelectCountMethod` ObjectDataSource s uložena ve stavu zobrazení GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-305">With custom paging, the page count value returned by the ObjectDataSource�s `SelectCountMethod` is stored in the GridView�s view state.</span></span> <span data-ttu-id="93859-306">Jiné prvky GridView proměnné `PageIndex`, `EditIndex`, `SelectedIndex`, `DataKeys` Collection a tak dále jsou uloženy ve *stavu ovládacího prvku*, který je trvalý bez ohledu na hodnotu vlastnosti `EnableViewState` prvku GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-306">Other GridView variables the `PageIndex`, `EditIndex`, `SelectedIndex`, `DataKeys` collection, and so on are stored in *control state*, which is persisted regardless of the value of the GridView�s `EnableViewState` property.</span></span> <span data-ttu-id="93859-307">Vzhledem k tomu, že hodnota `PageCount` je trvala napříč zpětným voláním pomocí stavu zobrazení, při použití rozhraní stránkování, které obsahuje odkaz pro přechod na poslední stránku, je nezbytné, aby byl stav zobrazení GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-307">Since the `PageCount` value is persisted across postbacks using view state, when using a paging interface that includes a link to take you to the last page, it is imperative that the GridView�s view state be enabled.</span></span> <span data-ttu-id="93859-308">(Pokud rozhraní pro stránkování neobsahuje přímý odkaz na poslední stránku, můžete stav zobrazení zakázat.)</span><span class="sxs-lookup"><span data-stu-id="93859-308">(If your paging interface does not include a direct link to the last page, then you may disable view state.)</span></span>

<span data-ttu-id="93859-309">Kliknutím na odkaz na poslední stránku dojde k postbacku a ovládací prvek GridViewa vydá pokyn, aby aktualizoval jeho vlastnost `PageIndex`.</span><span class="sxs-lookup"><span data-stu-id="93859-309">Clicking the last page link causes a postback and instructs the GridView to update its `PageIndex` property.</span></span> <span data-ttu-id="93859-310">Pokud se klikne na odkaz na poslední stránku, ovládací prvek GridView přiřadí jeho vlastnost `PageIndex` k hodnotě, která je nižší než jeho vlastnost `PageCount`.</span><span class="sxs-lookup"><span data-stu-id="93859-310">If the last page link is clicked, the GridView assigns its `PageIndex` property to a value one less than its `PageCount` property.</span></span> <span data-ttu-id="93859-311">Je-li stav zobrazení zakázán, hodnota `PageCount` je ztracena mezi zpětnými odesláními a `PageIndex` je místo toho přiřazena maximální celočíselná hodnota.</span><span class="sxs-lookup"><span data-stu-id="93859-311">With view state disabled, the `PageCount` value is lost across postbacks and the `PageIndex` is assigned the maximum integer value instead.</span></span> <span data-ttu-id="93859-312">V dalším kroku se prvek GridView pokusí určit počáteční index řádku vynásobením vlastností `PageSize` a `PageCount`.</span><span class="sxs-lookup"><span data-stu-id="93859-312">Next, the GridView attempts to determine the starting row index by multiplying the `PageSize` and `PageCount` properties.</span></span> <span data-ttu-id="93859-313">Výsledkem je `OverflowException`, protože součin překračuje maximální povolenou velikost celého čísla.</span><span class="sxs-lookup"><span data-stu-id="93859-313">This results in an `OverflowException` since the product exceeds the maximum allowed integer size.</span></span>

## <a name="implement-custom-paging-and-sorting"></a><span data-ttu-id="93859-314">Implementace vlastního stránkování a řazení</span><span class="sxs-lookup"><span data-stu-id="93859-314">Implement Custom Paging and Sorting</span></span>

<span data-ttu-id="93859-315">Naše aktuální implementace vlastního stránkování vyžaduje, aby pořadí, podle kterého jsou data stránkovaná, byla určena staticky při vytváření `GetProductsPaged` uložené procedury.</span><span class="sxs-lookup"><span data-stu-id="93859-315">Our current custom paging implementation requires that the order by which the data is paged through be specified statically when creating the `GetProductsPaged` stored procedure.</span></span> <span data-ttu-id="93859-316">Nicméně jste si mohli všimnout, že inteligentní značka GridView s obsahuje kromě možnosti Povolit stránkování také zaškrtávací políčko Povolit řazení.</span><span class="sxs-lookup"><span data-stu-id="93859-316">However, you may have noted that the GridView s smart tag contains an Enable Sorting checkbox in addition to the Enable Paging option.</span></span> <span data-ttu-id="93859-317">Bohužel přidání podpory řazení do prvku GridView s naší aktuální implementací vlastního stránkování bude setříděno pouze záznamy na aktuálně zobrazené stránce dat.</span><span class="sxs-lookup"><span data-stu-id="93859-317">Unfortunately, adding sorting support to the GridView with our current custom paging implementation will only sort the records on the currently viewed page of data.</span></span> <span data-ttu-id="93859-318">Pokud například nakonfigurujete prvek GridView tak, aby podporoval také stránkování a pak při zobrazení první stránky dat se v sestupném pořadí seřadí podle názvu produktu sestupně, obrátí se pořadí produktů na stránce 1.</span><span class="sxs-lookup"><span data-stu-id="93859-318">For example, if you configure the GridView to also support paging and then, when viewing the first page of data, sort by product name in descending order, it will reverse the order of the products on page 1.</span></span> <span data-ttu-id="93859-319">Jak ukazuje obrázek 18, například Carnarvon Tigers jako první produkt při řazení v obráceném abecedním pořadí, které ignoruje 71 dalších produktů, které jsou po Carnarvon Tigers, abecedně; pouze záznamy na první stránce jsou posuzovány v řazení.</span><span class="sxs-lookup"><span data-stu-id="93859-319">As Figure 18 shows, such shows Carnarvon Tigers as the first product when sorting in reverse alphabetical order, which ignores the 71 other products that come after Carnarvon Tigers, alphabetically; only those records on the first page are considered in the sorting.</span></span>

<span data-ttu-id="93859-320">[přiřadí se ![jenom data zobrazená na aktuální stránce.](efficiently-paging-through-large-amounts-of-data-vb/_static/image23.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image22.png)</span><span class="sxs-lookup"><span data-stu-id="93859-320">[![Only the Data Shown on the Current Page is Sorted](efficiently-paging-through-large-amounts-of-data-vb/_static/image23.png)](efficiently-paging-through-large-amounts-of-data-vb/_static/image22.png)</span></span>

<span data-ttu-id="93859-321">**Obrázek 18**: seřazení pouze dat zobrazených na aktuální stránce ([kliknutím zobrazíte obrázek v plné velikosti](efficiently-paging-through-large-amounts-of-data-vb/_static/image24.png))</span><span class="sxs-lookup"><span data-stu-id="93859-321">**Figure 18**: Only the Data Shown on the Current Page is Sorted ([Click to view full-size image](efficiently-paging-through-large-amounts-of-data-vb/_static/image24.png))</span></span>

<span data-ttu-id="93859-322">Řazení se vztahuje pouze na aktuální stránku dat, protože k řazení dochází po načtení dat z metody knihoven BLL s `GetProductsPaged` a tato metoda vrátí pouze ty záznamy pro konkrétní stránku.</span><span class="sxs-lookup"><span data-stu-id="93859-322">The sorting only applies to the current page of data because the sorting is occurring after the data has been retrieved from the BLL s `GetProductsPaged` method, and this method only returns those records for the specific page.</span></span> <span data-ttu-id="93859-323">Aby bylo řazení správně implementováno, je nutné předat výraz řazení metodě `GetProductsPaged`, aby bylo možné data seřadit odpovídajícím způsobem před vrácením konkrétní stránky dat.</span><span class="sxs-lookup"><span data-stu-id="93859-323">To implement sorting correctly, we need to pass the sort expression to the `GetProductsPaged` method so that the data can be ranked appropriately before returning the specific page of data.</span></span> <span data-ttu-id="93859-324">V našem dalším kurzu zjistíme, jak to provést.</span><span class="sxs-lookup"><span data-stu-id="93859-324">We'll see how to accomplish this in our next tutorial.</span></span>

## <a name="implementing-custom-paging-and-deleting"></a><span data-ttu-id="93859-325">Implementace vlastního stránkování a odstranění</span><span class="sxs-lookup"><span data-stu-id="93859-325">Implementing Custom Paging and Deleting</span></span>

<span data-ttu-id="93859-326">Pokud povolíte odstraňování funkcí v prvku GridView, jehož data jsou stránkovaná pomocí vlastních technik stránkování, zjistíte, že při odstranění posledního záznamu z poslední stránky se v prvku GridView zmizí místo aby se `PageIndex`prvků GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-326">If you enabling deleting functionality in a GridView whose data is paged using custom paging techniques you will find that when deleting the last record from the last page, the GridView disappears rather than appropriately decrementing the GridView s `PageIndex`.</span></span> <span data-ttu-id="93859-327">Pro reprodukování této chyby povolte odstranění pro kurz, který jsme právě vytvořili.</span><span class="sxs-lookup"><span data-stu-id="93859-327">To reproduce this bug, enable deleting for the tutorial just we just created.</span></span> <span data-ttu-id="93859-328">Přejděte na poslední stránku (stránku 9), kde byste měli vidět jeden produkt, protože máme na sebe stránkování přes 81 produktů, 10 produktů najednou.</span><span class="sxs-lookup"><span data-stu-id="93859-328">Go to the last page (page 9), where you should see a single product since we are paging through 81 products, 10 products at a time.</span></span> <span data-ttu-id="93859-329">Odstranit tento produkt</span><span class="sxs-lookup"><span data-stu-id="93859-329">Delete this product.</span></span>

<span data-ttu-id="93859-330">Po odstranění posledního produktu by prvek GridView *měl* automaticky přejít na osmá stránku a tato funkce se zobrazí s výchozím stránkováním.</span><span class="sxs-lookup"><span data-stu-id="93859-330">Upon deleting the last product, the GridView *should* automatically go to the eighth page, and such functionality is exhibited with default paging.</span></span> <span data-ttu-id="93859-331">S vlastním stránkováním ale po odstranění tohoto posledního produktu na poslední stránce se v prvku GridView jednoduše z obrazovky úplně zmizí.</span><span class="sxs-lookup"><span data-stu-id="93859-331">With custom paging, however, after deleting that last product on the last page, the GridView simply disappears from the screen altogether.</span></span> <span data-ttu-id="93859-332">Přesný důvod, *Proč* k tomu dochází, je trochu nad rámec tohoto kurzu; viz [odstranění posledního záznamu na poslední stránce z prvku GridView s vlastním stránkováním](http://scottonwriting.net/sowblog/posts/7326.aspx) pro podrobnosti nízké úrovně jako zdroj tohoto problému.</span><span class="sxs-lookup"><span data-stu-id="93859-332">The precise reason *why* this happens is a bit beyond the scope of this tutorial; see [Deleting the Last Record on the Last Page from a GridView with Custom Paging](http://scottonwriting.net/sowblog/posts/7326.aspx) for the low-level details as to the source of this problem.</span></span> <span data-ttu-id="93859-333">V souhrnu je to kvůli následující sekvenci kroků, které jsou provedeny v prvku GridView při kliknutí na tlačítko Odstranit:</span><span class="sxs-lookup"><span data-stu-id="93859-333">In summary it s due to the following sequence of steps that are performed by the GridView when the Delete button is clicked:</span></span>

1. <span data-ttu-id="93859-334">Odstranit záznam</span><span class="sxs-lookup"><span data-stu-id="93859-334">Delete the record</span></span>
2. <span data-ttu-id="93859-335">Získat příslušné záznamy, které se zobrazí pro zadaný `PageIndex` a `PageSize`</span><span class="sxs-lookup"><span data-stu-id="93859-335">Get the appropriate records to display for the specified `PageIndex` and `PageSize`</span></span>
3. <span data-ttu-id="93859-336">Ověřte, zda `PageIndex` nepřekračuje počet stránek dat ve zdroji dat; Pokud má, automaticky sníží vlastnost `PageIndex` prvku GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-336">Check to ensure that the `PageIndex` does not exceed the number of pages of data in the data source; if it does, automatically decrement the GridView s `PageIndex` property</span></span>
4. <span data-ttu-id="93859-337">Vytvoření vazby příslušné stránky dat k prvku GridView pomocí záznamů získaných v kroku 2</span><span class="sxs-lookup"><span data-stu-id="93859-337">Bind the appropriate page of data to the GridView using the records obtained in Step 2</span></span>

<span data-ttu-id="93859-338">Problém vychází z faktu, že v kroku 2 `PageIndex`, který se používá při přepočetování záznamů k zobrazení, stále `PageIndex` poslední stránky, jejíž jediný záznam byl právě odstraněn.</span><span class="sxs-lookup"><span data-stu-id="93859-338">The problem stems from the fact that in Step 2 the `PageIndex` used when grabbing the records to display is still the `PageIndex` of the last page whose sole record was just deleted.</span></span> <span data-ttu-id="93859-339">Proto v kroku 2 nejsou vráceny *žádné* záznamy, protože poslední stránka dat již neobsahuje žádné záznamy.</span><span class="sxs-lookup"><span data-stu-id="93859-339">Therefore, in Step 2, *no* records are returned since that last page of data no longer contains any records.</span></span> <span data-ttu-id="93859-340">V kroku 3 potom v ovládacím prvku GridView vzroste, že jeho vlastnost `PageIndex` je větší než celkový počet stránek ve zdroji dat (protože jsme na poslední stránce odstranili poslední záznam), a proto sníží jeho vlastnost `PageIndex`.</span><span class="sxs-lookup"><span data-stu-id="93859-340">Then, in Step 3, the GridView realizes that its `PageIndex` property is greater than the total number of pages in the data source (since we ve deleted the last record in the last page) and therefore decrements its `PageIndex` property.</span></span> <span data-ttu-id="93859-341">V kroku 4 se GridView pokusí vázat sebe na data získaná v kroku 2; v kroku 2 však nebyly vráceny žádné záznamy, takže výsledkem je prázdný prvek GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-341">In Step 4 the GridView attempts to bind itself to the data retrieved in Step 2; however, in Step 2 no records were returned, therefore resulting in an empty GridView.</span></span> <span data-ttu-id="93859-342">Při výchozím stránkování se tento problém nejedná o Surface t, protože v kroku 2 se ze zdroje dat načítají *všechny* záznamy.</span><span class="sxs-lookup"><span data-stu-id="93859-342">With default paging, this problem doesn t surface because in Step 2 *all* records are retrieved from the data source.</span></span>

<span data-ttu-id="93859-343">K vyřešení tohoto problému máme dvě možnosti.</span><span class="sxs-lookup"><span data-stu-id="93859-343">To fix this we have two options.</span></span> <span data-ttu-id="93859-344">Prvním je vytvoření obslužné rutiny události pro ovládací prvek GridView s `RowDeleted`, který určuje, kolik záznamů bylo zobrazeno na stránce, která byla právě odstraněna.</span><span class="sxs-lookup"><span data-stu-id="93859-344">The first is to create an event handler for the GridView s `RowDeleted` event handler that determines how many records were displayed in the page that was just deleted.</span></span> <span data-ttu-id="93859-345">Pokud existoval pouze jeden záznam, musí být záznam právě odstraněn a musíme `PageIndex`prvky GridView.</span><span class="sxs-lookup"><span data-stu-id="93859-345">If there was only one record, then the record just deleted must have been the last one and we need to decrement the GridView s `PageIndex`.</span></span> <span data-ttu-id="93859-346">Samozřejmě chceme pouze aktualizovat `PageIndex`, pokud byla operace odstranění skutečně úspěšná, což lze určit tak, že zajistíte, že vlastnost `e.Exception` je `null`.</span><span class="sxs-lookup"><span data-stu-id="93859-346">Of course, we only want to update the `PageIndex` if the delete operation was actually successful, which can be determined by ensuring that the `e.Exception` property is `null`.</span></span>

<span data-ttu-id="93859-347">Tento přístup funguje, protože aktualizuje `PageIndex` po kroku 1, ale před krokem 2.</span><span class="sxs-lookup"><span data-stu-id="93859-347">This approach works because it updates the `PageIndex` after Step 1 but before Step 2.</span></span> <span data-ttu-id="93859-348">Proto se v kroku 2 vrátí vhodná sada záznamů.</span><span class="sxs-lookup"><span data-stu-id="93859-348">Therefore, in Step 2, the appropriate set of records is returned.</span></span> <span data-ttu-id="93859-349">K tomu použijte kód podobný následujícímu:</span><span class="sxs-lookup"><span data-stu-id="93859-349">To accomplish this, use code like the following:</span></span>

[!code-vb[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample11.vb)]

<span data-ttu-id="93859-350">Alternativním řešením je vytvořit obslužnou rutinu události pro událost ObjectDataSource s `RowDeleted` a nastavit vlastnost `AffectedRows` na hodnotu 1.</span><span class="sxs-lookup"><span data-stu-id="93859-350">An alternative workaround is to create an event handler for the ObjectDataSource s `RowDeleted` event and to set the `AffectedRows` property to a value of 1.</span></span> <span data-ttu-id="93859-351">Po odstranění záznamu v kroku 1 (ale před opakovaným načtením dat v kroku 2) prvek GridView aktualizuje jeho vlastnost `PageIndex`, pokud byl jeden nebo více řádků ovlivněn operací.</span><span class="sxs-lookup"><span data-stu-id="93859-351">After deleting the record in Step 1 (but before re-retrieving the data in Step 2), the GridView updates its `PageIndex` property if one or more rows were affected by the operation.</span></span> <span data-ttu-id="93859-352">Vlastnost `AffectedRows` však není nastavena prvkem ObjectDataSource, a proto je tento krok vynechán.</span><span class="sxs-lookup"><span data-stu-id="93859-352">However, the `AffectedRows` property is not set by the ObjectDataSource and therefore this step is omitted.</span></span> <span data-ttu-id="93859-353">Jedním ze způsobů, jak tento krok provést, je ručně nastavit vlastnost `AffectedRows`, pokud se operace odstranění úspěšně dokončí.</span><span class="sxs-lookup"><span data-stu-id="93859-353">One way to have this step executed is to manually set the `AffectedRows` property if the delete operation completes successfully.</span></span> <span data-ttu-id="93859-354">To lze provést pomocí kódu podobného následujícímu:</span><span class="sxs-lookup"><span data-stu-id="93859-354">This can be accomplished using code like the following:</span></span>

[!code-vb[Main](efficiently-paging-through-large-amounts-of-data-vb/samples/sample12.vb)]

<span data-ttu-id="93859-355">Kód pro obě tyto obslužné rutiny události lze nalézt v rámci třídy kódu na pozadí `EfficientPaging.aspx` příkladu.</span><span class="sxs-lookup"><span data-stu-id="93859-355">The code for both of these events handlers can be found in code-behind class of the `EfficientPaging.aspx` example.</span></span>

## <a name="comparing-the-performance-of-default-and-custom-paging"></a><span data-ttu-id="93859-356">Porovnání výkonu výchozích a vlastních stránkování</span><span class="sxs-lookup"><span data-stu-id="93859-356">Comparing the Performance of Default and Custom Paging</span></span>

<span data-ttu-id="93859-357">Vzhledem k tomu, že vlastní stránkování načítá pouze potřebné záznamy, zatímco výchozí stránkování vrátí *všechny* záznamy pro každou prohlíženou stránku, bude jasné, že vlastní stránkování je efektivnější než výchozí stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-357">Since custom paging only retrieves the needed records, whereas default paging returns *all* of the records for each page being viewed, it s clear that custom paging is more efficient than default paging.</span></span> <span data-ttu-id="93859-358">Ale jenom to, jak mnohem efektivnější je vlastní stránkování?</span><span class="sxs-lookup"><span data-stu-id="93859-358">But just how much more efficient is custom paging?</span></span> <span data-ttu-id="93859-359">Jaký je způsob, jakým můžete obcházet nárůst výkonu přesunutím z výchozího stránkování do vlastního stránkování?</span><span class="sxs-lookup"><span data-stu-id="93859-359">What sort of performance gains can be seen by moving from default paging to custom paging?</span></span>

<span data-ttu-id="93859-360">Neexistují však žádné místo, které by vyhovovaly všem odpovědím.</span><span class="sxs-lookup"><span data-stu-id="93859-360">Unfortunately, there s no one size fits all answer here.</span></span> <span data-ttu-id="93859-361">Zvýšení výkonu závisí na několika faktorech, na nejvýraznějších dvou typech záznamů a na zatížení na databázovém serveru a na komunikačních kanálech mezi webovým serverem a databázovým serverem.</span><span class="sxs-lookup"><span data-stu-id="93859-361">The performance gain depends on a number of factors, the most prominent two being the number of records being paged through and the load placed on the database server and communication channels between the web server and database server.</span></span> <span data-ttu-id="93859-362">U malých tabulek, které mají jen pár desítek záznamů, může být rozdíl výkonu zanedbatelný.</span><span class="sxs-lookup"><span data-stu-id="93859-362">For small tables with just a few dozen records, the performance difference may be negligible.</span></span> <span data-ttu-id="93859-363">V případě rozsáhlých tabulek, které mají tisíce až stovky tisíc řádků, je rozdíl mezi výkonem i čárkou.</span><span class="sxs-lookup"><span data-stu-id="93859-363">For large tables, with thousands to hundreds of thousands of rows, though, the performance difference is acute.</span></span>

<span data-ttu-id="93859-364">Článek o svém [vlastním stránkování v ASP.NET 2,0 s SQL Server 2005](http://aspnet.4guysfromrolla.com/articles/031506-1.aspx)obsahuje některé testy výkonu, které jsem narazil na rozdíly mezi těmito dvěma technikami stránkování při procházení databázovými tabulkami pomocí záznamů 50 000.</span><span class="sxs-lookup"><span data-stu-id="93859-364">An article of mine, [Custom Paging in ASP.NET 2.0 with SQL Server 2005](http://aspnet.4guysfromrolla.com/articles/031506-1.aspx), contains some performance tests I ran to exhibit the differences in performance between these two paging techniques when paging through a database table with 50,000 records.</span></span> <span data-ttu-id="93859-365">V těchto testech jsem zkoumal čas spustit dotaz na úrovni SQL Server (pomocí [SQL profileru](https://msdn.microsoft.com/library/ms173757.aspx)) a na stránce ASP.NET pomocí [funkcí trasování ASP.NET s](https://msdn.microsoft.com/library/y13fw6we.aspx).</span><span class="sxs-lookup"><span data-stu-id="93859-365">In these tests I examined both the time to execute the query at the SQL Server level (using [SQL Profiler](https://msdn.microsoft.com/library/ms173757.aspx)) and at the ASP.NET page using [ASP.NET s tracing features](https://msdn.microsoft.com/library/y13fw6we.aspx).</span></span> <span data-ttu-id="93859-366">Pamatujte, že tyto testy byly spuštěny ve vývojovém poli s jedním aktivním uživatelem, a proto nejsou vědecky a Nenapodobují typický vzor zatížení webu.</span><span class="sxs-lookup"><span data-stu-id="93859-366">Keep in mind that these tests were run on my development box with a single active user, and therefore are unscientific and do not mimic typical website load patterns.</span></span> <span data-ttu-id="93859-367">Bez ohledu na to, jaké výsledky ilustrují relativní rozdíly v době provádění výchozího a vlastního stránkování při práci s dostatečně velkým objemem dat.</span><span class="sxs-lookup"><span data-stu-id="93859-367">Regardless, the results illustrate the relative differences in execution time for default and custom paging when working with sufficiently large amounts of data.</span></span>

|  | <span data-ttu-id="93859-368">**Průměrná doba trvání (s)**</span><span class="sxs-lookup"><span data-stu-id="93859-368">**Avg. Duration (sec)**</span></span> | <span data-ttu-id="93859-369">**Operace**</span><span class="sxs-lookup"><span data-stu-id="93859-369">**Reads**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="93859-370">**Výchozí stránkovací Profiler SQL**</span><span class="sxs-lookup"><span data-stu-id="93859-370">**Default Paging SQL Profiler**</span></span> | <span data-ttu-id="93859-371">1.411</span><span class="sxs-lookup"><span data-stu-id="93859-371">1.411</span></span> | <span data-ttu-id="93859-372">383</span><span class="sxs-lookup"><span data-stu-id="93859-372">383</span></span> |
| <span data-ttu-id="93859-373">**Vlastní stránkování – Profiler SQL**</span><span class="sxs-lookup"><span data-stu-id="93859-373">**Custom Paging SQL Profiler**</span></span> | <span data-ttu-id="93859-374">0.002</span><span class="sxs-lookup"><span data-stu-id="93859-374">0.002</span></span> | <span data-ttu-id="93859-375">29</span><span class="sxs-lookup"><span data-stu-id="93859-375">29</span></span> |
| <span data-ttu-id="93859-376">**Výchozí ASP.NET trasování stránkování**</span><span class="sxs-lookup"><span data-stu-id="93859-376">**Default Paging ASP.NET Trace**</span></span> | <span data-ttu-id="93859-377">2.379</span><span class="sxs-lookup"><span data-stu-id="93859-377">2.379</span></span> | <span data-ttu-id="93859-378">*–*</span><span class="sxs-lookup"><span data-stu-id="93859-378">*N/A*</span></span> |
| <span data-ttu-id="93859-379">**Vlastní ASP.NET trasování stránkování**</span><span class="sxs-lookup"><span data-stu-id="93859-379">**Custom Paging ASP.NET Trace**</span></span> | <span data-ttu-id="93859-380">0.029</span><span class="sxs-lookup"><span data-stu-id="93859-380">0.029</span></span> | <span data-ttu-id="93859-381">*–*</span><span class="sxs-lookup"><span data-stu-id="93859-381">*N/A*</span></span> |

<span data-ttu-id="93859-382">Jak vidíte, načítají se konkrétní stránka dat, která vyžaduje 354 čtení v průměru a dokončených ve zlomcích času.</span><span class="sxs-lookup"><span data-stu-id="93859-382">As you can see, retrieving a particular page of data required 354 less reads on average and completed in a fraction of the time.</span></span> <span data-ttu-id="93859-383">Na stránce ASP.NET se vlastní stránka mohla vykreslit za blížící se 1/100<sup>tou</sup> dobu trvání při použití výchozího stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-383">At the ASP.NET page, custom the page was able to render in close to 1/100<sup>th</sup> of the time it took when using default paging.</span></span> <span data-ttu-id="93859-384">V [tomto článku](http://aspnet.4guysfromrolla.com/articles/031506-1.aspx) najdete další informace o těchto výsledcích spolu s kódem a databází, kterou si můžete stáhnout pro reprodukování těchto testů ve vlastním prostředí.</span><span class="sxs-lookup"><span data-stu-id="93859-384">See [my article](http://aspnet.4guysfromrolla.com/articles/031506-1.aspx) for more information on these results along with code and a database you can download to reproduce these tests in your own environment.</span></span>

## <a name="summary"></a><span data-ttu-id="93859-385">Souhrn</span><span class="sxs-lookup"><span data-stu-id="93859-385">Summary</span></span>

<span data-ttu-id="93859-386">Výchozí stránkování je cinch k implementaci zaškrtnutím políčka Povolit stránkování v inteligentní značce webového ovládacího prvku dat, ale jednoduchost se dostane na náklady na výkon.</span><span class="sxs-lookup"><span data-stu-id="93859-386">Default paging is a cinch to implement just check the Enable Paging checkbox in the data Web control s smart tag but such simplicity comes at the cost of performance.</span></span> <span data-ttu-id="93859-387">S výchozím stránkováním, když uživatel požádá o jakoukoli stránku dat, vrátí se *všechny* záznamy, i když je možné zobrazit jenom malé zlomky.</span><span class="sxs-lookup"><span data-stu-id="93859-387">With default paging, when a user requests any page of data *all* records are returned, even though only a tiny fraction of them may be shown.</span></span> <span data-ttu-id="93859-388">Pro boj proti této režii výkonu prvek ObjectDataSource nabízí alternativní stránkování možnosti stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-388">To combat this performance overhead, the ObjectDataSource offers an alternative paging option custom paging.</span></span>

<span data-ttu-id="93859-389">I když se vlastní stránkování vylepšuje při potížích s výkonem výchozích stránkování načtením pouze těch záznamů, které je třeba zobrazit, je více zapojeno k implementaci vlastního stránkování.</span><span class="sxs-lookup"><span data-stu-id="93859-389">While custom paging improves upon default paging s performance issues by retrieving only those records that need to be displayed, it s more involved to implement custom paging.</span></span> <span data-ttu-id="93859-390">Nejprve je třeba zapsat dotaz, který bude správně (a efektivně) přistupovat ke konkrétní podmnožině požadovaných záznamů.</span><span class="sxs-lookup"><span data-stu-id="93859-390">First, a query must be written that correctly (and efficiently) accesses the specific subset of records requested.</span></span> <span data-ttu-id="93859-391">To lze provést několika způsoby. ta, kterou jsme prozkoumali v tomto kurzu, je použití SQL Server 2005 s New `ROW_NUMBER()` funkce k zařazení výsledků a vrácení pouze těch výsledků, jejichž řazení spadá do zadaného rozsahu.</span><span class="sxs-lookup"><span data-stu-id="93859-391">This can be accomplished in a number of ways; the one we examined in this tutorial is to use SQL Server 2005 s new `ROW_NUMBER()` function to rank results, and then to return just those results whose ranking falls within a specified range.</span></span> <span data-ttu-id="93859-392">Kromě toho je potřeba přidat způsob, jak určit celkový počet záznamů, které se stránkují.</span><span class="sxs-lookup"><span data-stu-id="93859-392">Furthermore, we need to add a means to determine the total number of records being paged through.</span></span> <span data-ttu-id="93859-393">Po vytvoření těchto metod DAL a knihoven BLL je také potřeba nakonfigurovat prvek ObjectDataSource tak, aby bylo možné určit, kolik celkových záznamů se stránkuje, a může správně předávat index řádků zahájení a maximální počet řádků pro knihoven BLL.</span><span class="sxs-lookup"><span data-stu-id="93859-393">After creating these DAL and BLL methods, we also need to configure the ObjectDataSource so that it can determine how many total records are being paged through and can correctly pass the Start Row Index and Maximum Rows values to the BLL.</span></span>

<span data-ttu-id="93859-394">Při implementaci vlastního stránkování je potřeba provést několik kroků a není skoro tak jednoduché jako výchozí stránkování, vlastní stránkování je nutné při stránkování prostřednictvím dostatečně velkých objemů dat.</span><span class="sxs-lookup"><span data-stu-id="93859-394">While implementing custom paging does require a number of steps and is not nearly as simple as default paging, custom paging is a necessity when paging through sufficiently large amounts of data.</span></span> <span data-ttu-id="93859-395">Vzhledem k zobrazeným výsledkům může vlastní stránkování uvolnit sekundy od času vykreslování stránky ASP.NET a může vyšetřit zatížení databázového serveru o jeden mnohem více objednávek.</span><span class="sxs-lookup"><span data-stu-id="93859-395">As the results examined showed, custom paging can shed seconds off of the ASP.NET page render time and can lighten the load on the database server by one ore more orders of magnitude.</span></span>

<span data-ttu-id="93859-396">Šťastné programování!</span><span class="sxs-lookup"><span data-stu-id="93859-396">Happy Programming!</span></span>

## <a name="about-the-author"></a><span data-ttu-id="93859-397">O autorovi</span><span class="sxs-lookup"><span data-stu-id="93859-397">About the Author</span></span>

<span data-ttu-id="93859-398">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), autor 7 ASP/ASP. NET Books a zakladatel of [4GuysFromRolla.com](http://www.4guysfromrolla.com), pracoval s webovými technologiemi Microsoftu od 1998.</span><span class="sxs-lookup"><span data-stu-id="93859-398">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of seven ASP/ASP.NET books and founder of [4GuysFromRolla.com](http://www.4guysfromrolla.com), has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="93859-399">Scott funguje jako nezávislý konzultant, Trainer a zapisovač.</span><span class="sxs-lookup"><span data-stu-id="93859-399">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="93859-400">Nejnovější kniha je [*Sams naučit se ASP.NET 2,0 za 24 hodin*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span><span class="sxs-lookup"><span data-stu-id="93859-400">His latest book is [*Sams Teach Yourself ASP.NET 2.0 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="93859-401">Dá se získat na [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="93859-401">He can be reached at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span> <span data-ttu-id="93859-402">nebo prostřednictvím svého blogu, který najdete na adrese [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span><span class="sxs-lookup"><span data-stu-id="93859-402">or via his blog, which can be found at [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="93859-403">[Předchozí](paging-and-sorting-report-data-vb.md)
> [Další](sorting-custom-paged-data-vb.md)</span><span class="sxs-lookup"><span data-stu-id="93859-403">[Previous](paging-and-sorting-report-data-vb.md)
[Next](sorting-custom-paged-data-vb.md)</span></span>
