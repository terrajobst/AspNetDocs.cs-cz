---
uid: mvc/overview/getting-started/getting-started-with-ef-using-mvc/creating-a-more-complex-data-model-for-an-asp-net-mvc-application
title: 'Kurz: Vytvoření složitějšího datového modelu pro aplikaci ASP.NET MVC'
author: tdykstra
description: V tomto kurzu přidáte další entity a vztahy a budete přizpůsobovat datový model zadáním pravidel formátování, ověřování a mapování databáze.
ms.author: riande
ms.date: 01/22/2019
ms.topic: tutorial
ms.assetid: 46f7f3c9-274f-4649-811d-92222a9b27e2
msc.legacyurl: /mvc/overview/getting-started/getting-started-with-ef-using-mvc/creating-a-more-complex-data-model-for-an-asp-net-mvc-application
msc.type: authoredcontent
ms.openlocfilehash: 933354b09eb4674e6f523f8a65816410521f026f
ms.sourcegitcommit: aa3c2efd56466fc6bdc387ee01ad6f50a261665b
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/26/2019
ms.locfileid: "70020999"
---
# <a name="tutorial-create-a-more-complex-data-model-for-an-aspnet-mvc-app"></a><span data-ttu-id="cab65-103">Kurz: Vytvoření složitějšího datového modelu pro aplikaci ASP.NET MVC</span><span class="sxs-lookup"><span data-stu-id="cab65-103">Tutorial: Create a more complex data model for an ASP.NET MVC app</span></span>

<span data-ttu-id="cab65-104">V předchozích kurzech jste pracovali s jednoduchým datovým modelem, který se skládá ze tří entit.</span><span class="sxs-lookup"><span data-stu-id="cab65-104">In the previous tutorials you worked with a simple data model that was composed of three entities.</span></span> <span data-ttu-id="cab65-105">V tomto kurzu přidáte další entity a vztahy a upravíte datový model zadáním pravidel formátování, ověřování a mapování databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-105">In this tutorial you add more entities and relationships and you customize the data model by specifying formatting, validation, and database mapping rules.</span></span> <span data-ttu-id="cab65-106">Tento článek ukazuje dva způsoby přizpůsobení datového modelu: přidáním atributů do tříd entit a přidáním kódu do třídy kontextu databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-106">This article shows two ways to customize the data model: by adding attributes to entity classes and by adding code to the database context class.</span></span>

<span data-ttu-id="cab65-107">Až budete hotovi, třídy entit vytvoří dokončený datový model, který je znázorněn na následujícím obrázku:</span><span class="sxs-lookup"><span data-stu-id="cab65-107">When you're finished, the entity classes will make up the completed data model that's shown in the following illustration:</span></span>

![School_class_diagram](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image1.png)

<span data-ttu-id="cab65-109">V tomto kurzu se naučíte:</span><span class="sxs-lookup"><span data-stu-id="cab65-109">In this tutorial, you:</span></span>

> [!div class="checklist"]
> * <span data-ttu-id="cab65-110">Přizpůsobení datového modelu</span><span class="sxs-lookup"><span data-stu-id="cab65-110">Customize the data model</span></span>
> * <span data-ttu-id="cab65-111">Aktualizovat entitu studenta</span><span class="sxs-lookup"><span data-stu-id="cab65-111">Update Student entity</span></span>
> * <span data-ttu-id="cab65-112">Vytvořit entitu instruktora</span><span class="sxs-lookup"><span data-stu-id="cab65-112">Create Instructor entity</span></span>
> * <span data-ttu-id="cab65-113">Vytvořit entitu OfficeAssignment</span><span class="sxs-lookup"><span data-stu-id="cab65-113">Create OfficeAssignment entity</span></span>
> * <span data-ttu-id="cab65-114">Úprava entity kurzu</span><span class="sxs-lookup"><span data-stu-id="cab65-114">Modify the Course entity</span></span>
> * <span data-ttu-id="cab65-115">Vytvořit entitu oddělení</span><span class="sxs-lookup"><span data-stu-id="cab65-115">Create the Department entity</span></span>
> * <span data-ttu-id="cab65-116">Úprava entity registrace</span><span class="sxs-lookup"><span data-stu-id="cab65-116">Modify the Enrollment entity</span></span>
> * <span data-ttu-id="cab65-117">Přidat kód do kontextu databáze</span><span class="sxs-lookup"><span data-stu-id="cab65-117">Add code to database context</span></span>
> * <span data-ttu-id="cab65-118">Počáteční databáze s testovacími daty</span><span class="sxs-lookup"><span data-stu-id="cab65-118">Seed database with test data</span></span>
> * <span data-ttu-id="cab65-119">Přidání migrace</span><span class="sxs-lookup"><span data-stu-id="cab65-119">Add a migration</span></span>
> * <span data-ttu-id="cab65-120">Aktualizace databáze</span><span class="sxs-lookup"><span data-stu-id="cab65-120">Update the database</span></span>

## <a name="prerequisites"></a><span data-ttu-id="cab65-121">Požadavky</span><span class="sxs-lookup"><span data-stu-id="cab65-121">Prerequisites</span></span>

* [<span data-ttu-id="cab65-122">Code First migrace a nasazení</span><span class="sxs-lookup"><span data-stu-id="cab65-122">Code First migrations and deployment</span></span>](migrations-and-deployment-with-the-entity-framework-in-an-asp-net-mvc-application.md)

## <a name="customize-the-data-model"></a><span data-ttu-id="cab65-123">Přizpůsobení datového modelu</span><span class="sxs-lookup"><span data-stu-id="cab65-123">Customize the data model</span></span>

<span data-ttu-id="cab65-124">V této části se dozvíte, jak přizpůsobit datový model pomocí atributů, které určují pravidla formátování, ověřování a mapování databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-124">In this section you'll see how to customize the data model by using attributes that specify formatting, validation, and database mapping rules.</span></span> <span data-ttu-id="cab65-125">Potom v několika následujících částech vytvoříte kompletní `School` datový model přidáním atributů do tříd, které jste již vytvořili, a vytvořením nových tříd pro zbývající typy entit v modelu.</span><span class="sxs-lookup"><span data-stu-id="cab65-125">Then in several of the following sections you'll create the complete `School` data model by adding attributes to the classes you already created and creating new classes for the remaining entity types in the model.</span></span>

### <a name="the-datatype-attribute"></a><span data-ttu-id="cab65-126">Atribut DataType</span><span class="sxs-lookup"><span data-stu-id="cab65-126">The DataType Attribute</span></span>

<span data-ttu-id="cab65-127">Pro data o registraci studenta se aktuálně zobrazují všechny webové stránky, které jsou aktuálně současně s datem, i když jsou pro toto pole k disstará data.</span><span class="sxs-lookup"><span data-stu-id="cab65-127">For student enrollment dates, all of the web pages currently display the time along with the date, although all you care about for this field is the date.</span></span> <span data-ttu-id="cab65-128">Pomocí atributů datových poznámek můžete vytvořit jednu změnu kódu, která bude opravovat formát zobrazení v každém zobrazení, které zobrazuje data.</span><span class="sxs-lookup"><span data-stu-id="cab65-128">By using data annotation attributes, you can make one code change that will fix the display format in every view that shows the data.</span></span> <span data-ttu-id="cab65-129">Chcete-li zobrazit příklad toho, jak to provést, přidáte atribut do `EnrollmentDate` vlastnosti `Student` třídy.</span><span class="sxs-lookup"><span data-stu-id="cab65-129">To see an example of how to do that, you'll add an attribute to the `EnrollmentDate` property in the `Student` class.</span></span>

<span data-ttu-id="cab65-130">V *Models\Student.cs*přidejte `using` příkaz pro `System.ComponentModel.DataAnnotations` `DataType` obornázvů`DisplayFormat` a přidejte atributy do vlastnosti,jakjeznázorněnovnásledujícímpříkladu:`EnrollmentDate`</span><span class="sxs-lookup"><span data-stu-id="cab65-130">In *Models\Student.cs*, add a `using` statement for the `System.ComponentModel.DataAnnotations` namespace and add `DataType` and `DisplayFormat` attributes to the `EnrollmentDate` property, as shown in the following example:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample1.cs?highlight=3,12-13)]

<span data-ttu-id="cab65-131">Atribut [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) se používá k určení datového typu, který je konkrétnější než vnitřní typ databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-131">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute is used to specify a data type that is more specific than the database intrinsic type.</span></span> <span data-ttu-id="cab65-132">V tomto případě chceme sledovat pouze datum, nikoli datum a čas.</span><span class="sxs-lookup"><span data-stu-id="cab65-132">In this case we only want to keep track of the date, not the date and time.</span></span> <span data-ttu-id="cab65-133">[Výčet DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) poskytuje mnoho datových typů, jako je *datum, čas, PhoneNumber, měna, EmailAddress* a další.</span><span class="sxs-lookup"><span data-stu-id="cab65-133">The [DataType Enumeration](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) provides for many data types, such as *Date, Time, PhoneNumber, Currency, EmailAddress* and more.</span></span> <span data-ttu-id="cab65-134">`DataType` Atribut může také povolit aplikaci automatické poskytování funkcí specifických pro typ.</span><span class="sxs-lookup"><span data-stu-id="cab65-134">The `DataType` attribute can also enable the application to automatically provide type-specific features.</span></span> <span data-ttu-id="cab65-135">Například `mailto:` odkaz lze vytvořit pro [typ DataType. EmailAddress](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx)a selektor data lze zadat pro [typ DataType. Date](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) v prohlížečích, které podporují [HTML5](http://html5.org/).</span><span class="sxs-lookup"><span data-stu-id="cab65-135">For example, a `mailto:` link can be created for [DataType.EmailAddress](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx), and a date selector can be provided for [DataType.Date](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) in browsers that support [HTML5](http://html5.org/).</span></span> <span data-ttu-id="cab65-136">Atributy [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) emitují atributy [data](http://ejohn.org/blog/html-5-data-attributes/) HTML 5 (vyslovované *datové přerušované*), které mohou prohlížeče HTML 5 pochopit.</span><span class="sxs-lookup"><span data-stu-id="cab65-136">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attributes emits HTML 5 [data-](http://ejohn.org/blog/html-5-data-attributes/) (pronounced *data dash*) attributes that HTML 5 browsers can understand.</span></span> <span data-ttu-id="cab65-137">Atributy [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) neposkytují žádné ověření.</span><span class="sxs-lookup"><span data-stu-id="cab65-137">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attributes do not provide any validation.</span></span>

<span data-ttu-id="cab65-138">`DataType.Date`neurčuje formát data, které se zobrazí.</span><span class="sxs-lookup"><span data-stu-id="cab65-138">`DataType.Date` does not specify the format of the date that is displayed.</span></span> <span data-ttu-id="cab65-139">Ve výchozím nastavení se datové pole zobrazuje v závislosti na výchozích formátech na základě objektu [CultureInfo](https://msdn.microsoft.com/library/vstudio/system.globalization.cultureinfo(v=vs.110).aspx)serveru.</span><span class="sxs-lookup"><span data-stu-id="cab65-139">By default, the data field is displayed according to the default formats based on the server's [CultureInfo](https://msdn.microsoft.com/library/vstudio/system.globalization.cultureinfo(v=vs.110).aspx).</span></span>

<span data-ttu-id="cab65-140">`DisplayFormat` Atribut slouží k explicitnímu zadání formátu data:</span><span class="sxs-lookup"><span data-stu-id="cab65-140">The `DisplayFormat` attribute is used to explicitly specify the date format:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample2.cs)]

<span data-ttu-id="cab65-141">`ApplyFormatInEditMode` Nastavení určuje, že zadané formátování by mělo být použito i v případě, že se hodnota v textovém poli zobrazí pro úpravy.</span><span class="sxs-lookup"><span data-stu-id="cab65-141">The `ApplyFormatInEditMode` setting specifies that the specified formatting should also be applied when the value is displayed in a text box for editing.</span></span> <span data-ttu-id="cab65-142">(Pro některá pole (například pro hodnoty měny možná nebudete chtít), nebudete chtít v textovém poli pro úpravy chtít symbol měny.)</span><span class="sxs-lookup"><span data-stu-id="cab65-142">(You might not want that for some fields — for example, for currency values, you might not want the currency symbol in the text box for editing.)</span></span>

<span data-ttu-id="cab65-143">Můžete použít atribut [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) sám o sobě, ale obecně je vhodné použít také atribut [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) .</span><span class="sxs-lookup"><span data-stu-id="cab65-143">You can use the [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) attribute by itself, but it's generally a good idea to use the [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute also.</span></span> <span data-ttu-id="cab65-144">Atribut vyjadřuje sémantiku dat na rozdíl od způsobu vykreslování na obrazovce a poskytuje následující výhody, které se `DisplayFormat`nedají získat: `DataType`</span><span class="sxs-lookup"><span data-stu-id="cab65-144">The `DataType` attribute conveys the *semantics* of the data as opposed to how to render it on a screen, and provides the following benefits that you don't get with `DisplayFormat`:</span></span>

- <span data-ttu-id="cab65-145">Prohlížeč může povolit funkce HTML5 (například pro zobrazení ovládacího prvku kalendáře, symbolu měny odpovídající národním prostředí, e-mailových odkazů, některých ověření vstupu na straně klienta atd.).</span><span class="sxs-lookup"><span data-stu-id="cab65-145">The browser can enable HTML5 features (for example to show a calendar control, the locale-appropriate currency symbol, email links, some client-side input validation, etc.).</span></span>
- <span data-ttu-id="cab65-146">Ve výchozím nastavení bude prohlížeč data vykreslovat pomocí správného formátu na základě vašeho [národního prostředí](https://msdn.microsoft.com/library/vstudio/wyzd2bce.aspx).</span><span class="sxs-lookup"><span data-stu-id="cab65-146">By default, the browser will render data using the correct format based on your [locale](https://msdn.microsoft.com/library/vstudio/wyzd2bce.aspx).</span></span>
- <span data-ttu-id="cab65-147">Atribut [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) může MVC povolit, aby vybrali šablonu pravého pole pro vykreslení dat ( [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) používá šablonu řetězce).</span><span class="sxs-lookup"><span data-stu-id="cab65-147">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute can enable MVC to choose the right field template to render the data (the [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) uses the string template).</span></span> <span data-ttu-id="cab65-148">Další informace najdete v tématu [šablony ASP.NET MVC 2](http://bradwilson.typepad.com/blog/2009/10/aspnet-mvc-2-templates-part-1-introduction.html)pro Brad Wilson.</span><span class="sxs-lookup"><span data-stu-id="cab65-148">For more information, see Brad Wilson's [ASP.NET MVC 2 Templates](http://bradwilson.typepad.com/blog/2009/10/aspnet-mvc-2-templates-part-1-introduction.html).</span></span> <span data-ttu-id="cab65-149">(I když je napsaný pro MVC 2, Tento článek se stále vztahuje na aktuální verzi ASP.NET MVC.)</span><span class="sxs-lookup"><span data-stu-id="cab65-149">(Though written for MVC 2, this article still applies to the current version of ASP.NET MVC.)</span></span>

<span data-ttu-id="cab65-150">Použijete `DataType` -li atribut s polem datum, je nutné `DisplayFormat` zadat atribut také, aby bylo zajištěno, že pole bude v prohlížečích Chrome správně vykresleno.</span><span class="sxs-lookup"><span data-stu-id="cab65-150">If you use the `DataType` attribute with a date field, you have to specify the `DisplayFormat` attribute also in order to ensure that the field renders correctly in Chrome browsers.</span></span> <span data-ttu-id="cab65-151">Další informace najdete v [tomto vlákně StackOverflow](http://stackoverflow.com/questions/12633471/mvc4-datatype-date-editorfor-wont-display-date-value-in-chrome-fine-in-ie).</span><span class="sxs-lookup"><span data-stu-id="cab65-151">For more information, see [this StackOverflow thread](http://stackoverflow.com/questions/12633471/mvc4-datatype-date-editorfor-wont-display-date-value-in-chrome-fine-in-ie).</span></span>

<span data-ttu-id="cab65-152">Další informace o tom, jak zpracovat jiné formáty kalendářních dat v MVC, [najdete v tématu MVC 5 Úvod: Prozkoumání metod Edit a zobrazení](../introduction/examining-the-edit-methods-and-edit-view.md) pro úpravy a hledání na stránce pro &quot;mezinárodní využití.&quot;</span><span class="sxs-lookup"><span data-stu-id="cab65-152">For more information about how to handle other date formats in MVC, go to [MVC 5 Introduction: Examining the Edit Methods and Edit View](../introduction/examining-the-edit-methods-and-edit-view.md) and search in the page for &quot;internationalization&quot;.</span></span>

<span data-ttu-id="cab65-153">Spusťte znovu stránku indexu studenta a Všimněte si, že časy se už nezobrazuje pro data registrace.</span><span class="sxs-lookup"><span data-stu-id="cab65-153">Run the Student Index page again and notice that times are no longer displayed for the enrollment dates.</span></span> <span data-ttu-id="cab65-154">Totéž platí pro všechna zobrazení, která používají `Student` model.</span><span class="sxs-lookup"><span data-stu-id="cab65-154">The same will be true for any view that uses the `Student` model.</span></span>

![Students_index_page_with_formatted_date](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image2.png)

### <a name="the-stringlengthattribute"></a><span data-ttu-id="cab65-156">StringLengthAttribute</span><span class="sxs-lookup"><span data-stu-id="cab65-156">The StringLengthAttribute</span></span>

<span data-ttu-id="cab65-157">Můžete také zadat pravidla ověřování dat a chybové zprávy ověřování pomocí atributů.</span><span class="sxs-lookup"><span data-stu-id="cab65-157">You can also specify data validation rules and validation error messages using attributes.</span></span> <span data-ttu-id="cab65-158">[Atribut StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) nastaví maximální délku v databázi a poskytuje ověřování na straně klienta a na straně serveru pro ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="cab65-158">The [StringLength attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) sets the maximum length in the database and provides client side and server side validation for ASP.NET MVC.</span></span> <span data-ttu-id="cab65-159">V tomto atributu můžete také zadat minimální délku řetězce, ale minimální hodnota nemá žádný vliv na schéma databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-159">You can also specify the minimum string length in this attribute, but the minimum value has no impact on the database schema.</span></span>

<span data-ttu-id="cab65-160">Předpokládejme, že chcete zajistit, aby uživatelé nezadali více než 50 znaků pro název.</span><span class="sxs-lookup"><span data-stu-id="cab65-160">Suppose you want to ensure that users don't enter more than 50 characters for a name.</span></span> <span data-ttu-id="cab65-161">Chcete-li přidat toto omezení [](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) , přidejte atributy StringLength `LastName` do `FirstMidName` vlastností a, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="cab65-161">To add this limitation, add [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attributes to the `LastName` and `FirstMidName` properties, as shown in the following example:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample3.cs?highlight=10,12)]

<span data-ttu-id="cab65-162">Atribut [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) nezabrání uživateli v zadání prázdného místa pro název.</span><span class="sxs-lookup"><span data-stu-id="cab65-162">The [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attribute won't prevent a user from entering white space for a name.</span></span> <span data-ttu-id="cab65-163">Pro použití omezení na vstup můžete použít atribut [RegularExpression](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) .</span><span class="sxs-lookup"><span data-stu-id="cab65-163">You can use the [RegularExpression](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) attribute to apply restrictions to the input.</span></span> <span data-ttu-id="cab65-164">Například následující kód vyžaduje, aby první znak byl velkými písmeny a aby zbývající znaky byly abecedně:</span><span class="sxs-lookup"><span data-stu-id="cab65-164">For example the following code requires the first character to be upper case and the remaining characters to be alphabetical:</span></span>

`[RegularExpression(@"^[A-Z]+[a-zA-Z""'\s-]*$")]`

<span data-ttu-id="cab65-165">Atribut [MaxLength](https://msdn.microsoft.com/library/System.ComponentModel.DataAnnotations.MaxLengthAttribute.aspx) poskytuje podobnou funkci atributu [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) , ale neposkytuje ověřování na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="cab65-165">The [MaxLength](https://msdn.microsoft.com/library/System.ComponentModel.DataAnnotations.MaxLengthAttribute.aspx) attribute provides similar functionality to the [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attribute but doesn't provide client side validation.</span></span>

<span data-ttu-id="cab65-166">Spusťte aplikaci a klikněte na kartu **studenti** . Zobrazí se následující chyba:</span><span class="sxs-lookup"><span data-stu-id="cab65-166">Run the application and click the **Students** tab. You get the following error:</span></span>

<span data-ttu-id="cab65-167">*Model, který provedl zálohování kontextu ' SchoolContext ', se od vytvoření databáze změnil. Zvažte použití Migrace Code First k aktualizaci databáze ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)).*</span><span class="sxs-lookup"><span data-stu-id="cab65-167">*The model backing the 'SchoolContext' context has changed since the database was created. Consider using Code First Migrations to update the database ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)).*</span></span>

<span data-ttu-id="cab65-168">Model databáze se změnil způsobem, který vyžaduje změnu schématu databáze a Entity Framework zjistil, že.</span><span class="sxs-lookup"><span data-stu-id="cab65-168">The database model has changed in a way that requires a change in the database schema, and Entity Framework detected that.</span></span> <span data-ttu-id="cab65-169">Pomocí migrace aktualizujete schéma bez ztráty dat přidaných do databáze pomocí uživatelského rozhraní.</span><span class="sxs-lookup"><span data-stu-id="cab65-169">You'll use migrations to update the schema without losing any data that you added to the database by using the UI.</span></span> <span data-ttu-id="cab65-170">Pokud jste změnili data vytvořená `Seed` metodou, která se změní zpátky na původní stav z důvodu metody [AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) , kterou používáte v `Seed` metodě.</span><span class="sxs-lookup"><span data-stu-id="cab65-170">If you changed data that was created by the `Seed` method, that will be changed back to its original state because of the [AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) method that you're using in the `Seed` method.</span></span> <span data-ttu-id="cab65-171">([AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) je ekvivalentem operace "Upsert" z terminologie databáze.)</span><span class="sxs-lookup"><span data-stu-id="cab65-171">([AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) is equivalent to an "upsert" operation from database terminology.)</span></span>

<span data-ttu-id="cab65-172">V konzole správce balíčků (PMC) zadejte následující příkazy:</span><span class="sxs-lookup"><span data-stu-id="cab65-172">In the Package Manager Console (PMC), enter the following commands:</span></span>

[!code-console[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample4.cmd)]

<span data-ttu-id="cab65-173">Příkaz vytvoří soubor s názvem  *&lt;&gt;TimeStamp\_MaxLengthOnNames.cs.* `add-migration`</span><span class="sxs-lookup"><span data-stu-id="cab65-173">The `add-migration` command creates a file named *&lt;timeStamp&gt;\_MaxLengthOnNames.cs*.</span></span> <span data-ttu-id="cab65-174">Tento soubor obsahuje kód v `Up` metodě, která bude aktualizovat databázi tak, aby odpovídala aktuálnímu datovému modelu.</span><span class="sxs-lookup"><span data-stu-id="cab65-174">This file contains code in the `Up` method that will update the database to match the current data model.</span></span> <span data-ttu-id="cab65-175">`update-database` Příkaz spustil tento kód.</span><span class="sxs-lookup"><span data-stu-id="cab65-175">The `update-database` command ran that code.</span></span>

<span data-ttu-id="cab65-176">Časové razítko, které se přiřadí k názvu souboru migrace, používá Entity Framework k seřazení migrace.</span><span class="sxs-lookup"><span data-stu-id="cab65-176">The timestamp prepended to the migrations file name is used by Entity Framework to order the migrations.</span></span> <span data-ttu-id="cab65-177">Před spuštěním `update-database` příkazu můžete vytvořit několik migrací a všechny migrace pak budou aplikovány v pořadí, ve kterém byly vytvořeny.</span><span class="sxs-lookup"><span data-stu-id="cab65-177">You can create multiple migrations before running the `update-database` command, and then all of the migrations are applied in the order in which they were created.</span></span>

<span data-ttu-id="cab65-178">Spusťte stránku **vytvořit** a zadejte název delší než 50 znaků.</span><span class="sxs-lookup"><span data-stu-id="cab65-178">Run the **Create** page, and enter either name longer than 50 characters.</span></span> <span data-ttu-id="cab65-179">Když kliknete na **vytvořit**, ověřování na straně klienta zobrazí chybovou zprávu: *Pole LastName musí být řetězec s maximální délkou 50.*</span><span class="sxs-lookup"><span data-stu-id="cab65-179">When you click **Create**, client side validation shows an error message: *The field LastName must be a string with a maximum length of 50.*</span></span>

### <a name="the-column-attribute"></a><span data-ttu-id="cab65-180">Atribut Column</span><span class="sxs-lookup"><span data-stu-id="cab65-180">The Column Attribute</span></span>

<span data-ttu-id="cab65-181">Můžete také použít atributy pro řízení způsobu, jakým jsou třídy a vlastnosti namapovány na databázi.</span><span class="sxs-lookup"><span data-stu-id="cab65-181">You can also use attributes to control how your classes and properties are mapped to the database.</span></span> <span data-ttu-id="cab65-182">Předpokládejme, že jste použili název `FirstMidName` pole jméno a příjmení, protože pole může obsahovat také prostřední jméno.</span><span class="sxs-lookup"><span data-stu-id="cab65-182">Suppose you had used the name `FirstMidName` for the first-name field because the field might also contain a middle name.</span></span> <span data-ttu-id="cab65-183">Ale chcete, aby byl sloupec databáze pojmenován `FirstName`, protože uživatelé, kteří budou psát ad hoc dotazy na databázi, jsou zvyklí na tento název.</span><span class="sxs-lookup"><span data-stu-id="cab65-183">But you want the database column to be named `FirstName`, because users who will be writing ad-hoc queries against the database are accustomed to that name.</span></span> <span data-ttu-id="cab65-184">Chcete-li toto mapování provést, můžete použít `Column` atribut.</span><span class="sxs-lookup"><span data-stu-id="cab65-184">To make this mapping, you can use the `Column` attribute.</span></span>

<span data-ttu-id="cab65-185">Atribut určuje, že při vytvoření databáze bude název `FirstName`sloupce `Student` tabulky, která je `FirstMidName` namapována na vlastnost. `Column`</span><span class="sxs-lookup"><span data-stu-id="cab65-185">The `Column` attribute specifies that when the database is created, the column of the `Student` table that maps to the `FirstMidName` property will be named `FirstName`.</span></span> <span data-ttu-id="cab65-186">Jinými slovy, pokud kód odkazuje na `Student.FirstMidName`, data budou pocházet z nebo aktualizována `FirstName` ve sloupci `Student` tabulky.</span><span class="sxs-lookup"><span data-stu-id="cab65-186">In other words, when your code refers to `Student.FirstMidName`, the data will come from or be updated in the `FirstName` column of the `Student` table.</span></span> <span data-ttu-id="cab65-187">Pokud názvy sloupců nezadáte, budou mít stejný název jako název vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="cab65-187">If you don't specify column names, they are given the same name as the property name.</span></span>

<span data-ttu-id="cab65-188">V souboru *student.cs* přidejte `using` příkaz pro [System. ComponentModel. DataAnnotations. Schema](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.aspx) a přidejte do `FirstMidName` vlastnosti atribut název sloupce, jak je znázorněno v následujícím zvýrazněném kódu:</span><span class="sxs-lookup"><span data-stu-id="cab65-188">In the *Student.cs* file, add a `using` statement for [System.ComponentModel.DataAnnotations.Schema](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.aspx) and add the column name attribute to the `FirstMidName` property, as shown in the following highlighted code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample5.cs?highlight=4,14)]

<span data-ttu-id="cab65-189">Přidání [atributu Column](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) změní model, který SchoolContext zálohuje, takže se neshoduje s databází.</span><span class="sxs-lookup"><span data-stu-id="cab65-189">The addition of the [Column attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) changes the model backing the SchoolContext, so it won't match the database.</span></span> <span data-ttu-id="cab65-190">Zadáním následujících příkazů v PMC vytvořte další migraci:</span><span class="sxs-lookup"><span data-stu-id="cab65-190">Enter the following commands in the PMC to create another migration:</span></span>

[!code-console[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample6.cmd)]

<span data-ttu-id="cab65-191">V **Průzkumník serveru**otevřete Návrhář tabulky *student* dvojitým kliknutím na tabulku *student* .</span><span class="sxs-lookup"><span data-stu-id="cab65-191">In **Server Explorer**, open the *Student* table designer by double-clicking the *Student* table.</span></span>

<span data-ttu-id="cab65-192">Následující obrázek ukazuje původní název sloupce, protože byl předtím, než jste použili první dvě migrace.</span><span class="sxs-lookup"><span data-stu-id="cab65-192">The following image shows the original column name as it was before you applied the first two migrations.</span></span> <span data-ttu-id="cab65-193">Kromě názvu sloupce, ze kterého se mění `FirstMidName` z `FirstName`na, se dva sloupce s názvy změnily z `MAX` délky na 50 znaků.</span><span class="sxs-lookup"><span data-stu-id="cab65-193">In addition to the column name changing from `FirstMidName` to `FirstName`, the two name columns have changed from `MAX` length to 50 characters.</span></span>

![](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image5.png)

<span data-ttu-id="cab65-194">Změny v mapování databáze můžete také provést pomocí [rozhraní API Fluent](https://msdn.microsoft.com/data/jj591617), jak vidíte později v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="cab65-194">You can also make database mapping changes using the [Fluent API](https://msdn.microsoft.com/data/jj591617), as you'll see later in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="cab65-195">Pokud se pokusíte kompilovat před dokončením vytváření všech tříd entit v následujících oddílech, může dojít k chybám kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="cab65-195">If you try to compile before you finish creating all of the entity classes in the following sections, you might get compiler errors.</span></span>

## <a name="update-student-entity"></a><span data-ttu-id="cab65-196">Aktualizovat entitu studenta</span><span class="sxs-lookup"><span data-stu-id="cab65-196">Update Student entity</span></span>

<span data-ttu-id="cab65-197">V *Models\Student.cs*nahraďte kód, který jste přidali dříve, pomocí následujícího kódu.</span><span class="sxs-lookup"><span data-stu-id="cab65-197">In *Models\Student.cs*, replace the code you added earlier with the following code.</span></span> <span data-ttu-id="cab65-198">Změny jsou zvýrazněné.</span><span class="sxs-lookup"><span data-stu-id="cab65-198">The changes are highlighted.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample7.cs?highlight=11,13,15,18,22,25-32)]

### <a name="the-required-attribute"></a><span data-ttu-id="cab65-199">Požadovaný atribut</span><span class="sxs-lookup"><span data-stu-id="cab65-199">The Required Attribute</span></span>

<span data-ttu-id="cab65-200">[Požadovaný atribut](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) nastaví název vlastnosti povinná pole.</span><span class="sxs-lookup"><span data-stu-id="cab65-200">The [Required attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) makes the name properties required fields.</span></span> <span data-ttu-id="cab65-201">`Required attribute` Není nutné pro typy hodnot, jako jsou DateTime, int, Double a float.</span><span class="sxs-lookup"><span data-stu-id="cab65-201">The `Required attribute` is not needed for value types such as DateTime, int, double, and float.</span></span> <span data-ttu-id="cab65-202">Hodnotovým typům nelze přiřadit hodnotu null, takže jsou z hlediska jejich podstaty považována za povinná pole.</span><span class="sxs-lookup"><span data-stu-id="cab65-202">Value types cannot be assigned a null value, so they are inherently treated as required fields.</span></span> 

<span data-ttu-id="cab65-203">Atribut musí být použit s `MinimumLength` pro `MinimumLength` vymáhání. `Required`</span><span class="sxs-lookup"><span data-stu-id="cab65-203">The `Required` attribute must be used with `MinimumLength` for the `MinimumLength` to be enforced.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample8.cs?highlight=2)]

<span data-ttu-id="cab65-204">`MinimumLength`a `Required` umožňují, aby prázdné znaky splňovaly ověření.</span><span class="sxs-lookup"><span data-stu-id="cab65-204">`MinimumLength` and `Required` allow whitespace to satisfy the validation.</span></span> <span data-ttu-id="cab65-205">`RegularExpression` Použijte atribut pro úplné řízení přes řetězec.</span><span class="sxs-lookup"><span data-stu-id="cab65-205">Use the `RegularExpression` attribute for full controll over the string.</span></span>

### <a name="the-display-attribute"></a><span data-ttu-id="cab65-206">Atribut zobrazení</span><span class="sxs-lookup"><span data-stu-id="cab65-206">The Display Attribute</span></span>

<span data-ttu-id="cab65-207">`Display` Atribut určuje, že titulek pro textová pole by měl být "jméno", "příjmení", "celé jméno" a "datum zápisu" místo názvu vlastnosti v každé instanci (což nemá mezeru dělená slova).</span><span class="sxs-lookup"><span data-stu-id="cab65-207">The `Display` attribute specifies that the caption for the text boxes should be "First Name", "Last Name", "Full Name", and "Enrollment Date" instead of the property name in each instance (which has no space dividing the words).</span></span>

### <a name="the-fullname-calculated-property"></a><span data-ttu-id="cab65-208">Vypočítaná vlastnost FullName</span><span class="sxs-lookup"><span data-stu-id="cab65-208">The FullName Calculated Property</span></span>

<span data-ttu-id="cab65-209">`FullName`je vypočtená vlastnost, která vrací hodnotu, která je vytvořena zřetězením dvou dalších vlastností.</span><span class="sxs-lookup"><span data-stu-id="cab65-209">`FullName` is a calculated property that returns a value that's created by concatenating two other properties.</span></span> <span data-ttu-id="cab65-210">Proto má pouze `get` přistupující objekt a v databázi nebude `FullName` vygenerován žádný sloupec.</span><span class="sxs-lookup"><span data-stu-id="cab65-210">Therefore it has only a `get` accessor, and no `FullName` column will be generated in the database.</span></span>

## <a name="create-instructor-entity"></a><span data-ttu-id="cab65-211">Vytvořit entitu instruktora</span><span class="sxs-lookup"><span data-stu-id="cab65-211">Create Instructor entity</span></span>

<span data-ttu-id="cab65-212">Vytvořte *Models\Instructor.cs*a nahraďte kód šablony následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cab65-212">Create *Models\Instructor.cs*, replacing the template code with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample9.cs)]

<span data-ttu-id="cab65-213">Všimněte si, že některé vlastnosti jsou stejné v `Student` entitách a `Instructor` .</span><span class="sxs-lookup"><span data-stu-id="cab65-213">Notice that several properties are the same in the `Student` and `Instructor` entities.</span></span> <span data-ttu-id="cab65-214">V kurzu [Implementace dědičnosti](implementing-inheritance-with-the-entity-framework-in-an-asp-net-mvc-application.md) níže v této sérii můžete tento kód Refaktorovat, aby se vyloučila redundance.</span><span class="sxs-lookup"><span data-stu-id="cab65-214">In the [Implementing Inheritance](implementing-inheritance-with-the-entity-framework-in-an-asp-net-mvc-application.md) tutorial later in this series, you'll refactor this code to eliminate the redundancy.</span></span>

<span data-ttu-id="cab65-215">Můžete umístit více atributů na jeden řádek, takže můžete také napsat třídu instruktora následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="cab65-215">You can put multiple attributes on one line, so you could also write the instructor class as follows:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample10.cs)]

### <a name="the-courses-and-officeassignment-navigation-properties"></a><span data-ttu-id="cab65-216">Kurzy a navigační vlastnosti OfficeAssignment</span><span class="sxs-lookup"><span data-stu-id="cab65-216">The Courses and OfficeAssignment Navigation Properties</span></span>

<span data-ttu-id="cab65-217">Vlastnosti `Courses` a`OfficeAssignment` jsou navigační vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="cab65-217">The `Courses` and `OfficeAssignment` properties are navigation properties.</span></span> <span data-ttu-id="cab65-218">Jak bylo vysvětleno dříve, jsou obvykle definovány jako [virtuální](https://msdn.microsoft.com/library/9fkccyh4(v=vs.110).aspx) , aby mohli využívat funkci Entity Framework s názvem [opožděné načítání](https://msdn.microsoft.com/magazine/hh205756.aspx).</span><span class="sxs-lookup"><span data-stu-id="cab65-218">As was explained earlier, they are typically defined as [virtual](https://msdn.microsoft.com/library/9fkccyh4(v=vs.110).aspx) so that they can take advantage of an Entity Framework feature called [lazy loading](https://msdn.microsoft.com/magazine/hh205756.aspx).</span></span> <span data-ttu-id="cab65-219">Kromě toho, pokud navigační vlastnost může obsahovat více entit, její typ musí implementovat rozhraní [ICollection&lt;T&gt; ](https://msdn.microsoft.com/library/92t2ye13.aspx) .</span><span class="sxs-lookup"><span data-stu-id="cab65-219">In addition, if a navigation property can hold multiple entities, its type must implement the [ICollection&lt;T&gt;](https://msdn.microsoft.com/library/92t2ye13.aspx) Interface.</span></span> <span data-ttu-id="cab65-220">Například objekt [IList&lt;t&gt; ](https://msdn.microsoft.com/library/5y536ey6.aspx) má nárok, ale [ne&lt;IEnumerable&gt; t](https://msdn.microsoft.com/library/9eekhta0.aspx) , protože `IEnumerable<T>` neimplementuje metodu [Add](https://msdn.microsoft.com/library/63ywd54z.aspx).</span><span class="sxs-lookup"><span data-stu-id="cab65-220">For example [IList&lt;T&gt;](https://msdn.microsoft.com/library/5y536ey6.aspx) qualifies but not [IEnumerable&lt;T&gt;](https://msdn.microsoft.com/library/9eekhta0.aspx) because `IEnumerable<T>` doesn't implement [Add](https://msdn.microsoft.com/library/63ywd54z.aspx).</span></span>

<span data-ttu-id="cab65-221">Instruktor může naučit libovolný počet kurzů, takže `Courses` je definován jako `Course` kolekce entit.</span><span class="sxs-lookup"><span data-stu-id="cab65-221">An instructor can teach any number of courses, so `Courses` is defined as a collection of `Course` entities.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample11.cs)]

<span data-ttu-id="cab65-222">Naše obchodní pravidla mají za stát, že instruktor může mít jenom jednu kancelář, `OfficeAssignment` takže se definuje jako jediná `OfficeAssignment` entita (která může být `null` , pokud není přiřazená žádná kancelář).</span><span class="sxs-lookup"><span data-stu-id="cab65-222">Our business rules state an instructor can only have at most one office, so `OfficeAssignment` is defined as a single `OfficeAssignment` entity (which may be `null` if no office is assigned).</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample12.cs)]

## <a name="create-officeassignment-entity"></a><span data-ttu-id="cab65-223">Vytvořit entitu OfficeAssignment</span><span class="sxs-lookup"><span data-stu-id="cab65-223">Create OfficeAssignment entity</span></span>

<span data-ttu-id="cab65-224">Vytvořte *Models\OfficeAssignment.cs* pomocí následujícího kódu:</span><span class="sxs-lookup"><span data-stu-id="cab65-224">Create *Models\OfficeAssignment.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample13.cs)]

<span data-ttu-id="cab65-225">Sestavte projekt, který uloží změny a ověří, že jste neudělali žádné chyby kopírování a vložení, které může kompilátor zachytit.</span><span class="sxs-lookup"><span data-stu-id="cab65-225">Build the project, which saves your changes and verifies you haven't made any copy and paste errors the compiler can catch.</span></span>

### <a name="the-key-attribute"></a><span data-ttu-id="cab65-226">Klíčový atribut</span><span class="sxs-lookup"><span data-stu-id="cab65-226">The Key Attribute</span></span>

<span data-ttu-id="cab65-227">Mezi `Instructor` `OfficeAssignment` entitami a entitami je vztah 1:1 nebo jedna hodnota.</span><span class="sxs-lookup"><span data-stu-id="cab65-227">There's a one-to-zero-or-one relationship between the `Instructor` and the `OfficeAssignment` entities.</span></span> <span data-ttu-id="cab65-228">Přiřazení kanceláře existuje jenom ve vztahu k instruktorovi, ke kterému je přiřazená, a proto jeho primární klíč je také cizímu klíči k `Instructor` entitě.</span><span class="sxs-lookup"><span data-stu-id="cab65-228">An office assignment only exists in relation to the instructor it's assigned to, and therefore its primary key is also its foreign key to the `Instructor` entity.</span></span> <span data-ttu-id="cab65-229">Entity Framework ale nemůžou automaticky rozpoznat `InstructorID` jako primární klíč této entity, protože její název `ID` nedodržuje konvence pojmenování názvů nebo *ClassName* `ID` .</span><span class="sxs-lookup"><span data-stu-id="cab65-229">But the Entity Framework can't automatically recognize `InstructorID` as the primary key of this entity because its name doesn't follow the `ID` or *classname* `ID` naming convention.</span></span> <span data-ttu-id="cab65-230">`Key` Proto atribut slouží k identifikaci jako klíč:</span><span class="sxs-lookup"><span data-stu-id="cab65-230">Therefore, the `Key` attribute is used to identify it as the key:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample14.cs)]

<span data-ttu-id="cab65-231">Atribut lze použít také v `Key` případě, že má entita svůj vlastní primární klíč, ale chcete vlastnost pojmenovat jinou než `classnameID` nebo `ID`.</span><span class="sxs-lookup"><span data-stu-id="cab65-231">You can also use the `Key` attribute if the entity does have its own primary key but you want to name the property something different than `classnameID` or `ID`.</span></span> <span data-ttu-id="cab65-232">Ve výchozím nastavení EF považuje klíč za generovaný nedatabází, protože sloupec je určen pro identifikaci vztahu.</span><span class="sxs-lookup"><span data-stu-id="cab65-232">By default EF treats the key as non-database-generated because the column is for an identifying relationship.</span></span>

### <a name="the-foreignkey-attribute"></a><span data-ttu-id="cab65-233">Atribut klíč ForeignKey</span><span class="sxs-lookup"><span data-stu-id="cab65-233">The ForeignKey Attribute</span></span>

<span data-ttu-id="cab65-234">Pokud existuje relace 1:1 nebo 1:1 nebo relace 1:1 mezi dvěma entitami (například mezi `OfficeAssignment` a `Instructor`), EF nemůže pracovat, který konec vztahu je objekt zabezpečení a který je závislý na konci.</span><span class="sxs-lookup"><span data-stu-id="cab65-234">When there is a one-to-zero-or-one relationship or a one-to-one relationship between two entities (such as between `OfficeAssignment` and `Instructor`), EF can't work out which end of the relationship is the principal and which end is dependent.</span></span> <span data-ttu-id="cab65-235">Relace 1:1 mají v každé třídě k druhé třídě referenční navigační vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cab65-235">One-to-one relationships have a reference navigation property in each class to the other class.</span></span> <span data-ttu-id="cab65-236">[Atribut klíč ForeignKey](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx) lze použít pro závislou třídu k vytvoření relace.</span><span class="sxs-lookup"><span data-stu-id="cab65-236">The [ForeignKey Attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx) can be applied to the dependent class to establish the relationship.</span></span> <span data-ttu-id="cab65-237">Vynecháte-li [atribut klíč ForeignKey](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx), při pokusu o vytvoření migrace se zobrazí následující chyba:</span><span class="sxs-lookup"><span data-stu-id="cab65-237">If you omit the [ForeignKey Attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx), you get the following error when you try to create the migration:</span></span>

<span data-ttu-id="cab65-238">*Nelze určit hlavní konec přidružení mezi typy ' ContosoUniversity. Models. OfficeAssignment ' a ' ContosoUniversity. Models. Instructor '. Hlavní konec tohoto přidružení musí být explicitně nakonfigurovaný buď pomocí rozhraní API Fluent, nebo datových poznámek.*</span><span class="sxs-lookup"><span data-stu-id="cab65-238">*Unable to determine the principal end of an association between the types 'ContosoUniversity.Models.OfficeAssignment' and 'ContosoUniversity.Models.Instructor'. The principal end of this association must be explicitly configured using either the relationship fluent API or data annotations.*</span></span>

<span data-ttu-id="cab65-239">Později v tomto kurzu uvidíte, jak nakonfigurovat tento vztah pomocí rozhraní Fluent API.</span><span class="sxs-lookup"><span data-stu-id="cab65-239">Later in the tutorial you'll see how to configure this relationship with the fluent API.</span></span>

### <a name="the-instructor-navigation-property"></a><span data-ttu-id="cab65-240">Navigační vlastnost instruktora</span><span class="sxs-lookup"><span data-stu-id="cab65-240">The Instructor Navigation Property</span></span>

<span data-ttu-id="cab65-241">Entita má vlastnost navigace s `OfficeAssignment` možnou hodnotou null (protože instruktor nemusí mít `OfficeAssignment` přiřazený Office) a entita má vlastnost navigace, která nemůže mít `Instructor` hodnotu null (protože přiřazení kanceláře nemůže `Instructor` existuje bez instruktora – `InstructorID` není možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="cab65-241">The `Instructor` entity has a nullable `OfficeAssignment` navigation property (because an instructor might not have an office assignment), and the `OfficeAssignment` entity has a non-nullable `Instructor` navigation property (because an office assignment can't exist without an instructor -- `InstructorID` is non-nullable).</span></span> <span data-ttu-id="cab65-242">Pokud má `OfficeAssignment` entita související entitu, bude mít Každá entita odkaz na druhou vlastnost v její navigační vlastnosti. `Instructor`</span><span class="sxs-lookup"><span data-stu-id="cab65-242">When an `Instructor` entity has a related `OfficeAssignment` entity, each entity will have a reference to the other one in its navigation property.</span></span>

<span data-ttu-id="cab65-243">Můžete umístit `[Required]` atribut do navigační vlastnosti instruktora a určit tak, že se musí jednat o související instruktor, ale nemusíte to dělat, protože cizí klíč InstructorID (což je také klíč k této tabulce) nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="cab65-243">You could put a `[Required]` attribute on the Instructor navigation property to specify that there must be a related instructor, but you don't have to do that because the InstructorID foreign key (which is also the key to this table) is non-nullable.</span></span>

## <a name="modify-the-course-entity"></a><span data-ttu-id="cab65-244">Úprava entity kurzu</span><span class="sxs-lookup"><span data-stu-id="cab65-244">Modify the Course entity</span></span>

<span data-ttu-id="cab65-245">V *Models\Course.cs*nahraďte kód, který jste přidali dříve, pomocí následujícího kódu:</span><span class="sxs-lookup"><span data-stu-id="cab65-245">In *Models\Course.cs*, replace the code you added earlier with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample15.cs)]

<span data-ttu-id="cab65-246">Entita kurzu má vlastnost `DepartmentID` cizího klíče, která odkazuje na související `Department` `Department` entitu a má vlastnost navigace.</span><span class="sxs-lookup"><span data-stu-id="cab65-246">The course entity has a foreign key property `DepartmentID` which points to the related `Department` entity and it has a `Department` navigation property.</span></span> <span data-ttu-id="cab65-247">Entity Framework nevyžaduje, abyste do datového modelu přidali vlastnost cizího klíče, když máte vlastnost navigace pro související entitu.</span><span class="sxs-lookup"><span data-stu-id="cab65-247">The Entity Framework doesn't require you to add a foreign key property to your data model when you have a navigation property for a related entity.</span></span> <span data-ttu-id="cab65-248">EF v databázi automaticky vytvoří cizí klíče bez ohledu na to, kde jsou potřeba.</span><span class="sxs-lookup"><span data-stu-id="cab65-248">EF automatically creates foreign keys in the database wherever they are needed.</span></span> <span data-ttu-id="cab65-249">Ale při používání cizího klíče v datovém modelu může být aktualizace jednodušší a efektivnější.</span><span class="sxs-lookup"><span data-stu-id="cab65-249">But having the foreign key in the data model can make updates simpler and more efficient.</span></span> <span data-ttu-id="cab65-250">Když například načtete entitu kurzu, která se má upravit, `Department` má entita hodnotu null, pokud ji nenačtete, takže když aktualizujete entitu kurzu, budete muset nejprve `Department` načíst entitu.</span><span class="sxs-lookup"><span data-stu-id="cab65-250">For example, when you fetch a course entity to edit, the `Department` entity is null if you don't load it, so when you update the course entity, you would have to first fetch the `Department` entity.</span></span> <span data-ttu-id="cab65-251">Pokud je v datovém `DepartmentID` modelu zahrnutá vlastnost cizího klíče, nemusíte před aktualizací `Department` entitu načítat.</span><span class="sxs-lookup"><span data-stu-id="cab65-251">When the foreign key property `DepartmentID` is included in the data model, you don't need to fetch the `Department` entity before you update.</span></span>

### <a name="the-databasegenerated-attribute"></a><span data-ttu-id="cab65-252">Atribut DatabaseGenerated</span><span class="sxs-lookup"><span data-stu-id="cab65-252">The DatabaseGenerated Attribute</span></span>

<span data-ttu-id="cab65-253">[Atribut DatabaseGenerated](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedattribute.aspx) s `CourseID` parametrem [none](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedoption(v=vs.110).aspx) u vlastnosti určuje, že uživatel zadá hodnoty primárního klíče místo vygenerovaného databází.</span><span class="sxs-lookup"><span data-stu-id="cab65-253">The [DatabaseGenerated attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedattribute.aspx) with the [None](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedoption(v=vs.110).aspx) parameter on the `CourseID` property specifies that primary key values are provided by the user rather than generated by the database.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample16.cs)]

<span data-ttu-id="cab65-254">Ve výchozím nastavení Entity Framework předpokládá, že se hodnoty primárního klíče generují v databázi.</span><span class="sxs-lookup"><span data-stu-id="cab65-254">By default, the Entity Framework assumes that primary key values are generated by the database.</span></span> <span data-ttu-id="cab65-255">To je to, co chcete ve většině scénářů.</span><span class="sxs-lookup"><span data-stu-id="cab65-255">That's what you want in most scenarios.</span></span> <span data-ttu-id="cab65-256">U `Course` entit ale použijete uživatelem zadané číslo kurzu, jako je například řada 1000, pro jedno oddělení, 2000 Series pro jiné oddělení a tak dále.</span><span class="sxs-lookup"><span data-stu-id="cab65-256">However, for `Course` entities, you'll use a user-specified course number such as a 1000 series for one department, a 2000 series for another department, and so on.</span></span>

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="cab65-257">Vlastnosti cizích klíčů a navigace</span><span class="sxs-lookup"><span data-stu-id="cab65-257">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="cab65-258">Vlastnosti cizího klíče a navigační vlastnosti v `Course` entitě odráží následující vztahy:</span><span class="sxs-lookup"><span data-stu-id="cab65-258">The foreign key properties and navigation properties in the `Course` entity reflect the following relationships:</span></span>

- <span data-ttu-id="cab65-259">Kurz se přiřadí jednomu oddělení, takže existuje `DepartmentID` cizí klíč `Department` a vlastnost navigace z výše uvedených důvodů.</span><span class="sxs-lookup"><span data-stu-id="cab65-259">A course is assigned to one department, so there's a `DepartmentID` foreign key and a `Department` navigation property for the reasons mentioned above.</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample17.cs)]
- <span data-ttu-id="cab65-260">V rámci kurzu může být zaregistrované několik studentů, takže `Enrollments` navigační vlastnost je kolekce:</span><span class="sxs-lookup"><span data-stu-id="cab65-260">A course can have any number of students enrolled in it, so the `Enrollments` navigation property is a collection:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample18.cs)]
- <span data-ttu-id="cab65-261">Kurz může být výukou více instruktorů, takže `Instructors` navigační vlastnost je kolekce:</span><span class="sxs-lookup"><span data-stu-id="cab65-261">A course may be taught by multiple instructors, so the `Instructors` navigation property is a collection:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample19.cs)]

## <a name="create-the-department-entity"></a><span data-ttu-id="cab65-262">Vytvořit entitu oddělení</span><span class="sxs-lookup"><span data-stu-id="cab65-262">Create the Department entity</span></span>

<span data-ttu-id="cab65-263">Vytvořte *Models\Department.cs* pomocí následujícího kódu:</span><span class="sxs-lookup"><span data-stu-id="cab65-263">Create *Models\Department.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample20.cs)]

### <a name="the-column-attribute"></a><span data-ttu-id="cab65-264">Atribut Column</span><span class="sxs-lookup"><span data-stu-id="cab65-264">The Column Attribute</span></span>

<span data-ttu-id="cab65-265">Dříve jste použili [atribut sloupce](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) pro změnu mapování názvu sloupce.</span><span class="sxs-lookup"><span data-stu-id="cab65-265">Earlier you used the [Column attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) to change column name mapping.</span></span> <span data-ttu-id="cab65-266">V kódu pro `Department` entitu `Column` se atribut používá ke změně mapování datových typů SQL tak, aby byl sloupec definován pomocí SQL Server [peněžního](https://msdn.microsoft.com/library/ms179882.aspx) typu v databázi:</span><span class="sxs-lookup"><span data-stu-id="cab65-266">In the code for the `Department` entity, the `Column` attribute is being used to change SQL data type mapping so that the column will be defined using the SQL Server [money](https://msdn.microsoft.com/library/ms179882.aspx) type in the database:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample21.cs)]

<span data-ttu-id="cab65-267">Mapování sloupce není obecně vyžadováno, protože Entity Framework obvykle zvolí vhodný SQL Server datový typ na základě typu CLR, který definujete pro vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cab65-267">Column mapping is generally not required, because the Entity Framework usually chooses the appropriate SQL Server data type based on the CLR type that you define for the property.</span></span> <span data-ttu-id="cab65-268">Typ CLR `decimal` se mapuje na typ SQL Server `decimal` .</span><span class="sxs-lookup"><span data-stu-id="cab65-268">The CLR `decimal` type maps to a SQL Server `decimal` type.</span></span> <span data-ttu-id="cab65-269">Ale v tomto případě víte, že se ve sloupci budou držet peněžní částky, a datový typ [Money](https://msdn.microsoft.com/library/ms179882.aspx) je pro to vhodnější.</span><span class="sxs-lookup"><span data-stu-id="cab65-269">But in this case you know that the column will be holding currency amounts, and the [money](https://msdn.microsoft.com/library/ms179882.aspx) data type is more appropriate for that.</span></span> <span data-ttu-id="cab65-270">Další informace o datových typech CLR a o tom, jak odpovídají SQL Server datovým typům, najdete v tématu [SqlClient pro Entity Framework](https://msdn.microsoft.com/library/bb896344.aspx).</span><span class="sxs-lookup"><span data-stu-id="cab65-270">For more information about CLR data types and how they match to SQL Server data types, see [SqlClient for Entity FrameworkTypes](https://msdn.microsoft.com/library/bb896344.aspx).</span></span>

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="cab65-271">Vlastnosti cizích klíčů a navigace</span><span class="sxs-lookup"><span data-stu-id="cab65-271">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="cab65-272">Vlastnosti cizího klíče a navigace odrážejí následující vztahy:</span><span class="sxs-lookup"><span data-stu-id="cab65-272">The foreign key and navigation properties reflect the following relationships:</span></span>

- <span data-ttu-id="cab65-273">Oddělení může nebo nemusí mít správce a správce je vždy instruktorem.</span><span class="sxs-lookup"><span data-stu-id="cab65-273">A department may or may not have an administrator, and an administrator is always an instructor.</span></span> <span data-ttu-id="cab65-274">Proto je `Instructor` `int` vlastnost zahrnutá jako cizí klíč k entitě a otazník se přidá za označení typu k označení vlastnosti jako Nullable. `InstructorID` Navigační vlastnost má název `Administrator` , ale `Instructor` obsahuje entitu:</span><span class="sxs-lookup"><span data-stu-id="cab65-274">Therefore the `InstructorID` property is included as the foreign key to the `Instructor` entity, and a question mark is added after the `int` type designation to mark the property as nullable.The navigation property is named `Administrator` but holds an `Instructor` entity:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample22.cs)]
- <span data-ttu-id="cab65-275">Oddělení může mít mnoho kurzů, takže `Courses` máte navigační vlastnost:</span><span class="sxs-lookup"><span data-stu-id="cab65-275">A department may have many courses, so there's a `Courses` navigation property:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample23.cs)]

  > [!NOTE]
  > <span data-ttu-id="cab65-276">Podle konvence Entity Framework umožňuje odstranit Kaskádové odstraňování cizích klíčů, které neumožňují hodnotu null, a pro relace m:n.</span><span class="sxs-lookup"><span data-stu-id="cab65-276">By convention, the Entity Framework enables cascade delete for non-nullable foreign keys and for many-to-many relationships.</span></span> <span data-ttu-id="cab65-277">Výsledkem může být cyklická kaskádová odstranění pravidel, která způsobí výjimku při pokusu o přidání migrace.</span><span class="sxs-lookup"><span data-stu-id="cab65-277">This can result in circular cascade delete rules, which will cause an exception when you try to add a migration.</span></span> <span data-ttu-id="cab65-278">Například pokud jste `Department.InstructorID` vlastnost nedefinovali jako možnou hodnotu null, zobrazí se následující zpráva o výjimce: "Referenční relace má za následek cyklický odkaz, který není povolený."</span><span class="sxs-lookup"><span data-stu-id="cab65-278">For example, if you didn't define the `Department.InstructorID` property as nullable, you'd get the following exception message: "The referential relationship will result in a cyclical reference that's not allowed."</span></span> <span data-ttu-id="cab65-279">Pokud vaše obchodní pravidla vyžadují `InstructorID` vlastnost neumožňující hodnotu null, bude nutné použít následující příkaz rozhraní API Fluent k zakázání kaskádových odstranění v relaci:</span><span class="sxs-lookup"><span data-stu-id="cab65-279">If your business rules required `InstructorID` property to be non-nullable, you would have to use the following fluent API statement to disable cascade delete on the relationship:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample24.cs)]

## <a name="modify-the-enrollment-entity"></a><span data-ttu-id="cab65-280">Úprava entity registrace</span><span class="sxs-lookup"><span data-stu-id="cab65-280">Modify the Enrollment entity</span></span>

 <span data-ttu-id="cab65-281">V *Models\Enrollment.cs*nahraďte kód, který jste přidali dříve, pomocí následujícího kódu.</span><span class="sxs-lookup"><span data-stu-id="cab65-281">In *Models\Enrollment.cs*, replace the code you added earlier with the following code</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample25.cs?highlight=1,15)]

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="cab65-282">Vlastnosti cizích klíčů a navigace</span><span class="sxs-lookup"><span data-stu-id="cab65-282">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="cab65-283">Vlastnosti cizího klíče a vlastnosti navigace odrážejí následující vztahy:</span><span class="sxs-lookup"><span data-stu-id="cab65-283">The foreign key properties and navigation properties reflect the following relationships:</span></span>

- <span data-ttu-id="cab65-284">Záznam zápisu je pro jeden kurz, takže existuje `CourseID` vlastnost cizího klíče `Course` a navigační vlastnost:</span><span class="sxs-lookup"><span data-stu-id="cab65-284">An enrollment record is for a single course, so there's a `CourseID` foreign key property and a `Course` navigation property:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample26.cs)]
- <span data-ttu-id="cab65-285">Záznam zápisu je určen pro jednoho studenta, takže existuje `StudentID` vlastnost cizího klíče `Student` a navigační vlastnost:</span><span class="sxs-lookup"><span data-stu-id="cab65-285">An enrollment record is for a single student, so there's a `StudentID` foreign key property and a `Student` navigation property:</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample27.cs)]

### <a name="many-to-many-relationships"></a><span data-ttu-id="cab65-286">Relace m:n</span><span class="sxs-lookup"><span data-stu-id="cab65-286">Many-to-Many Relationships</span></span>

<span data-ttu-id="cab65-287">Mezi `Student` entitami a `Course` je relace m:n a `Enrollment` entita funguje jako tabulka JOIN typu m:n *s datovou částí* v databázi.</span><span class="sxs-lookup"><span data-stu-id="cab65-287">There's a many-to-many relationship between the `Student` and `Course` entities, and the `Enrollment` entity functions as a many-to-many join table *with payload* in the database.</span></span> <span data-ttu-id="cab65-288">To znamená, že `Enrollment` tabulka obsahuje další data kromě cizích klíčů pro Spojené tabulky (v tomto případě primární klíč `Grade` a vlastnost).</span><span class="sxs-lookup"><span data-stu-id="cab65-288">This means that the `Enrollment` table contains additional data besides foreign keys for the joined tables (in this case, a primary key and a `Grade` property).</span></span>

<span data-ttu-id="cab65-289">Následující ilustrace znázorňuje, co tyto vztahy vypadají jako v diagramu entit.</span><span class="sxs-lookup"><span data-stu-id="cab65-289">The following illustration shows what these relationships look like in an entity diagram.</span></span> <span data-ttu-id="cab65-290">(Tento diagram byl vygenerován pomocí [Entity Framework nástrojů pro napájení](https://visualstudiogallery.msdn.microsoft.com/72a60b14-1581-4b9b-89f2-846072eff19d). Vytvoření diagramu není součástí kurzu, je zde pouze použito jako ilustrace.)</span><span class="sxs-lookup"><span data-stu-id="cab65-290">(This diagram was generated using the [Entity Framework Power Tools](https://visualstudiogallery.msdn.microsoft.com/72a60b14-1581-4b9b-89f2-846072eff19d); creating the diagram isn't part of the tutorial, it's just being used here as an illustration.)</span></span>

![Student – Course_many-to-many_relationship](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image12.png)

<span data-ttu-id="cab65-292">Každá čára relace má 1 na jednom konci a hvězdičku (\*) na druhé straně, která indikuje relaci 1: n.</span><span class="sxs-lookup"><span data-stu-id="cab65-292">Each relationship line has a 1 at one end and an asterisk (\*) at the other, indicating a one-to-many relationship.</span></span>

<span data-ttu-id="cab65-293">Pokud tabulka neobsahuje informace o třídě, musí obsahovat pouze dva cizí klíče `CourseID` a `StudentID`. `Enrollment`</span><span class="sxs-lookup"><span data-stu-id="cab65-293">If the `Enrollment` table didn't include grade information, it would only need to contain the two foreign keys `CourseID` and `StudentID`.</span></span> <span data-ttu-id="cab65-294">V takovém případě by to odpovídalo tabulkám JOIN m:n *bez datové části* (nebo *čisté joinové tabulky*) v databázi a nemuseli byste pro ni vytvořit třídu modelu.</span><span class="sxs-lookup"><span data-stu-id="cab65-294">In that case, it would correspond to a many-to-many join table *without payload* (or a *pure join table*) in the database, and you wouldn't have to create a model class for it at all.</span></span> <span data-ttu-id="cab65-295">Entity `Instructor` a`Course` mají tento druh relace m:n a jak vidíte, neexistuje mezi nimi žádná třída entit:</span><span class="sxs-lookup"><span data-stu-id="cab65-295">The `Instructor` and `Course` entities have that kind of many-to-many relationship, and as you can see, there is no entity class between them:</span></span>

![Instruktor – Course_many-do-many_relationship](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image13.png)

<span data-ttu-id="cab65-297">V databázi se ale vyžaduje tabulka JOIN, jak je znázorněno v následujícím databázovém diagramu:</span><span class="sxs-lookup"><span data-stu-id="cab65-297">A join table is required in the database, however, as shown in the following database diagram:</span></span>

![Instruktor – Course_many-do-many_relationship_tables](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image14.png)

<span data-ttu-id="cab65-299">Entity Framework automaticky vytvoří `CourseInstructor` tabulku a Vy ji přečtete a aktualizujete nepřímo tím, že si `Instructor.Courses` přečtete a `Course.Instructors` aktualizujete vlastnosti a navigace.</span><span class="sxs-lookup"><span data-stu-id="cab65-299">The Entity Framework automatically creates the `CourseInstructor` table, and you read and update it indirectly by reading and updating the `Instructor.Courses` and `Course.Instructors` navigation properties.</span></span>

## <a name="entity-relationship-diagram"></a><span data-ttu-id="cab65-300">Diagram vztahů mezi entitami</span><span class="sxs-lookup"><span data-stu-id="cab65-300">Entity relationship diagram</span></span>

<span data-ttu-id="cab65-301">Následující ilustrace znázorňuje diagram, který nástroje Entity Framework Power Tools vytvoří pro dokončený školní model.</span><span class="sxs-lookup"><span data-stu-id="cab65-301">The following illustration shows the diagram that the Entity Framework Power Tools create for the completed School model.</span></span>

![School_data_model_diagram](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image1.png)

<span data-ttu-id="cab65-303">Kromě řádků relace\* m:n ( \*a) a řádků \*relace 1:1 (1 – n) vidíte na řádku relace 1:1 (1 – 0.1) mezi `Instructor` a a `OfficeAssignment` , která je rovna. entity a řádek relace nula-nebo-n mezi entitami instruktora a oddělení (0.. \*1 do).</span><span class="sxs-lookup"><span data-stu-id="cab65-303">Besides the many-to-many relationship lines (\* to \*) and the one-to-many relationship lines (1 to \*), you can see here the one-to-zero-or-one relationship line (1 to 0..1) between the `Instructor` and `OfficeAssignment` entities and the zero-or-one-to-many relationship line (0..1 to \*) between the Instructor and Department entities.</span></span>

## <a name="add-code-to-database-context"></a><span data-ttu-id="cab65-304">Přidat kód do kontextu databáze</span><span class="sxs-lookup"><span data-stu-id="cab65-304">Add code to database context</span></span>

<span data-ttu-id="cab65-305">Dále přidáte nové entity do `SchoolContext` třídy a upravíte některé mapování pomocí volání [Fluent API](https://msdn.microsoft.com/data/jj591617) .</span><span class="sxs-lookup"><span data-stu-id="cab65-305">Next you'll add the new entities to the `SchoolContext` class and customize some of the mapping using [fluent API](https://msdn.microsoft.com/data/jj591617) calls.</span></span> <span data-ttu-id="cab65-306">Rozhraní API je "Fluent", protože se často používá k zřetězení řady volání metody do jednoho příkazu, jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="cab65-306">The API is "fluent" because it's often used by stringing a series of method calls together into a single statement, as in the following example:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample28.cs)]

<span data-ttu-id="cab65-307">V tomto kurzu použijete rozhraní Fluent API jenom pro mapování databáze, které nemůžete s atributy dělat.</span><span class="sxs-lookup"><span data-stu-id="cab65-307">In this tutorial you'll use the fluent API only for database mapping that you can't do with attributes.</span></span> <span data-ttu-id="cab65-308">Rozhraní Fluent API ale můžete použít i k určení většiny pravidel formátování, ověřování a mapování, která můžete provádět pomocí atributů.</span><span class="sxs-lookup"><span data-stu-id="cab65-308">However, you can also use the fluent API to specify most of the formatting, validation, and mapping rules that you can do by using attributes.</span></span> <span data-ttu-id="cab65-309">Některé atributy, `MinimumLength` jako například, se nedají použít s rozhraním API Fluent.</span><span class="sxs-lookup"><span data-stu-id="cab65-309">Some attributes such as `MinimumLength` can't be applied with the fluent API.</span></span> <span data-ttu-id="cab65-310">Jak bylo zmíněno `MinimumLength` dříve, nemění schéma, používá pouze pravidlo ověřování na straně klienta a serveru.</span><span class="sxs-lookup"><span data-stu-id="cab65-310">As mentioned previously, `MinimumLength` doesn't change the schema, it only applies a client and server side validation rule</span></span>

<span data-ttu-id="cab65-311">Někteří vývojáři dávají přednost použití rozhraní Fluent API, aby mohli zachovat třídy entit "vyčistit".</span><span class="sxs-lookup"><span data-stu-id="cab65-311">Some developers prefer to use the fluent API exclusively so that they can keep their entity classes "clean."</span></span> <span data-ttu-id="cab65-312">V případě potřeby můžete kombinovat atributy a rozhraní API Fluent a existuje několik úprav, které je možné provést jenom pomocí rozhraní Fluent API, ale obecně doporučujeme zvolit jednu z těchto dvou přístupů a použít ji konzistentně co nejvíce.</span><span class="sxs-lookup"><span data-stu-id="cab65-312">You can mix attributes and fluent API if you want, and there are a few customizations that can only be done by using fluent API, but in general the recommended practice is to choose one of these two approaches and use that consistently as much as possible.</span></span>

<span data-ttu-id="cab65-313">Chcete-li přidat nové entity do datového modelu a provést mapování databáze, které jste neprováděli pomocí atributů, nahraďte kód v *DAL\SchoolContext.cs* následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cab65-313">To add the new entities to the data model and perform database mapping that you didn't do by using attributes, replace the code in *DAL\SchoolContext.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample29.cs)]

<span data-ttu-id="cab65-314">V příkazu New v metodě [OnModelCreating](https://msdn.microsoft.com/library/system.data.entity.dbcontext.onmodelcreating(v=vs.103).aspx) se konfiguruje tabulka JOIN m:n:</span><span class="sxs-lookup"><span data-stu-id="cab65-314">The new statement in the [OnModelCreating](https://msdn.microsoft.com/library/system.data.entity.dbcontext.onmodelcreating(v=vs.103).aspx) method configures the many-to-many join table:</span></span>

- <span data-ttu-id="cab65-315">Pro vztah n:n mezi `Instructor` entitami a `Course` Určuje kód tabulku a názvy sloupců pro tabulku JOIN.</span><span class="sxs-lookup"><span data-stu-id="cab65-315">For the many-to-many relationship between the `Instructor` and `Course` entities, the code specifies the table and column names for the join table.</span></span> <span data-ttu-id="cab65-316">Code First může nakonfigurovat relaci m:n pro vás bez tohoto kódu, ale pokud ji nebudete volat, získáte výchozí názvy `InstructorInstructorID` `InstructorID` pro sloupec.</span><span class="sxs-lookup"><span data-stu-id="cab65-316">Code First can configure the many-to-many relationship for you without this code, but if you don't call it, you will get default names such as `InstructorInstructorID` for the `InstructorID` column.</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample30.cs)]

<span data-ttu-id="cab65-317">Následující kód poskytuje příklad, jak byste mohli použít rozhraní Fluent API namísto atributů k určení vztahu mezi `Instructor` entitami a: `OfficeAssignment`</span><span class="sxs-lookup"><span data-stu-id="cab65-317">The following code provides an example of how you could have used fluent API instead of attributes to specify the relationship between the `Instructor` and `OfficeAssignment` entities:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample31.cs)]

<span data-ttu-id="cab65-318">Informace o tom, co se děje s příkazy "Fluent API" na pozadí, najdete v blogovém příspěvku [rozhraní Fluent API](https://blogs.msdn.com/b/aspnetue/archive/2011/05/04/entity-framework-code-first-tutorial-supplement-what-is-going-on-in-a-fluent-api-call.aspx) .</span><span class="sxs-lookup"><span data-stu-id="cab65-318">For information about what "fluent API" statements are doing behind the scenes, see the [Fluent API](https://blogs.msdn.com/b/aspnetue/archive/2011/05/04/entity-framework-code-first-tutorial-supplement-what-is-going-on-in-a-fluent-api-call.aspx) blog post.</span></span>

## <a name="seed-database-with-test-data"></a><span data-ttu-id="cab65-319">Počáteční databáze s testovacími daty</span><span class="sxs-lookup"><span data-stu-id="cab65-319">Seed database with test data</span></span>

<span data-ttu-id="cab65-320">Nahraďte kód v souboru *Migrations\Configuration.cs* následujícím kódem, aby bylo možné poskytnout počáteční data pro nové entity, které jste vytvořili.</span><span class="sxs-lookup"><span data-stu-id="cab65-320">Replace the code in the *Migrations\Configuration.cs* file with the following code in order to provide seed data for the new entities you've created.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample32.cs)]

<span data-ttu-id="cab65-321">Jak jste viděli v prvním kurzu, většinu tohoto kódu jednoduše aktualizuje nebo vytvoří nové objekty entit a načte vzorová data do vlastností podle potřeby pro testování.</span><span class="sxs-lookup"><span data-stu-id="cab65-321">As you saw in the first tutorial, most of this code simply updates or creates new entity objects and loads sample data into properties as required for testing.</span></span> <span data-ttu-id="cab65-322">Všimněte si však, že `Course` je zpracována entita, která má vztah n:n `Instructor` s entitou:</span><span class="sxs-lookup"><span data-stu-id="cab65-322">However, notice how the `Course` entity, which has a many-to-many relationship with the `Instructor` entity, is handled:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample33.cs)]

<span data-ttu-id="cab65-323">Při vytváření `Course` objektu se pomocí kódu `Instructors` `Instructors = new List<Instructor>()`inicializuje vlastnost navigace jako prázdná kolekce.</span><span class="sxs-lookup"><span data-stu-id="cab65-323">When you create a `Course` object, you initialize the `Instructors` navigation property as an empty collection using the code `Instructors = new List<Instructor>()`.</span></span> <span data-ttu-id="cab65-324">Díky tomu je možné přidat `Instructor` entity, které se k tomuto `Course` vztahu vztahují, pomocí `Instructors.Add` metody.</span><span class="sxs-lookup"><span data-stu-id="cab65-324">This makes it possible to add `Instructor` entities that are related to this `Course` by using the `Instructors.Add` method.</span></span> <span data-ttu-id="cab65-325">Pokud jste nevytvořili prázdný seznam, nebudete moci přidat tyto relace, protože `Instructors` vlastnost by měla hodnotu null a nemůže by by `Add` měla být metoda.</span><span class="sxs-lookup"><span data-stu-id="cab65-325">If you didn't create an empty list, you wouldn't be able to add these relationships, because the `Instructors` property would be null and wouldn't have an `Add` method.</span></span> <span data-ttu-id="cab65-326">Můžete také přidat inicializaci seznamu do konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="cab65-326">You could also add the list initialization to the constructor.</span></span>

## <a name="add-a-migration"></a><span data-ttu-id="cab65-327">Přidání migrace</span><span class="sxs-lookup"><span data-stu-id="cab65-327">Add a migration</span></span>

<span data-ttu-id="cab65-328">Do PMC zadejte `add-migration` příkaz (ještě `update-database` příkaz neprovádějte):</span><span class="sxs-lookup"><span data-stu-id="cab65-328">From the PMC, enter the `add-migration` command (don't do the `update-database` command yet):</span></span>

`add-Migration ComplexDataModel`

<span data-ttu-id="cab65-329">Pokud jste se v tomto okamžiku `update-database` pokusili spustit příkaz (ještě to neuděláte), zobrazí se následující chyba:</span><span class="sxs-lookup"><span data-stu-id="cab65-329">If you tried to run the `update-database` command at this point (don't do it yet), you would get the following error:</span></span>

<span data-ttu-id="cab65-330">*Příkaz ALTER TABLE je v konfliktu s omezením cizího klíče "\_FK dbo. \_Dbo. Oddělení\_DepartmentID ". Ke konfliktu došlo v databázi "ContosoUniversity", tabulce "dbo". Oddělení ", sloupec" DepartmentID ".*</span><span class="sxs-lookup"><span data-stu-id="cab65-330">*The ALTER TABLE statement conflicted with the FOREIGN KEY constraint "FK\_dbo.Course\_dbo.Department\_DepartmentID". The conflict occurred in database "ContosoUniversity", table "dbo.Department", column 'DepartmentID'.*</span></span>

<span data-ttu-id="cab65-331">Při provádění migrace s existujícími daty je někdy potřeba vložit do databáze zástupná data, abyste splnili omezení cizího klíče. to je to, co je teď potřeba udělat.</span><span class="sxs-lookup"><span data-stu-id="cab65-331">Sometimes when you execute migrations with existing data, you need to insert stub data into the database to satisfy foreign key constraints, and that's what you have to do now.</span></span> <span data-ttu-id="cab65-332">Generovaný kód v metodě ComplexDataModel `Up` přidá do `Course` tabulky cizí klíč, který `DepartmentID` nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="cab65-332">The generated code in the ComplexDataModel `Up` method adds a non-nullable `DepartmentID` foreign key to the `Course` table.</span></span> <span data-ttu-id="cab65-333">Vzhledem k tomu `Course` `AddColumn` , že v tabulce jsou již řádky, když je spuštěn kód, operace se nezdaří, protože SQL Server neví, jaká hodnota má být vložena do sloupce, který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="cab65-333">Because there are already rows in the `Course` table when the code runs, the `AddColumn` operation will fail because SQL Server doesn't know what value to put in the column that can't be null.</span></span> <span data-ttu-id="cab65-334">Proto je nutné změnit kód tak, aby nový sloupec poskytoval výchozí hodnotu, a vytvořit oddělení zástupných procedur s názvem "Temp", které bude fungovat jako výchozí oddělení.</span><span class="sxs-lookup"><span data-stu-id="cab65-334">Therefore have to change the code to give the new column a default value, and create a stub department named "Temp" to act as the default department.</span></span> <span data-ttu-id="cab65-335">V důsledku toho budou existující `Course` řádky `Up` po spuštění metody v relaci k "dočasnému" oddělení.</span><span class="sxs-lookup"><span data-stu-id="cab65-335">As a result, existing `Course` rows will all be related to the "Temp" department after the `Up` method runs.</span></span> <span data-ttu-id="cab65-336">Můžete je spojit se správnými odděleními v `Seed` metodě.</span><span class="sxs-lookup"><span data-stu-id="cab65-336">You can relate them to the correct departments in the `Seed` method.</span></span>

<span data-ttu-id="cab65-337">Upravte soubor *ComplexDataModel.cs&gt;časového razítka\_* , zakomentujte řádek kódu, který do tabulky Course přidá sloupec DepartmentID, a přidejte následující zvýrazněný kód (řádek s komentářem je také &lt; zvýrazněno):</span><span class="sxs-lookup"><span data-stu-id="cab65-337">Edit the &lt;*timestamp&gt;\_ComplexDataModel.cs* file, comment out the line of code that adds the DepartmentID column to the Course table, and add the following highlighted code (the commented line is also highlighted):</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample34.cs?highlight=14-18)]

<span data-ttu-id="cab65-338">Při spuštění `Department` `Course` metody se do tabulky vloží řádky, které budou pro tyto nové `Department` řádky odkazovat na stávající řádky. `Seed`</span><span class="sxs-lookup"><span data-stu-id="cab65-338">When the `Seed` method runs, it will insert rows in the `Department` table and it will relate existing `Course` rows to those new `Department` rows.</span></span> <span data-ttu-id="cab65-339">Pokud jste v uživatelském rozhraní nepřidali žádné kurzy, nebudete už potřebovat "dočasné" oddělení nebo výchozí hodnotu `Course.DepartmentID` sloupce.</span><span class="sxs-lookup"><span data-stu-id="cab65-339">If you haven't added any courses in the UI, you would then no longer need the "Temp" department or the default value on the `Course.DepartmentID` column.</span></span> <span data-ttu-id="cab65-340">Chcete-li umožnit možnost, že někdo mohl přidat kurzy pomocí aplikace, měli byste také aktualizovat `Seed` kód metody, aby se zajistilo, že všechny `Course` řádky (nikoli pouze ty, které byly vloženy `Seed` pomocí předchozích spuštění metody). platné `DepartmentID` hodnoty před odebráním výchozí hodnoty ze sloupce a odstraněním "dočasného" oddělení.</span><span class="sxs-lookup"><span data-stu-id="cab65-340">To allow for the possibility that someone might have added courses by using the application, you'd also want to update the `Seed` method code to ensure that all `Course` rows (not just the ones inserted by earlier runs of the `Seed` method) have valid `DepartmentID` values before you remove the default value from the column and delete the "Temp" department.</span></span>

## <a name="update-the-database"></a><span data-ttu-id="cab65-341">Aktualizace databáze</span><span class="sxs-lookup"><span data-stu-id="cab65-341">Update the database</span></span>

<span data-ttu-id="cab65-342">Po dokončení úprav &lt;souboru *&gt;ComplexDataModel.cs časového razítka\_* spusťte migraci zadáním `update-database` příkazu v PMC.</span><span class="sxs-lookup"><span data-stu-id="cab65-342">After you have finished editing the &lt;*timestamp&gt;\_ComplexDataModel.cs* file, enter the `update-database` command in the PMC to execute the migration.</span></span>

[!code-powershell[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample35.ps1)]

> [!NOTE]
> <span data-ttu-id="cab65-343">Při migraci dat a provádění změn schématu je možné získat další chyby.</span><span class="sxs-lookup"><span data-stu-id="cab65-343">It's possible to get other errors when migrating data and making schema changes.</span></span> <span data-ttu-id="cab65-344">Pokud se zobrazí chyby migrace, které nemůžete vyřešit, můžete buď změnit název databáze v připojovacím řetězci, nebo databázi odstranit.</span><span class="sxs-lookup"><span data-stu-id="cab65-344">If you get migration errors you can't resolve, you can either change the database name in the connection string or delete the database.</span></span> <span data-ttu-id="cab65-345">Nejjednodušším přístupem je přejmenovat databázi v souboru *Web. config* .</span><span class="sxs-lookup"><span data-stu-id="cab65-345">The simplest approach is to rename the database in *Web.config* file.</span></span> <span data-ttu-id="cab65-346">Následující příklad ukazuje název změněný na CU\_test:</span><span class="sxs-lookup"><span data-stu-id="cab65-346">The following example shows the name changed to CU\_Test:</span></span>
>
> [!code-xml[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample36.xml?highlight=1)]
>
> <span data-ttu-id="cab65-347">V případě nové databáze nejsou k dispozici žádná data k migraci a `update-database` příkaz je mnohem větší, než může být dokončeno bez chyb.</span><span class="sxs-lookup"><span data-stu-id="cab65-347">With a new database, there is no data to migrate, and the `update-database` command is much more likely to complete without errors.</span></span> <span data-ttu-id="cab65-348">Pokyny k odstranění databáze naleznete v tématu [How to drop a Database from a Visual Studio 2012](http://romiller.com/2013/05/17/how-to-drop-a-database-from-visual-studio-2012/).</span><span class="sxs-lookup"><span data-stu-id="cab65-348">For instructions on how to delete the database, see [How to Drop a Database from Visual Studio 2012](http://romiller.com/2013/05/17/how-to-drop-a-database-from-visual-studio-2012/).</span></span>
>
> <span data-ttu-id="cab65-349">Pokud se to nepovede, můžete zkusit znovu inicializovat databázi zadáním následujícího příkazu v PMC:</span><span class="sxs-lookup"><span data-stu-id="cab65-349">If that fails, another thing you can try is re-initialize the database by entering the following command in the PMC:</span></span>
>
> `update-database -TargetMigration:0`

<span data-ttu-id="cab65-350">Otevřete databázi v **Průzkumník serveru** jako dříve a rozbalte uzel **tabulky** , abyste viděli, že se vytvořily všechny tabulky.</span><span class="sxs-lookup"><span data-stu-id="cab65-350">Open the database in **Server Explorer** as you did earlier, and expand the **Tables** node to see that all of the tables have been created.</span></span> <span data-ttu-id="cab65-351">(Pokud stále **Průzkumník serveru** otevřít z dřívějšího času, klikněte na tlačítko **aktualizovat** .)</span><span class="sxs-lookup"><span data-stu-id="cab65-351">(If you still have **Server Explorer** open from the earlier time, click the **Refresh** button.)</span></span>

![](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image16.png)

<span data-ttu-id="cab65-352">Pro `CourseInstructor` tabulku jste nevytvořili třídu modelu.</span><span class="sxs-lookup"><span data-stu-id="cab65-352">You didn't create a model class for the `CourseInstructor` table.</span></span> <span data-ttu-id="cab65-353">Jak bylo vysvětleno dříve, jedná se o spojení mezi `Instructor` entitami a `Course` pro relaci n:n.</span><span class="sxs-lookup"><span data-stu-id="cab65-353">As explained earlier, this is a join table for the many-to-many relationship between the `Instructor` and `Course` entities.</span></span>

<span data-ttu-id="cab65-354">Klikněte pravým tlačítkem `CourseInstructor` myši na tabulku a vyberte možnost **Zobrazit data tabulky** a ověřte, že se v ní nacházejí data v `Instructor` důsledku entit, které jste `Course.Instructors` přidali do vlastnosti navigace.</span><span class="sxs-lookup"><span data-stu-id="cab65-354">Right-click the `CourseInstructor` table and select **Show Table Data** to verify that it has data in it as a result of the `Instructor` entities you added to the `Course.Instructors` navigation property.</span></span>

![Table_data_in_CourseInstructor_table](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image17.png)

## <a name="get-the-code"></a><span data-ttu-id="cab65-356">Získat kód</span><span class="sxs-lookup"><span data-stu-id="cab65-356">Get the code</span></span>

[<span data-ttu-id="cab65-357">Stáhnout dokončený projekt</span><span class="sxs-lookup"><span data-stu-id="cab65-357">Download Completed Project</span></span>](https://webpifeed.blob.core.windows.net/webpifeed/Partners/ASP.NET%20MVC%20Application%20Using%20Entity%20Framework%20Code%20First.zip)

## <a name="additional-resources"></a><span data-ttu-id="cab65-358">Další zdroje</span><span class="sxs-lookup"><span data-stu-id="cab65-358">Additional resources</span></span>

<span data-ttu-id="cab65-359">Odkazy na další prostředky Entity Framework najdete v prostředcích, které jsou doporučeny pro [přístup k datům ASP.NET](../../../../whitepapers/aspnet-data-access-content-map.md).</span><span class="sxs-lookup"><span data-stu-id="cab65-359">Links to other Entity Framework resources can be found in the [ASP.NET Data Access - Recommended Resources](../../../../whitepapers/aspnet-data-access-content-map.md).</span></span>

## <a name="next-steps"></a><span data-ttu-id="cab65-360">Další postup</span><span class="sxs-lookup"><span data-stu-id="cab65-360">Next steps</span></span>

<span data-ttu-id="cab65-361">V tomto kurzu se naučíte:</span><span class="sxs-lookup"><span data-stu-id="cab65-361">In this tutorial, you:</span></span>

> [!div class="checklist"]
> * <span data-ttu-id="cab65-362">Přizpůsobení datového modelu</span><span class="sxs-lookup"><span data-stu-id="cab65-362">Customized the data model</span></span>
> * <span data-ttu-id="cab65-363">Aktualizovaná entita studenta</span><span class="sxs-lookup"><span data-stu-id="cab65-363">Updated Student entity</span></span>
> * <span data-ttu-id="cab65-364">Vytvořená entita instruktora</span><span class="sxs-lookup"><span data-stu-id="cab65-364">Created Instructor entity</span></span>
> * <span data-ttu-id="cab65-365">Vytvořila se entita OfficeAssignment</span><span class="sxs-lookup"><span data-stu-id="cab65-365">Created OfficeAssignment entity</span></span>
> * <span data-ttu-id="cab65-366">Změnila se entita kurzu.</span><span class="sxs-lookup"><span data-stu-id="cab65-366">Modified the Course entity</span></span>
> * <span data-ttu-id="cab65-367">Vytvořila se entita oddělení.</span><span class="sxs-lookup"><span data-stu-id="cab65-367">Created the Department entity</span></span>
> * <span data-ttu-id="cab65-368">Změnila se entita registrace.</span><span class="sxs-lookup"><span data-stu-id="cab65-368">Modified the Enrollment entity</span></span>
> * <span data-ttu-id="cab65-369">Byl přidán kód do kontextu databáze.</span><span class="sxs-lookup"><span data-stu-id="cab65-369">Added code to database context</span></span>
> * <span data-ttu-id="cab65-370">Dosazení databáze s testovacími daty</span><span class="sxs-lookup"><span data-stu-id="cab65-370">Seeded database with test data</span></span>
> * <span data-ttu-id="cab65-371">Přidání migrace</span><span class="sxs-lookup"><span data-stu-id="cab65-371">Added a migration</span></span>
> * <span data-ttu-id="cab65-372">Aktualizace databáze</span><span class="sxs-lookup"><span data-stu-id="cab65-372">Updated the database</span></span>

<span data-ttu-id="cab65-373">V dalším článku se dozvíte, jak číst a zobrazovat související data, která Entity Framework načíst do vlastností navigace.</span><span class="sxs-lookup"><span data-stu-id="cab65-373">Advance to the next article to learn how to read and display related data that the Entity Framework loads into navigation properties.</span></span>

> [!div class="nextstepaction"]
> [<span data-ttu-id="cab65-374">Čtení souvisejících dat</span><span class="sxs-lookup"><span data-stu-id="cab65-374">Read related data</span></span>](reading-related-data-with-the-entity-framework-in-an-asp-net-mvc-application.md)
