---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Představení webových stránek ASP.NET – základy formulářů HTML | Microsoft Docs
author: Rick-Anderson
description: V tomto kurzu se dozvíte základní informace o tom, jak vytvořit vstupní formulář a jak zpracovat vstup uživatele, když používáte webové stránky ASP.NET (Razor). A teď...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574282"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="42013-104">Představení webových stránek ASP.NET – základy formulářů HTML</span><span class="sxs-lookup"><span data-stu-id="42013-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="42013-105">tím, že [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="42013-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="42013-106">V tomto kurzu se dozvíte základní informace o tom, jak vytvořit vstupní formulář a jak zpracovat vstup uživatele, když používáte webové stránky ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="42013-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="42013-107">A teď, když máte databázi, budete používat své dovednosti v podobě, aby uživatelé mohli v databázi najít konkrétní filmy.</span><span class="sxs-lookup"><span data-stu-id="42013-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="42013-108">Předpokládá se, že jste dokončili řadu prostřednictvím [úvodního zobrazení dat pomocí webových stránek ASP.NET](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="42013-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="42013-109">Naučíte se:</span><span class="sxs-lookup"><span data-stu-id="42013-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="42013-110">Vytvoření formuláře pomocí standardních prvků jazyka HTML.</span><span class="sxs-lookup"><span data-stu-id="42013-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="42013-111">Způsob čtení vstupu uživatele ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="42013-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="42013-112">Postup vytvoření dotazu SQL, který selektivně načte data pomocí hledaného termínu, který uživatel dodá.</span><span class="sxs-lookup"><span data-stu-id="42013-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="42013-113">Jak mít pole na stránce "pamatovat", co uživatel zadal.</span><span class="sxs-lookup"><span data-stu-id="42013-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="42013-114">Popsané funkce a technologie:</span><span class="sxs-lookup"><span data-stu-id="42013-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="42013-115">Objekt `Request`</span><span class="sxs-lookup"><span data-stu-id="42013-115">The `Request` object.</span></span>
> - <span data-ttu-id="42013-116">Klauzule `Where` SQL.</span><span class="sxs-lookup"><span data-stu-id="42013-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="42013-117">Co sestavíte</span><span class="sxs-lookup"><span data-stu-id="42013-117">What You'll Build</span></span>

<span data-ttu-id="42013-118">V předchozím kurzu jste vytvořili databázi, přidali do ní data a pak jste pomocí pomocníka `WebGrid` zobrazili data.</span><span class="sxs-lookup"><span data-stu-id="42013-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="42013-119">V tomto kurzu přidáte vyhledávací pole, které vám umožní najít filmy konkrétního žánru nebo jeho titul, který bude obsahovat jakékoli slovo, které zadáte.</span><span class="sxs-lookup"><span data-stu-id="42013-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="42013-120">(Například budete moci najít všechny filmy, jejichž Žánr je "Action" nebo jehož název obsahuje "Harry" nebo "Adventure.")</span><span class="sxs-lookup"><span data-stu-id="42013-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="42013-121">Po dokončení tohoto kurzu budete mít stránku, jako je tato:</span><span class="sxs-lookup"><span data-stu-id="42013-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Stránka filmy s hledáním žánru a nadpisu](form-basics/_static/image1.png)

<span data-ttu-id="42013-123">Část stránky výpisu je stejná jako v posledním kurzu &mdash; mřížce.</span><span class="sxs-lookup"><span data-stu-id="42013-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="42013-124">Rozdílem bude, že v mřížce se zobrazí pouze filmy, které jste hledali.</span><span class="sxs-lookup"><span data-stu-id="42013-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="42013-125">O formulářích HTML</span><span class="sxs-lookup"><span data-stu-id="42013-125">About HTML Forms</span></span>

<span data-ttu-id="42013-126">(Pokud máte zkušenosti s vytvářením formulářů HTML a rozdílem mezi `GET` a `POST`, můžete tuto část přeskočit.)</span><span class="sxs-lookup"><span data-stu-id="42013-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="42013-127">Formulář obsahuje prvky uživatelského vstupu &mdash; textových polí, tlačítek, přepínačů, zaškrtávacích políček, rozevíracích seznamech atd.</span><span class="sxs-lookup"><span data-stu-id="42013-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="42013-128">Uživatelé tyto ovládací prvky vyplní nebo provedou výběry a pak formulář odešlou kliknutím na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="42013-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="42013-129">Základní syntaxe HTML formuláře je znázorněna v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="42013-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="42013-130">Když se tento kód spustí na stránce, vytvoří jednoduchý tvar, který bude vypadat jako na následujícím obrázku:</span><span class="sxs-lookup"><span data-stu-id="42013-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Základní HTML formulář jako vykreslený v prohlížeči](form-basics/_static/image2.png)

<span data-ttu-id="42013-132">Element `<form>` obklopuje elementy HTML, které se mají odeslat.</span><span class="sxs-lookup"><span data-stu-id="42013-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="42013-133">(Snadná chyba je, aby bylo možné přidat prvky na stránku, ale pak je zapomenout, aby byly vloženy do prvku `<form>`.</span><span class="sxs-lookup"><span data-stu-id="42013-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="42013-134">V takovém případě není nic odesláno.) Atribut `method` oznamuje prohlížeči, jak odeslat vstup uživatele.</span><span class="sxs-lookup"><span data-stu-id="42013-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="42013-135">Tuto hodnotu nastavíte na `post`, pokud provádíte aktualizaci na serveru nebo `get`, pokud načítáte data ze serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="42013-136">**Zabezpečení operací GET, POST a HTTP**</span><span class="sxs-lookup"><span data-stu-id="42013-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="42013-137">HTTP, protokol, který prohlížeče a servery používají k výměně informací, je výjimečně jednoduché v základních operacích.</span><span class="sxs-lookup"><span data-stu-id="42013-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="42013-138">Prohlížeče při vytváření požadavků na servery používají jenom několik příkazů.</span><span class="sxs-lookup"><span data-stu-id="42013-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="42013-139">Při psaní kódu pro web je užitečné pochopit tyto příkazy a způsob jejich použití v prohlížeči a na serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="42013-140">A daleko nejčastěji používané operace jsou tyto:</span><span class="sxs-lookup"><span data-stu-id="42013-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="42013-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="42013-141">`GET`.</span></span> <span data-ttu-id="42013-142">Prohlížeč používá tuto operaci k načtení něčeho ze serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="42013-143">Když například zadáte adresu URL do prohlížeče, prohlížeč provede operaci `GET` a vyžádá si požadovanou stránku.</span><span class="sxs-lookup"><span data-stu-id="42013-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="42013-144">Pokud stránka obsahuje grafiku, prohlížeč pro získání imagí provede další operace `GET`.</span><span class="sxs-lookup"><span data-stu-id="42013-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="42013-145">Pokud `GET` operace musí předat informace na server, informace se předávají jako součást adresy URL v řetězci dotazu.</span><span class="sxs-lookup"><span data-stu-id="42013-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="42013-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="42013-146">`POST`.</span></span> <span data-ttu-id="42013-147">Prohlížeč pošle žádost o `POST`, aby mohla odesílat data, která se mají přidat nebo změnit na serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="42013-148">Například příkaz `POST` slouží k vytváření záznamů v databázi nebo změně existujících.</span><span class="sxs-lookup"><span data-stu-id="42013-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="42013-149">Ve většině případů, když vyplníte formulář a kliknete na tlačítko Odeslat, prohlížeč provede operaci `POST`.</span><span class="sxs-lookup"><span data-stu-id="42013-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="42013-150">V operaci `POST` se data předávaná na server nachází v těle stránky.</span><span class="sxs-lookup"><span data-stu-id="42013-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="42013-151">Důležitým rozdílem mezi těmito operacemi je, že operace `GET` není třeba měnit cokoli na serveru, nebo ji vložit do trochu abstraktního způsobu, `GET` operace nevede ke změně stavu na serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="42013-152">Operaci `GET` můžete provádět u stejných prostředků tolikrát, kolikrát chcete, a tyto prostředky se nezmění.</span><span class="sxs-lookup"><span data-stu-id="42013-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="42013-153">(Operace `GET` je často označována jako "bezpečná" nebo "použití technického období", je *idempotentní*.) Naproti tomu `POST` požadavek na server mění při každém provedení operace něco na serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="42013-154">Toto rozlišení vám může ukázat dva příklady.</span><span class="sxs-lookup"><span data-stu-id="42013-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="42013-155">Když provedete vyhledávání pomocí modulu, jako je například Bing nebo Google, vyplníte formulář, který se skládá z jednoho textového pole, a kliknete na tlačítko Hledat.</span><span class="sxs-lookup"><span data-stu-id="42013-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="42013-156">Prohlížeč provede operaci `GET` s hodnotou, kterou jste zadali do pole předaného jako součást adresy URL.</span><span class="sxs-lookup"><span data-stu-id="42013-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="42013-157">Použití operace `GET` pro tento typ formuláře je přesné, protože operace vyhledávání nemění žádné prostředky na serveru, pouze načítá informace.</span><span class="sxs-lookup"><span data-stu-id="42013-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="42013-158">Nyní zvažte proces řazení do online režimu.</span><span class="sxs-lookup"><span data-stu-id="42013-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="42013-159">Vyplníte podrobnosti objednávky a potom kliknete na tlačítko Odeslat.</span><span class="sxs-lookup"><span data-stu-id="42013-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="42013-160">Tato operace bude `POST` požadavek, protože výsledkem operace budou změny na serveru, jako je například nový záznam objednávky, změna informací o účtu a pravděpodobně mnoho dalších změn.</span><span class="sxs-lookup"><span data-stu-id="42013-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="42013-161">Na rozdíl od operace `GET` nemůžete žádost o `POST` opakovat – Pokud jste to provedli, vygenerujete novou objednávku na serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="42013-162">(V takových případech se na webech často varuje, že nekliknete na tlačítko Odeslat více než jednou, nebo zakážete tlačítko Odeslat, aby nedošlo k nechtěnému opakovanému odeslání formuláře.)</span><span class="sxs-lookup"><span data-stu-id="42013-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="42013-163">V průběhu tohoto kurzu budete pracovat s formuláři HTML pomocí operace `GET` a operace `POST`.</span><span class="sxs-lookup"><span data-stu-id="42013-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="42013-164">V každém z těchto případů vybereme vysvětlení, proč je to ten, co používáte.</span><span class="sxs-lookup"><span data-stu-id="42013-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="42013-165">(Další informace o příkazech HTTP naleznete v článku [definice metod](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) na webu W3C.)</span><span class="sxs-lookup"><span data-stu-id="42013-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="42013-166">Většina prvků uživatelského vstupu je `<input>` prvky HTML.</span><span class="sxs-lookup"><span data-stu-id="42013-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="42013-167">Vypadají jako `<input type="type" name="name">,`, kde *typ* určuje druh ovládacího prvku pro zadávání uživatelského vstupu, který chcete.</span><span class="sxs-lookup"><span data-stu-id="42013-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="42013-168">Tyto prvky jsou společné:</span><span class="sxs-lookup"><span data-stu-id="42013-168">These elements are the common ones:</span></span>

- <span data-ttu-id="42013-169">Textové pole: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="42013-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="42013-170">Zaškrtávací políčko: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="42013-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="42013-171">Přepínač: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="42013-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="42013-172">Tlačítko: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="42013-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="42013-173">Tlačítko Odeslat: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="42013-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="42013-174">Pomocí elementu `<textarea>` lze také vytvořit víceřádkové textové pole a prvek `<select>` k vytvoření rozevíracího seznamu nebo rolovacího seznamu.</span><span class="sxs-lookup"><span data-stu-id="42013-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="42013-175">(Další informace o prvcích formuláře HTML najdete v tématu [formuláře HTML a vstup](http://www.w3schools.com/html/html_forms.asp) na webu w3schools.)</span><span class="sxs-lookup"><span data-stu-id="42013-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="42013-176">Atribut `name` je velmi důležitý, protože název představuje způsob, jakým se získá hodnota elementu později, jak uvidíte za chvíli.</span><span class="sxs-lookup"><span data-stu-id="42013-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="42013-177">Zajímavá část je to, co jste vy, vývojář stránky dělat se vstupem uživatele.</span><span class="sxs-lookup"><span data-stu-id="42013-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="42013-178">K těmto prvkům není přidruženo žádné integrované chování.</span><span class="sxs-lookup"><span data-stu-id="42013-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="42013-179">Místo toho musíte získat hodnoty, které uživatel zadal nebo vybral, a s nimi pracovat.</span><span class="sxs-lookup"><span data-stu-id="42013-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="42013-180">To je to, co se v tomto kurzu naučíte.</span><span class="sxs-lookup"><span data-stu-id="42013-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="42013-181">**HTML5 a vstupní formuláře**</span><span class="sxs-lookup"><span data-stu-id="42013-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="42013-182">Jak je to možné, HTML je v přechodu a nejnovější verze (HTML5) obsahuje podporu pro více intuitivních způsobů, jak mohou uživatelé zadávat informace.</span><span class="sxs-lookup"><span data-stu-id="42013-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="42013-183">Například v HTML5 můžete (vývojář stránky) sdělit stránku, na kterou má uživatel zadat datum.</span><span class="sxs-lookup"><span data-stu-id="42013-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="42013-184">Prohlížeč pak může automaticky zobrazit kalendář, aniž by uživatel musel zadat datum ručně.</span><span class="sxs-lookup"><span data-stu-id="42013-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="42013-185">HTML5 je však novinkou a ve všech prohlížečích ještě není podporováno.</span><span class="sxs-lookup"><span data-stu-id="42013-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="42013-186">Webové stránky ASP.NET podporují vstup HTML5 do rozsahu, který je v prohlížeči uživatele.</span><span class="sxs-lookup"><span data-stu-id="42013-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="42013-187">Představu o nových atributech `<input>` elementu v HTML5 najdete v tématu [atribut typu input&gt; &lt;HTML](http://www.w3schools.com/html/html_form_input_types.asp) na webu w3schools.</span><span class="sxs-lookup"><span data-stu-id="42013-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="42013-188">Vytvoření formuláře</span><span class="sxs-lookup"><span data-stu-id="42013-188">Creating the Form</span></span>

<span data-ttu-id="42013-189">Ve WebMatrixu v pracovním prostoru **soubory** otevřete stránku *filmy. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42013-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="42013-190">Za uzavírací značku `</h1>` a před otevírací `<div>`ovou značku `grid.GetHtml` volání přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="42013-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="42013-191">Tento kód vytvoří formulář, který obsahuje textové pole s názvem `searchGenre` a tlačítko Odeslat.</span><span class="sxs-lookup"><span data-stu-id="42013-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="42013-192">Textové pole a tlačítko Odeslat jsou uzavřeny v elementu `<form>`, jehož atribut `method` je nastaven na `get`.</span><span class="sxs-lookup"><span data-stu-id="42013-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="42013-193">(Nezapomeňte, že pokud textové pole nevložíte a kliknete na tlačítko Odeslat uvnitř prvku `<form>`, nebude se po kliknutí na tlačítko odesílat žádná data.) Tady můžete použít `GET` sloveso, protože vytváříte formulář, který na serveru neprovede žádné změny – výsledkem je hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="42013-194">(V předchozím kurzu jste použili metodu `post`, což je způsob, jakým odešlete změny na server.</span><span class="sxs-lookup"><span data-stu-id="42013-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="42013-195">Uvidíte, že v dalším kurzu znovu.)</span><span class="sxs-lookup"><span data-stu-id="42013-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="42013-196">Spusťte stránku.</span><span class="sxs-lookup"><span data-stu-id="42013-196">Run the page.</span></span> <span data-ttu-id="42013-197">I když jste nedefinovali žádné chování formuláře, vidíte, co vypadá takto:</span><span class="sxs-lookup"><span data-stu-id="42013-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Stránka filmy s vyhledávacím polem pro Žánr](form-basics/_static/image3.png)

<span data-ttu-id="42013-199">Do textového pole zadejte hodnotu, například "komedie".</span><span class="sxs-lookup"><span data-stu-id="42013-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="42013-200">Pak klikněte na **Hledat Žánr**.</span><span class="sxs-lookup"><span data-stu-id="42013-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="42013-201">Poznamenejte si adresu URL stránky.</span><span class="sxs-lookup"><span data-stu-id="42013-201">Take note of the URL of the page.</span></span> <span data-ttu-id="42013-202">Vzhledem k tomu, že jste nastavili atribut `method` elementu `<form>` na hodnotu `get`, hodnota, kterou jste zadali, je teď součástí řetězce dotazu v adrese URL, třeba takto:</span><span class="sxs-lookup"><span data-stu-id="42013-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="42013-203">Čtení hodnot formuláře</span><span class="sxs-lookup"><span data-stu-id="42013-203">Reading Form Values</span></span>

<span data-ttu-id="42013-204">Stránka již obsahuje nějaký kód, který získá data databáze a zobrazí výsledky v mřížce.</span><span class="sxs-lookup"><span data-stu-id="42013-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="42013-205">Nyní je nutné přidat kód, který přečte hodnotu textového pole, abyste mohli spustit dotaz SQL, který obsahuje hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="42013-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="42013-206">Vzhledem k tomu, že jste nastavili metodu formuláře na `get`, můžete si přečíst hodnotu, která byla zadána do textového pole, pomocí kódu podobného následujícímu:</span><span class="sxs-lookup"><span data-stu-id="42013-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="42013-207">Objekt `Request.QueryString` (vlastnost `QueryString` objektu `Request`) obsahuje hodnoty prvků, které byly odeslány jako součást operace `GET`.</span><span class="sxs-lookup"><span data-stu-id="42013-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="42013-208">Vlastnost `Request.QueryString` obsahuje *kolekci* (seznam) hodnot, které jsou odeslány ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="42013-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="42013-209">Chcete-li získat konkrétní hodnotu, zadejte název prvku, který chcete.</span><span class="sxs-lookup"><span data-stu-id="42013-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="42013-210">To je důvod, proč musíte mít atribut `name` u elementu `<input>` (`searchTerm`), který vytvoří textové pole.</span><span class="sxs-lookup"><span data-stu-id="42013-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="42013-211">(Další informace o objektu `Request` naleznete v [postranním panelu](#BKMK_TheRequestObject) později.)</span><span class="sxs-lookup"><span data-stu-id="42013-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="42013-212">Je dostatečně snadné číst hodnotu textového pole.</span><span class="sxs-lookup"><span data-stu-id="42013-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="42013-213">Pokud ale uživatel v textovém poli vůbec nezadal cokoli, ale na tlačítko **Hledat** , můžete ho ignorovat, protože není k dispozici žádné hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="42013-214">Následující kód je příklad, který ukazuje, jak implementovat tyto podmínky.</span><span class="sxs-lookup"><span data-stu-id="42013-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="42013-215">(Tento kód ještě nemusíte přidávat. za chvilku to provedete.)</span><span class="sxs-lookup"><span data-stu-id="42013-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="42013-216">Test rozdělí tímto způsobem:</span><span class="sxs-lookup"><span data-stu-id="42013-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="42013-217">Získat hodnotu `Request.QueryString["searchGenre"]`, konkrétně hodnotu, která byla zadána do prvku `<input>` s názvem `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="42013-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="42013-218">Zjistěte, jestli je prázdný pomocí metody `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="42013-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="42013-219">Tato metoda je standardní způsob, jak určit, zda něco (například element Form) obsahuje hodnotu.</span><span class="sxs-lookup"><span data-stu-id="42013-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="42013-220">Ale opravdu se zajímáte jenom v případě, že *není* prázdné, proto...</span><span class="sxs-lookup"><span data-stu-id="42013-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="42013-221">Před testem `IsEmpty` přidejte operátor `!`.</span><span class="sxs-lookup"><span data-stu-id="42013-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="42013-222">(Operátor `!` znamená logický NOT).</span><span class="sxs-lookup"><span data-stu-id="42013-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="42013-223">V prostém jazyce English je celý `if` podmínka přeložena do následujícího: *Pokud není element searchGenre formuláře prázdný, pak...*</span><span class="sxs-lookup"><span data-stu-id="42013-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="42013-224">Tento blok nastaví fázi pro vytvoření dotazu, který používá hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="42013-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="42013-225">Provedete to v další části.</span><span class="sxs-lookup"><span data-stu-id="42013-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="42013-226">**Objekt žádosti**</span><span class="sxs-lookup"><span data-stu-id="42013-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="42013-227">Objekt `Request` obsahuje všechny informace, které prohlížeč posílá do vaší aplikace, když je vyžádána nebo odeslána stránka.</span><span class="sxs-lookup"><span data-stu-id="42013-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="42013-228">Tento objekt obsahuje všechny informace, které uživatel poskytuje, jako jsou například hodnoty textového pole nebo soubor k nahrání.</span><span class="sxs-lookup"><span data-stu-id="42013-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="42013-229">Zahrnuje také všechny druhy dalších informací, jako jsou soubory cookie, hodnoty v řetězci dotazu adresy URL (pokud existují), cestu k souboru na spuštěné stránce, typ prohlížeče, který uživatel používá, seznam jazyků, které jsou nastaveny v prohlížeči. a mnoho dalšího.</span><span class="sxs-lookup"><span data-stu-id="42013-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="42013-230">Objekt `Request` je *kolekce* (seznam) hodnot.</span><span class="sxs-lookup"><span data-stu-id="42013-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="42013-231">Z kolekce získáte jednotlivou hodnotu zadáním jejího názvu:</span><span class="sxs-lookup"><span data-stu-id="42013-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="42013-232">Objekt `Request` ve skutečnosti zveřejňuje několik dílčích sad.</span><span class="sxs-lookup"><span data-stu-id="42013-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="42013-233">Příklad:</span><span class="sxs-lookup"><span data-stu-id="42013-233">For example:</span></span>
> 
> - <span data-ttu-id="42013-234">`Request.Form` poskytuje hodnoty z prvků v odeslaném `<form>` elementu, pokud je požadavek `POST` požadavek.</span><span class="sxs-lookup"><span data-stu-id="42013-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="42013-235">`Request.QueryString` poskytuje pouze hodnoty v řetězci dotazu adresy URL.</span><span class="sxs-lookup"><span data-stu-id="42013-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="42013-236">(V adrese URL, jako je například `http://mysite/myapp/page?searchGenre=action&page=2`, je oddíl `?searchGenre=action&page=2` v adrese URL řetězec dotazu.)</span><span class="sxs-lookup"><span data-stu-id="42013-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="42013-237">kolekce `Request.Cookies` umožňuje přístup k souborům cookie, které prohlížeč odeslal.</span><span class="sxs-lookup"><span data-stu-id="42013-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="42013-238">Pokud chcete získat hodnotu, o kterou víte, že je v odeslaném formuláři, můžete použít `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="42013-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="42013-239">Alternativně můžete použít konkrétnější verze `Request.Form["name"]` (pro `POST` požadavků) nebo `Request.QueryString["name"]` (pro `GET` požadavky).</span><span class="sxs-lookup"><span data-stu-id="42013-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="42013-240">Samozřejmě je *název* položky, kterou chcete získat.</span><span class="sxs-lookup"><span data-stu-id="42013-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="42013-241">Název položky, kterou chcete získat, musí být jedinečný v rámci kolekce, kterou používáte.</span><span class="sxs-lookup"><span data-stu-id="42013-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="42013-242">To je důvod, proč objekt `Request` poskytuje podmnožiny jako `Request.Form` a `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="42013-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="42013-243">Předpokládejme, že stránka obsahuje element formuláře s názvem `userName` a obsahuje *také* soubor cookie s názvem `userName`.</span><span class="sxs-lookup"><span data-stu-id="42013-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="42013-244">Pokud získáte `Request["userName"]`, je nejednoznačné, zda chcete mít hodnotu formuláře nebo soubor cookie.</span><span class="sxs-lookup"><span data-stu-id="42013-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="42013-245">Pokud však získáte `Request.Form["userName"]` nebo `Request.Cookie["userName"]`, budete přesně o tom, která hodnota má být získána.</span><span class="sxs-lookup"><span data-stu-id="42013-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="42013-246">Je dobrým zvykem určit a využít podmnožinu `Request`, na kterou vás zajímáte, jako je `Request.Form` nebo `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="42013-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="42013-247">Pro jednoduché stránky, které v tomto kurzu vytváříte, to pravděpodobně nevede k žádným rozdílům.</span><span class="sxs-lookup"><span data-stu-id="42013-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="42013-248">Při vytváření složitějších stránek ale pomocí explicitní verze `Request.Form` nebo `Request.QueryString` může pomoci vyhnout se problémům, které mohou nastat, když stránka obsahuje formulář (nebo více formulářů), soubory cookie, hodnoty řetězce dotazu a tak dále.</span><span class="sxs-lookup"><span data-stu-id="42013-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="42013-249">Vytvoření dotazu pomocí hledaného termínu</span><span class="sxs-lookup"><span data-stu-id="42013-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="42013-250">Když teď víte, jak získat hledaný termín, který zadal uživatel, můžete vytvořit dotaz, který ho používá.</span><span class="sxs-lookup"><span data-stu-id="42013-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="42013-251">Nezapomeňte, že pokud chcete všechny položky videa získat z databáze, budete používat dotaz SQL, který vypadá jako tento příkaz:</span><span class="sxs-lookup"><span data-stu-id="42013-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="42013-252">Chcete-li získat pouze některé filmy, je nutné použít dotaz, který obsahuje klauzuli `Where`.</span><span class="sxs-lookup"><span data-stu-id="42013-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="42013-253">Tato klauzule vám umožní nastavit podmínku, na které se v dotazu vrátí řádky.</span><span class="sxs-lookup"><span data-stu-id="42013-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="42013-254">Tady je příklad:</span><span class="sxs-lookup"><span data-stu-id="42013-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="42013-255">Základní formát je `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="42013-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="42013-256">V závislosti na tom, co hledáte, můžete použít různé operátory kromě `=`, jako je `>` (větší než), `<` (menší než), `<>` (nerovná se), `<=` (menší než nebo rovno) atd.</span><span class="sxs-lookup"><span data-stu-id="42013-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="42013-257">V případě, že se zajímáte, příkazy SQL nerozlišují velká a malá písmena &mdash; `SELECT` jsou stejné jako `Select` (nebo dokonce `select`).</span><span class="sxs-lookup"><span data-stu-id="42013-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="42013-258">Lidé ale často nemají klíčová slova v příkazu SQL, jako je `SELECT` a `WHERE`, aby se usnadnilo jejich čtení.</span><span class="sxs-lookup"><span data-stu-id="42013-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="42013-259">Předání hledaného termínu jako parametru</span><span class="sxs-lookup"><span data-stu-id="42013-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="42013-260">Hledání konkrétního žánru je dostatečně snadné (`WHERE Genre = 'Action'`), ale chcete být schopni vyhledat jakýkoliv Žánr, který uživatel zadal.</span><span class="sxs-lookup"><span data-stu-id="42013-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="42013-261">Provedete to tak, že vytvoříte dotaz SQL, který obsahuje zástupný symbol pro hledanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="42013-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="42013-262">Bude vypadat podobně jako tento příkaz:</span><span class="sxs-lookup"><span data-stu-id="42013-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="42013-263">Zástupný symbol je `@` znak následovaný nulou.</span><span class="sxs-lookup"><span data-stu-id="42013-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="42013-264">Jak můžete odhadnout, dotaz může obsahovat několik zástupných symbolů a má název `@0`, `@1`, `@2`atd.</span><span class="sxs-lookup"><span data-stu-id="42013-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="42013-265">Chcete-li nastavit dotaz a skutečně ho předat jako hodnotu, použijte následující kód:</span><span class="sxs-lookup"><span data-stu-id="42013-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="42013-266">Tento kód je podobný tomu, co jste už provedli k zobrazení dat v mřížce.</span><span class="sxs-lookup"><span data-stu-id="42013-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="42013-267">Jedinými rozdíly jsou:</span><span class="sxs-lookup"><span data-stu-id="42013-267">The only differences are:</span></span>

- <span data-ttu-id="42013-268">Dotaz obsahuje zástupný symbol (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="42013-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="42013-269">Dotaz je vložen do proměnné (`selectCommand`); předtím jste předali dotaz přímo metodě `db.Query`.</span><span class="sxs-lookup"><span data-stu-id="42013-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="42013-270">Při volání metody `db.Query` předáte dotaz i hodnotu, která se má použít pro zástupný text.</span><span class="sxs-lookup"><span data-stu-id="42013-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="42013-271">(Pokud má dotaz více zástupných symbolů, měli byste je předat jako samostatné hodnoty do metody.)</span><span class="sxs-lookup"><span data-stu-id="42013-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="42013-272">Pokud všechny tyto prvky vložíte dohromady, získáte následující kód:</span><span class="sxs-lookup"><span data-stu-id="42013-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="42013-273">**Významná!**</span><span class="sxs-lookup"><span data-stu-id="42013-273">**Important!**</span></span> <span data-ttu-id="42013-274">Použití zástupných symbolů (například `@0`) k předávání hodnot příkazu SQL je *mimořádně důležité* pro zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="42013-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="42013-275">Způsob, jakým se tady vidíte, představuje zástupné symboly pro data proměnných, je jediným způsobem, jak byste měli vytvořit příkazy SQL.</span><span class="sxs-lookup"><span data-stu-id="42013-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="42013-276">Příkaz SQL nikdy neseskupte vložením (zřetězení) literálového textu a hodnot, které obdržíte od uživatele.</span><span class="sxs-lookup"><span data-stu-id="42013-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="42013-277">Zřetězení vstupu uživatele do příkazu SQL otevře váš web s *útokem na injektáže SQL* , kde uživatel se zlými úmysly odešle hodnoty na stránku, která by proznala vaši databázi.</span><span class="sxs-lookup"><span data-stu-id="42013-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="42013-278">(Další informace si můžete přečíst v článku [SQL vložení](https://msdn.microsoft.com/library/ms161953.aspx) webu MSDN.)</span><span class="sxs-lookup"><span data-stu-id="42013-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="42013-279">Aktualizace stránky filmů pomocí vyhledávacího kódu</span><span class="sxs-lookup"><span data-stu-id="42013-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="42013-280">Nyní můžete aktualizovat kód v souboru *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42013-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="42013-281">Chcete-li začít, nahraďte kód v bloku kódu v horní části stránky tímto kódem:</span><span class="sxs-lookup"><span data-stu-id="42013-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="42013-282">Rozdíl je v tom, že jste dotaz umístili do proměnné `selectCommand`, kterou předáte `db.Query` později.</span><span class="sxs-lookup"><span data-stu-id="42013-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="42013-283">Vložení příkazu SQL do proměnné umožňuje změnit příkaz, který provedete k provedení hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="42013-284">Odebrali jste také tyto dva řádky, které později vrátíte:</span><span class="sxs-lookup"><span data-stu-id="42013-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="42013-285">Dotaz ještě nechcete spustit (to znamená volání `db.Query`) a nechcete inicializovat pomocnou nápovědu `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="42013-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="42013-286">Tyto věci provedete po zjištění, který příkaz SQL musí být spuštěn.</span><span class="sxs-lookup"><span data-stu-id="42013-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="42013-287">Po provedení tohoto přepsaného bloku můžete přidat novou logiku pro zpracování vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="42013-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="42013-288">Dokončený kód bude vypadat jako následující.</span><span class="sxs-lookup"><span data-stu-id="42013-288">The completed code will look like the following.</span></span> <span data-ttu-id="42013-289">Aktualizujte kód na stránce tak, aby odpovídal následujícímu příkladu:</span><span class="sxs-lookup"><span data-stu-id="42013-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="42013-290">Stránka teď funguje podobně.</span><span class="sxs-lookup"><span data-stu-id="42013-290">The page now works like this.</span></span> <span data-ttu-id="42013-291">Při každém spuštění stránky kód otevře databázi a proměnná `selectCommand` je nastavena na příkaz jazyka SQL, který získá všechny záznamy z tabulky `Movies`.</span><span class="sxs-lookup"><span data-stu-id="42013-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="42013-292">Kód také inicializuje proměnnou `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42013-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="42013-293">Nicméně pokud aktuální požadavek obsahuje hodnotu pro `searchGenre` element, kód nastaví `selectCommand` na jiný dotaz – konkrétně na jeden, který obsahuje klauzuli `Where` pro hledání žánru.</span><span class="sxs-lookup"><span data-stu-id="42013-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="42013-294">Také nastavuje `searchTerm` pro pole hledání, které bylo předáno (což může být nic).</span><span class="sxs-lookup"><span data-stu-id="42013-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="42013-295">Bez ohledu na to, který příkaz SQL je v `selectCommand`, kód pak zavolá `db.Query` a spustí dotaz, který předává, ať už je v `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42013-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="42013-296">Pokud neexistuje žádný `searchTerm`, nezáleží na tom, protože v takovém případě neexistuje parametr pro předání hodnoty do `selectCommand` přesto.</span><span class="sxs-lookup"><span data-stu-id="42013-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="42013-297">Nakonec kód inicializuje pomoc `WebGrid` pomocí výsledků dotazu, stejně jako předtím.</span><span class="sxs-lookup"><span data-stu-id="42013-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="42013-298">Můžete vidět, že vložením příkazu jazyka SQL a hledaného termínu do proměnných jste přidali flexibilitu kódu.</span><span class="sxs-lookup"><span data-stu-id="42013-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="42013-299">Jak vidíte později v tomto kurzu, můžete použít tuto základní architekturu a nechat přidat logiku pro různé typy hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="42013-300">Testování funkce hledání podle žánru</span><span class="sxs-lookup"><span data-stu-id="42013-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="42013-301">Ve WebMatrixu spusťte stránku *filmy. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42013-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="42013-302">Zobrazí se stránka s textovým polem pro Žánr.</span><span class="sxs-lookup"><span data-stu-id="42013-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="42013-303">Zadejte Žánr, který jste zadali pro jeden ze záznamů testu, a pak klikněte na **Hledat**.</span><span class="sxs-lookup"><span data-stu-id="42013-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="42013-304">Tentokrát se zobrazí seznam pouze filmů, které odpovídají danému žánru:</span><span class="sxs-lookup"><span data-stu-id="42013-304">This time you see a listing of just the movies that match that genre:</span></span>

![Seznam filmů po hledání žánru](form-basics/_static/image4.png)

<span data-ttu-id="42013-306">Zadejte jiný Žánr a znovu proveďte hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-306">Enter a different genre and search again.</span></span> <span data-ttu-id="42013-307">Zkuste zadat Žánr pomocí všech malých a velkých písmen, abyste viděli, že hledání nerozlišuje malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="42013-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="42013-308">"Pamatuje", co uživatel zadal</span><span class="sxs-lookup"><span data-stu-id="42013-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="42013-309">Možná jste si všimli, že po zadání žánru a kliknutí na **Search žánru**jste viděli seznam pro tento žánr.</span><span class="sxs-lookup"><span data-stu-id="42013-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="42013-310">Textové pole hledání je ale prázdné &mdash; jinými slovy, nepamatuje si, co jste zadali.</span><span class="sxs-lookup"><span data-stu-id="42013-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="42013-311">Je důležité pochopit, proč k tomuto chování dochází.</span><span class="sxs-lookup"><span data-stu-id="42013-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="42013-312">Když odešlete stránku, prohlížeč odešle požadavek webovému serveru.</span><span class="sxs-lookup"><span data-stu-id="42013-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="42013-313">Když ASP.NET Získá požadavek, vytvoří značku – novou instanci stránky, spustí kód v něm a pak znovu vykreslí stránku do prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="42013-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="42013-314">V důsledku toho stránka sice neví, že jste právě pracovali s předchozí verzí sebe sama.</span><span class="sxs-lookup"><span data-stu-id="42013-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="42013-315">To znamená, že se v něm objevila žádost, která obsahuje nějaká data formuláře.</span><span class="sxs-lookup"><span data-stu-id="42013-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="42013-316">Pokaždé, když si vyžádáte stránku &mdash;, jestli poprvé nebo když ji odešlete &mdash; získáte novou stránku.</span><span class="sxs-lookup"><span data-stu-id="42013-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="42013-317">Webový server nemá žádnou paměť vaší poslední žádosti.</span><span class="sxs-lookup"><span data-stu-id="42013-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="42013-318">Nedělá ASP.NET ani prohlížeč.</span><span class="sxs-lookup"><span data-stu-id="42013-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="42013-319">Jediným připojením mezi těmito samostatnými instancemi stránky jsou všechna data, která mezi nimi přenášíte.</span><span class="sxs-lookup"><span data-stu-id="42013-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="42013-320">Pokud odešlete stránku, například nová instance stránky může získat data formuláře, která byla odeslána předchozí instancí.</span><span class="sxs-lookup"><span data-stu-id="42013-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="42013-321">(Další způsob, jak předat data mezi stránkami, je použití souborů cookie.)</span><span class="sxs-lookup"><span data-stu-id="42013-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="42013-322">Formální způsob, jak tuto situaci popsat, je říci, že webové stránky jsou *bezstavové*.</span><span class="sxs-lookup"><span data-stu-id="42013-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="42013-323">Webové servery a stránky a prvky na stránce neudržují žádné informace o předchozím stavu stránky.</span><span class="sxs-lookup"><span data-stu-id="42013-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="42013-324">Web byl navržen tímto způsobem, protože udržováním stavu pro jednotlivé požadavky by bylo možné rychle vyčerpat prostředky webových serverů, které často zpracovávají tisíce, možná i stovky tisíců požadavků za sekundu.</span><span class="sxs-lookup"><span data-stu-id="42013-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="42013-325">Takže to je důvod, proč je textové pole prázdné.</span><span class="sxs-lookup"><span data-stu-id="42013-325">So that's why the text box was empty.</span></span> <span data-ttu-id="42013-326">Po odeslání stránky ASP.NET vytvořila novou instanci stránky a běžela prostřednictvím kódu a kódu.</span><span class="sxs-lookup"><span data-stu-id="42013-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="42013-327">V tomto kódu se neobjevila žádná hodnota, která by ASP.NETa vložení hodnoty do textového pole.</span><span class="sxs-lookup"><span data-stu-id="42013-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="42013-328">Takže ASP.NET neudělal cokoli a textové pole bylo vykresleno bez hodnoty.</span><span class="sxs-lookup"><span data-stu-id="42013-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="42013-329">Tento problém se dá nejlépe snadno vyřešit.</span><span class="sxs-lookup"><span data-stu-id="42013-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="42013-330">Žánr, který jste zadali do textového pole, *je* k dispozici v kódu &mdash; je `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="42013-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="42013-331">Aktualizujte značky pro textové pole tak, aby atribut `value` pře`searchTerm`hodnotu z, jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="42013-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="42013-332">Na této stránce můžete také nastavit atribut `value` na `searchTerm` proměnnou, protože tato proměnná obsahuje také Žánr, který jste zadali.</span><span class="sxs-lookup"><span data-stu-id="42013-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="42013-333">Ale použití objektu `Request` k nastavení atributu `value`, jak je znázorněno zde, je standardní způsob, jak tuto úlohu provést.</span><span class="sxs-lookup"><span data-stu-id="42013-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="42013-334">(Za předpokladu, že to budete chtít udělat &mdash; v některých situacích, možná budete chtít vykreslit stránku *bez* hodnot v polích.</span><span class="sxs-lookup"><span data-stu-id="42013-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="42013-335">Vše závisí na tom, co se ve vaší aplikaci prochází.)</span><span class="sxs-lookup"><span data-stu-id="42013-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="42013-336">Nemůžete si pamatovat na hodnotu textového pole, které se používá pro hesla.</span><span class="sxs-lookup"><span data-stu-id="42013-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="42013-337">Může se jednat o bezpečnostní otvor, který umožňuje lidem vyplnit pole hesla pomocí kódu.</span><span class="sxs-lookup"><span data-stu-id="42013-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="42013-338">Spusťte znovu stránku, zadejte Žánr a klikněte na **Hledat Žánr**.</span><span class="sxs-lookup"><span data-stu-id="42013-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="42013-339">Tato doba nestačí jenom zobrazit výsledky hledání, ale textové pole pamatuje, co jste zadali za poslední čas:</span><span class="sxs-lookup"><span data-stu-id="42013-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Stránka ukazující, že textové pole má "rememberd" předchozí položku](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="42013-341">Hledání libovolného slova v nadpisu</span><span class="sxs-lookup"><span data-stu-id="42013-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="42013-342">Teď můžete vyhledat jakýkoliv Žánr, ale možná budete chtít také vyhledat nadpis.</span><span class="sxs-lookup"><span data-stu-id="42013-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="42013-343">Při hledání je obtížné získat nadpis přesně. místo toho můžete vyhledat slovo, které se zobrazí kdekoli v názvu.</span><span class="sxs-lookup"><span data-stu-id="42013-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="42013-344">Chcete-li to provést v SQL, použijte operátor `LIKE` a syntax podobný následujícímu:</span><span class="sxs-lookup"><span data-stu-id="42013-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="42013-345">Tento příkaz načte všechny filmy, jejichž názvy obsahují slovo "Adventure".</span><span class="sxs-lookup"><span data-stu-id="42013-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="42013-346">Při použití operátoru `LIKE` zahrnete zástupný znak `%` jako součást hledaného termínu.</span><span class="sxs-lookup"><span data-stu-id="42013-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="42013-347">Hledání `LIKE 'adventure%'` znamená "začíná slovem" Adventure ".</span><span class="sxs-lookup"><span data-stu-id="42013-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="42013-348">(Technicky to znamená "řetězec" Adventure "následovaný cokoli.") Podobně hledaný termín `LIKE '%adventure'` znamená "cokoli následované řetězcem" Adventure ", což je další způsob, jak vyslovit" "s" Adventure "".</span><span class="sxs-lookup"><span data-stu-id="42013-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="42013-349">Hledaný termín `LIKE '%adventure%'` proto znamená "" Adventure "kdekoli v názvu."</span><span class="sxs-lookup"><span data-stu-id="42013-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="42013-350">(Technicky, "cokoli v názvu, následované" Adventure "následovaný cokoli.")</span><span class="sxs-lookup"><span data-stu-id="42013-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="42013-351">Uvnitř elementu `<form>` přidejte následující kód vpravo pod uzavírací značku `</div>` pro hledání žánru (těsně před uzavíracím `</form>` elementem):</span><span class="sxs-lookup"><span data-stu-id="42013-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="42013-352">Kód pro zpracování tohoto hledání je podobný kódu pro hledání žánru, s výjimkou, že je nutné sestavit `LIKE` vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="42013-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="42013-353">Uvnitř bloku kódu v horní části stránky přidejte tento blok `if` hned za `if` bloku pro hledání žánru:</span><span class="sxs-lookup"><span data-stu-id="42013-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="42013-354">Tento kód používá stejnou logiku, kterou jste viděli dříve, s tím rozdílem, že vyhledávání používá operátor `LIKE` a kód vloží "`%`" před a za hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="42013-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="42013-355">Všimněte si, jak bylo snadné přidat další vyhledávání na stránku.</span><span class="sxs-lookup"><span data-stu-id="42013-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="42013-356">Měli byste to udělat:</span><span class="sxs-lookup"><span data-stu-id="42013-356">All you had to do was:</span></span>

- <span data-ttu-id="42013-357">Vytvořte `if` blok, který byl testován, aby bylo možné zjistit, zda příslušné vyhledávací pole mělo hodnotu.</span><span class="sxs-lookup"><span data-stu-id="42013-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="42013-358">Nastavte proměnnou `selectCommand` na nový příkaz SQL.</span><span class="sxs-lookup"><span data-stu-id="42013-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="42013-359">Nastavte proměnnou `searchTerm` na hodnotu, která má být předána dotazu.</span><span class="sxs-lookup"><span data-stu-id="42013-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="42013-360">Zde je kompletní blok kódu, který obsahuje novou logiku pro hledání v nadpisu:</span><span class="sxs-lookup"><span data-stu-id="42013-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="42013-361">Tady je přehled toho, co tento kód dělá:</span><span class="sxs-lookup"><span data-stu-id="42013-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="42013-362">Proměnné `searchTerm` a `selectCommand` jsou inicializovány v horní části.</span><span class="sxs-lookup"><span data-stu-id="42013-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="42013-363">Chystáte se nastavit tyto proměnné na příslušný hledaný výraz (pokud existuje) a odpovídající příkaz SQL na základě toho, co uživatel na stránce provádí.</span><span class="sxs-lookup"><span data-stu-id="42013-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="42013-364">Výchozím hledáním je jednoduchý případ získávání všech filmů z databáze.</span><span class="sxs-lookup"><span data-stu-id="42013-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="42013-365">V testech pro `searchGenre` a `searchTitle`kód nastaví `searchTerm` na hodnotu, kterou chcete vyhledat.</span><span class="sxs-lookup"><span data-stu-id="42013-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="42013-366">Tyto bloky kódu také nastavují `selectCommand` pro příslušné příkazy SQL pro toto hledání.</span><span class="sxs-lookup"><span data-stu-id="42013-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="42013-367">Metoda `db.Query` je vyvolána pouze jednou, přičemž použití jakéhokoliv příkazu SQL je v `selectedCommand` a libovolná hodnota je v `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42013-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="42013-368">Pokud neexistuje hledaný termín (žádný Žánr a žádné slovo title), hodnota `searchTerm` je prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="42013-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="42013-369">Nicméně to nezáleží, protože v takovém případě dotaz nepožaduje parametr.</span><span class="sxs-lookup"><span data-stu-id="42013-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="42013-370">Testování funkce hledání názvu</span><span class="sxs-lookup"><span data-stu-id="42013-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="42013-371">Nyní můžete otestovat dokončenou stránku vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="42013-371">Now you can test your completed search page.</span></span> <span data-ttu-id="42013-372">Spusťte *video. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="42013-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="42013-373">Zadejte Žánr a klikněte na **Hledat Žánr**.</span><span class="sxs-lookup"><span data-stu-id="42013-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="42013-374">Mřížka zobrazuje filmy tohoto žánru, například před.</span><span class="sxs-lookup"><span data-stu-id="42013-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="42013-375">Zadejte název slovo a klikněte na **Hledat nadpis**.</span><span class="sxs-lookup"><span data-stu-id="42013-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="42013-376">Mřížka zobrazuje filmy, které mají toto slovo v názvu.</span><span class="sxs-lookup"><span data-stu-id="42013-376">The grid displays movies that have that word in the title.</span></span>

![Seznam filmů po vyhledání ' The ' v nadpisu](form-basics/_static/image6.png)

<span data-ttu-id="42013-378">Obě textová pole ponechte prázdné a klikněte na jedno tlačítko.</span><span class="sxs-lookup"><span data-stu-id="42013-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="42013-379">V mřížce se zobrazí všechny filmy.</span><span class="sxs-lookup"><span data-stu-id="42013-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="42013-380">Kombinování dotazů</span><span class="sxs-lookup"><span data-stu-id="42013-380">Combining the Queries</span></span>

<span data-ttu-id="42013-381">Můžete si všimnout, že hledání, která můžete provádět, jsou exkluzivní.</span><span class="sxs-lookup"><span data-stu-id="42013-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="42013-382">Nemůžete současně prohledávat název a žánr, a to i v případě, že obě vyhledávací pole obsahují v nich hodnoty.</span><span class="sxs-lookup"><span data-stu-id="42013-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="42013-383">Například nemůžete vyhledat všechny filmy akcí, jejichž název obsahuje text "Adventure".</span><span class="sxs-lookup"><span data-stu-id="42013-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="42013-384">(Jak je stránka kódována, pokud zadáte hodnoty pro Žánr a název, bude hledání názvu přednost.) Chcete-li vytvořit vyhledávání, které kombinuje podmínky, je nutné vytvořit dotaz SQL, který má následující syntaxi:</span><span class="sxs-lookup"><span data-stu-id="42013-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="42013-385">A musíte spustit dotaz pomocí příkazu podobného následujícímu (přibližně řečeno):</span><span class="sxs-lookup"><span data-stu-id="42013-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="42013-386">Vytvoření logiky, která povoluje mnoho permutací kritérií vyhledávání, může mít za to, jak můžete vidět.</span><span class="sxs-lookup"><span data-stu-id="42013-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="42013-387">Proto tady zastavíme.</span><span class="sxs-lookup"><span data-stu-id="42013-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="42013-388">Připravujeme další</span><span class="sxs-lookup"><span data-stu-id="42013-388">Coming Up Next</span></span>

<span data-ttu-id="42013-389">V dalším kurzu vytvoříte stránku, která používá formulář, aby uživatelé mohli přidávat do databáze filmy.</span><span class="sxs-lookup"><span data-stu-id="42013-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="42013-390">Úplný výpis pro stránku videa (aktualizovaný pomocí hledání)</span><span class="sxs-lookup"><span data-stu-id="42013-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="42013-391">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="42013-391">Additional Resources</span></span>

- [<span data-ttu-id="42013-392">Seznámení s ASP.NET webovým programováním pomocí syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="42013-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="42013-393">[Klauzule WHERE jazyka SQL](http://www.w3schools.com/sql/sql_where.asp) na webu w3schools</span><span class="sxs-lookup"><span data-stu-id="42013-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="42013-394">Článek [definice metod](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) na webu W3C</span><span class="sxs-lookup"><span data-stu-id="42013-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="42013-395">[Předchozí](displaying-data.md)
> [Další](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="42013-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
