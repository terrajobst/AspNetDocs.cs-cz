---
uid: web-pages/overview/ui-layouts-and-themes/4-working-with-forms
title: Práce s formuláři HTML na webech ASP.NET Web Pages (Razor) | Microsoft Docs
author: Rick-Anderson
description: Formulář je oddíl dokumentu HTML, kam vložíte ovládací prvky pro zadání uživatele, jako jsou textová pole, zaškrtávací políčka, přepínače a rozevírací seznamy. Používáte formuláře Wh...
ms.author: riande
ms.date: 02/10/2014
ms.assetid: f3f4b8c8-e8f6-4474-ad94-69228a6c01ee
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/4-working-with-forms
msc.type: authoredcontent
ms.openlocfilehash: c7d4802063c8610a246afe67bd15eea429f7304a
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/06/2020
ms.locfileid: "78639704"
---
# <a name="working-with-html-forms-in-aspnet-web-pages-razor-sites"></a><span data-ttu-id="c63be-104">Práce s formuláři HTML na webech ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="c63be-104">Working with HTML Forms in ASP.NET Web Pages (Razor) Sites</span></span>

<span data-ttu-id="c63be-105">tím, že [FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c63be-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c63be-106">Tento článek popisuje, jak zpracovat formulář HTML (s textovými poli a tlačítky) při práci na webu ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="c63be-106">This article describes how to process an HTML form (with text boxes and buttons) when you are working in an ASP.NET Web Pages (Razor) website.</span></span>
> 
> <span data-ttu-id="c63be-107">**Co se naučíte:**</span><span class="sxs-lookup"><span data-stu-id="c63be-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="c63be-108">Vytvoření formuláře HTML.</span><span class="sxs-lookup"><span data-stu-id="c63be-108">How to create an HTML form.</span></span>
> - <span data-ttu-id="c63be-109">Čtení vstupu uživatele z formuláře.</span><span class="sxs-lookup"><span data-stu-id="c63be-109">How to read user input from the form.</span></span>
> - <span data-ttu-id="c63be-110">Ověření vstupu uživatele</span><span class="sxs-lookup"><span data-stu-id="c63be-110">How to validate user input.</span></span>
> - <span data-ttu-id="c63be-111">Obnovení hodnot formuláře po odeslání stránky.</span><span class="sxs-lookup"><span data-stu-id="c63be-111">How to restore form values after the page is submitted.</span></span>
> 
> <span data-ttu-id="c63be-112">Toto jsou koncepty programování ASP.NET představené v článku:</span><span class="sxs-lookup"><span data-stu-id="c63be-112">These are the ASP.NET programming concepts introduced in the article:</span></span>
> 
> - <span data-ttu-id="c63be-113">Objekt `Request`</span><span class="sxs-lookup"><span data-stu-id="c63be-113">The `Request` object.</span></span>
> - <span data-ttu-id="c63be-114">Ověřování vstupu.</span><span class="sxs-lookup"><span data-stu-id="c63be-114">Input validation.</span></span>
> - <span data-ttu-id="c63be-115">Kódování HTML.</span><span class="sxs-lookup"><span data-stu-id="c63be-115">HTML encoding.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="c63be-116">Verze softwaru použité v tomto kurzu</span><span class="sxs-lookup"><span data-stu-id="c63be-116">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="c63be-117">Webové stránky ASP.NET (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="c63be-117">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="c63be-118">Tento kurz funguje také s ASP.NET webovými stránkami 2.</span><span class="sxs-lookup"><span data-stu-id="c63be-118">This tutorial also works with ASP.NET Web Pages 2.</span></span>

## <a name="creating-a-simple-html-form"></a><span data-ttu-id="c63be-119">Vytvoření jednoduchého HTML formuláře</span><span class="sxs-lookup"><span data-stu-id="c63be-119">Creating a Simple HTML Form</span></span>

1. <span data-ttu-id="c63be-120">Vytvoří nový web.</span><span class="sxs-lookup"><span data-stu-id="c63be-120">Create a new website.</span></span>
2. <span data-ttu-id="c63be-121">V kořenové složce vytvořte webovou stránku s názvem *Form. cshtml* a zadejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="c63be-121">In the root folder, create a web page named *Form.cshtml* and enter the following markup:</span></span>

    [!code-html[Main](4-working-with-forms/samples/sample1.html)]
3. <span data-ttu-id="c63be-122">Spusťte stránku v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="c63be-122">Launch the page in your browser.</span></span> <span data-ttu-id="c63be-123">(Ve WebMatrixu v pracovním prostoru **soubory** klikněte pravým tlačítkem na soubor a vyberte **Spustit v prohlížeči**.) Zobrazí se jednoduchý formulář se třemi vstupními poli a tlačítkem **Odeslat** .</span><span class="sxs-lookup"><span data-stu-id="c63be-123">(In WebMatrix, in the **Files** workspace, right-click the file and then select **Launch in browser**.) A simple form with three input fields and a **Submit** button is displayed.</span></span>

    ![Snímek obrazovky formuláře se třemi textovými poli](4-working-with-forms/_static/image1.png)

    <span data-ttu-id="c63be-125">Pokud v tomto okamžiku kliknete na tlačítko **Odeslat** , nedojde k žádné akci.</span><span class="sxs-lookup"><span data-stu-id="c63be-125">At this point, if you click the **Submit** button, nothing happens.</span></span> <span data-ttu-id="c63be-126">Aby formulář mohl být užitečný, musíte přidat nějaký kód, který se spustí na serveru.</span><span class="sxs-lookup"><span data-stu-id="c63be-126">To make the form useful, you have to add some code that will run on the server.</span></span>

## <a name="reading-user-input-from-the-form"></a><span data-ttu-id="c63be-127">Čtení vstupu uživatele z formuláře</span><span class="sxs-lookup"><span data-stu-id="c63be-127">Reading User Input from the Form</span></span>

<span data-ttu-id="c63be-128">Chcete-li zpracovat formulář, přidejte kód, který přečte hodnoty odeslaných polí a provede s nimi něco.</span><span class="sxs-lookup"><span data-stu-id="c63be-128">To process the form, you add code that reads the submitted field values and does something with them.</span></span> <span data-ttu-id="c63be-129">Tento postup ukazuje, jak číst pole a zobrazit vstup uživatele na stránce.</span><span class="sxs-lookup"><span data-stu-id="c63be-129">This procedure shows you how to read the fields and display the user input on the page.</span></span> <span data-ttu-id="c63be-130">(V produkční aplikaci obecně provedete zajímavější věci s uživatelským vstupem.</span><span class="sxs-lookup"><span data-stu-id="c63be-130">(In a production application, you generally do more interesting things with user input.</span></span> <span data-ttu-id="c63be-131">Provedete to v článku o práci s databázemi.)</span><span class="sxs-lookup"><span data-stu-id="c63be-131">You'll do that in the article about working with databases.)</span></span>

1. <span data-ttu-id="c63be-132">V horní části souboru *. cshtml formuláře* zadejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="c63be-132">At the top of the *Form.cshtml* file, enter the following code:</span></span>

    [!code-cshtml[Main](4-working-with-forms/samples/sample2.cshtml)]

    <span data-ttu-id="c63be-133">Když uživatel poprvé vyžádá stránku, zobrazí se pouze prázdný formulář.</span><span class="sxs-lookup"><span data-stu-id="c63be-133">When the user first requests the page, only the empty form is displayed.</span></span> <span data-ttu-id="c63be-134">Uživatel (který bude) vyplní formulář a klikne na **Odeslat**.</span><span class="sxs-lookup"><span data-stu-id="c63be-134">The user (which will be you) fills in the form and then clicks **Submit**.</span></span> <span data-ttu-id="c63be-135">Tím se vstup uživatele odešle (odešle) na server.</span><span class="sxs-lookup"><span data-stu-id="c63be-135">This submits (posts) the user input to the server.</span></span> <span data-ttu-id="c63be-136">Ve výchozím nastavení požadavek přejde na stejnou stránku (konkrétně na *Form. cshtml*).</span><span class="sxs-lookup"><span data-stu-id="c63be-136">By default, the request goes to the same page (namely, *Form.cshtml*).</span></span>

    <span data-ttu-id="c63be-137">Když stránku odešlete tímto časem, hodnoty, které jste zadali, se zobrazí hned nad formulářem:</span><span class="sxs-lookup"><span data-stu-id="c63be-137">When you submit the page this time, the values you entered are displayed just above the form:</span></span>

    ![Snímek obrazovky zobrazující hodnoty, které jste zadali na stránce.](4-working-with-forms/_static/image2.png)

    <span data-ttu-id="c63be-139">Podívejte se na kód stránky.</span><span class="sxs-lookup"><span data-stu-id="c63be-139">Look at the code for the page.</span></span> <span data-ttu-id="c63be-140">Nejprve pomocí metody `IsPost` určíte, zda je stránka publikována &#8212; , tedy zda uživatel klikl na tlačítko **Odeslat** .</span><span class="sxs-lookup"><span data-stu-id="c63be-140">You first use the `IsPost` method to determine whether the page is being posted &#8212; that is, whether a user clicked the **Submit** button.</span></span> <span data-ttu-id="c63be-141">Pokud se jedná o příspěvek, `IsPost` vrátí hodnotu true.</span><span class="sxs-lookup"><span data-stu-id="c63be-141">If this is a post, `IsPost` returns true.</span></span> <span data-ttu-id="c63be-142">Toto je standardní způsob na webových stránkách ASP.NET, abyste zjistili, jestli pracujete s počátečním požadavkem (požadavek GET) nebo zpětným voláním (požadavek POST).</span><span class="sxs-lookup"><span data-stu-id="c63be-142">This is the standard way in ASP.NET Web Pages to determine whether you're working with an initial request (a GET request) or a postback (a POST request).</span></span> <span data-ttu-id="c63be-143">(Další informace o příkazu GET a POST najdete na bočním panelu "HTTP GET a POST a" Property "v tématu [Úvod k ASP.NET programování webových stránek pomocí syntaxe Razor](https://go.microsoft.com/fwlink/?LinkId=202890#SB_HttpGetPost).)</span><span class="sxs-lookup"><span data-stu-id="c63be-143">(For more information about GET and POST, see the sidebar "HTTP GET and POST and the IsPost Property" in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#SB_HttpGetPost).)</span></span>

    <span data-ttu-id="c63be-144">Dále získáte hodnoty, které uživatel vyplnil z objektu `Request.Form` a vložíte je do proměnných pro pozdější použití.</span><span class="sxs-lookup"><span data-stu-id="c63be-144">Next, you get the values that the user filled in from the `Request.Form` object, and you put them in variables for later.</span></span> <span data-ttu-id="c63be-145">Objekt `Request.Form` obsahuje všechny hodnoty, které byly odeslány se stránkou, z nichž každá je identifikována klíčem.</span><span class="sxs-lookup"><span data-stu-id="c63be-145">The `Request.Form` object contains all the values that were submitted with the page, each identified by a key.</span></span> <span data-ttu-id="c63be-146">Klíč je ekvivalentem atributu `name` pole formuláře, které chcete číst.</span><span class="sxs-lookup"><span data-stu-id="c63be-146">The key is the equivalent to the `name` attribute of the form field that you want to read.</span></span> <span data-ttu-id="c63be-147">Chcete-li například číst pole `companyname` (textové pole), použijte `Request.Form["companyname"]`.</span><span class="sxs-lookup"><span data-stu-id="c63be-147">For example, to read the `companyname` field (text box), you use `Request.Form["companyname"]`.</span></span>

    <span data-ttu-id="c63be-148">Hodnoty formuláře jsou uloženy v objektu `Request.Form` jako řetězce.</span><span class="sxs-lookup"><span data-stu-id="c63be-148">Form values are stored in the `Request.Form` object as strings.</span></span> <span data-ttu-id="c63be-149">Proto pokud je nutné pracovat s hodnotou jako číslo nebo datum nebo nějaký jiný typ, je nutné jej převést z řetězce na tento typ.</span><span class="sxs-lookup"><span data-stu-id="c63be-149">Therefore, when you have to work with a value as a number or a date or some other type, you have to convert it from a string to that type.</span></span> <span data-ttu-id="c63be-150">V příkladu je metoda `AsInt` `Request.Form` použita k převodu hodnoty pole zaměstnanci (které obsahuje počet zaměstnanců) na celé číslo.</span><span class="sxs-lookup"><span data-stu-id="c63be-150">In the example, the `AsInt` method of the `Request.Form` is used to convert the value of the employees field (which contains an employee count) to an integer.</span></span>
2. <span data-ttu-id="c63be-151">Otevřete stránku v prohlížeči, vyplňte pole formuláře a klikněte na **Odeslat**.</span><span class="sxs-lookup"><span data-stu-id="c63be-151">Launch the page in your browser, fill in the form fields, and click **Submit**.</span></span> <span data-ttu-id="c63be-152">Na stránce se zobrazí hodnoty, které jste zadali.</span><span class="sxs-lookup"><span data-stu-id="c63be-152">The page displays the values you entered.</span></span>

> [!TIP] 
> 
> <a id="SB_HTMLEncoding"></a>
> ### <a name="html-encoding-for-appearance-and-security"></a><span data-ttu-id="c63be-153">Kódování HTML pro vzhled a zabezpečení</span><span class="sxs-lookup"><span data-stu-id="c63be-153">HTML Encoding for Appearance and Security</span></span>
> 
> <span data-ttu-id="c63be-154">HTML má speciální použití pro znaky, jako `<`, `>`a `&`.</span><span class="sxs-lookup"><span data-stu-id="c63be-154">HTML has special uses for characters like `<`, `>`, and `&`.</span></span> <span data-ttu-id="c63be-155">Pokud se tyto speciální znaky zobrazí tam, kde nejsou očekávány, mohou ruin vzhled a funkce webové stránky.</span><span class="sxs-lookup"><span data-stu-id="c63be-155">If these special characters appear where they're not expected, they can ruin the appearance and functionality of your web page.</span></span> <span data-ttu-id="c63be-156">Například prohlížeč interpretuje `<` znak (Pokud není následován mezerou) jako začátek elementu HTML, jako je například `<b>` nebo `<input ...>`.</span><span class="sxs-lookup"><span data-stu-id="c63be-156">For example, the browser interprets the `<` character (unless it's followed by a space) as the beginning of an HTML element, like `<b>` or `<input ...>`.</span></span> <span data-ttu-id="c63be-157">Pokud prohlížeč nerozpozná prvek, jednoduše zahodí řetězec, který začíná na `<`, dokud nedosáhne něco, co znovu rozpoznává.</span><span class="sxs-lookup"><span data-stu-id="c63be-157">If the browser doesn't recognize the element, it simply discards the string that begins with `<` until it reaches something that it again recognizes.</span></span> <span data-ttu-id="c63be-158">Zjevně to může způsobit, že se na stránce divné vykreslování.</span><span class="sxs-lookup"><span data-stu-id="c63be-158">Obviously, this can result in some weird rendering in the page.</span></span>
> 
> <span data-ttu-id="c63be-159">Kódování HTML nahrazuje tyto vyhrazené znaky kódem, který prohlížeče interpretuje jako správný symbol.</span><span class="sxs-lookup"><span data-stu-id="c63be-159">HTML encoding replaces these reserved characters with a code that browsers interpret as the correct symbol.</span></span> <span data-ttu-id="c63be-160">Například znak `<` je nahrazen `&lt;` a znak `>` je nahrazen `&gt;`.</span><span class="sxs-lookup"><span data-stu-id="c63be-160">For example, the `<` character is replaced with `&lt;` and the `>` character is replaced with `&gt;`.</span></span> <span data-ttu-id="c63be-161">Prohlížeč vykreslí tyto náhradní řetězce jako znaky, které chcete zobrazit.</span><span class="sxs-lookup"><span data-stu-id="c63be-161">The browser renders these replacement strings as the characters that you want to see.</span></span>
> 
> <span data-ttu-id="c63be-162">Je vhodné použít kódování HTML při každém zobrazení řetězců (vstupů), které jste dostali od uživatele.</span><span class="sxs-lookup"><span data-stu-id="c63be-162">It's a good idea to use HTML encoding any time you display strings (input) that you got from a user.</span></span> <span data-ttu-id="c63be-163">Pokud to neuděláte, uživatel se může pokusit najít webovou stránku, aby spouštěl škodlivý skript nebo něco jiného, co neohrožuje zabezpečení vašeho webu, nebo to není, co máte v úmyslu.</span><span class="sxs-lookup"><span data-stu-id="c63be-163">If you don't, a user can try to get your web page to run a malicious script or do something else that compromises your site security or that's just not what you intend.</span></span> <span data-ttu-id="c63be-164">(To je obzvláště důležité, Pokud převezmete uživatelský vstup, uložte ho někde došlo a pak ho později &#8212; zobrazit jako komentář blogu, kontrolu uživatele nebo něco podobného.)</span><span class="sxs-lookup"><span data-stu-id="c63be-164">(This is particularly important if you take user input, store it someplace, and then display it later &#8212; for example, as a blog comment, user review, or something like that.)</span></span>
> 
> <span data-ttu-id="c63be-165">Aby se zabránilo těmto problémům, webové stránky ASP.NET automaticky zakóduje veškerý textový obsah, který je výstupem z kódu.</span><span class="sxs-lookup"><span data-stu-id="c63be-165">To help prevent these problems, ASP.NET Web Pages automatically HTML-encodes any text content that you output from your code.</span></span> <span data-ttu-id="c63be-166">Například při zobrazení obsahu proměnné nebo výrazu pomocí kódu, jako je například `@MyVar`, ASP.NET webové stránky automaticky zakóduje výstup.</span><span class="sxs-lookup"><span data-stu-id="c63be-166">For example, when you display the content of a variable or an expression using code such as `@MyVar`, ASP.NET Web Pages automatically encodes the output.</span></span>

## <a name="validating-user-input"></a><span data-ttu-id="c63be-167">Ověřování vstupu uživatele</span><span class="sxs-lookup"><span data-stu-id="c63be-167">Validating User Input</span></span>

<span data-ttu-id="c63be-168">Uživatelé provedou chyby.</span><span class="sxs-lookup"><span data-stu-id="c63be-168">Users make mistakes.</span></span> <span data-ttu-id="c63be-169">Požádáte je, aby vyplnili pole a zapomněli se k němu, nebo požádáte, aby zadali počet zaměstnanců, a místo toho zadá název.</span><span class="sxs-lookup"><span data-stu-id="c63be-169">You ask them to fill in a field, and they forget to, or you ask them to enter the number of employees and they type a name instead.</span></span> <span data-ttu-id="c63be-170">Chcete-li zajistit, aby byl formulář před jeho zpracováním vyplněn správně, ověřte vstup uživatele.</span><span class="sxs-lookup"><span data-stu-id="c63be-170">To make sure that a form has been filled in correctly before you process it, you validate the user's input.</span></span>

<span data-ttu-id="c63be-171">Tento postup ukazuje, jak ověřit všechna tři pole formuláře, abyste se ujistili, že uživatel je nenechal prázdný.</span><span class="sxs-lookup"><span data-stu-id="c63be-171">This procedure shows how to validate all three form fields to make sure the user didn't leave them blank.</span></span> <span data-ttu-id="c63be-172">Také zkontrolujete, jestli je hodnota počtu zaměstnanců číslo.</span><span class="sxs-lookup"><span data-stu-id="c63be-172">You also check that the employee count value is a number.</span></span> <span data-ttu-id="c63be-173">Pokud dojde k chybám, zobrazí se chybová zpráva s informacemi o tom, jaké hodnoty neprošly ověřením.</span><span class="sxs-lookup"><span data-stu-id="c63be-173">If there are errors, you'll display an error message that tells the user what values didn't pass validation.</span></span>

1. <span data-ttu-id="c63be-174">V souboru *Form. cshtml* nahraďte první blok kódu následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="c63be-174">In the *Form.cshtml* file, replace the first block of code with the following code:</span></span> 

    [!code-cshtml[Main](4-working-with-forms/samples/sample3.cshtml)]

    <span data-ttu-id="c63be-175">K ověření vstupu uživatele použijete pomoc `Validation`.</span><span class="sxs-lookup"><span data-stu-id="c63be-175">To validate user input, you use the `Validation` helper.</span></span> <span data-ttu-id="c63be-176">Požadovaná pole zaregistrujete voláním `Validation.RequireField`.</span><span class="sxs-lookup"><span data-stu-id="c63be-176">You register required fields by calling `Validation.RequireField`.</span></span> <span data-ttu-id="c63be-177">Další typy ověřování zaregistrujete voláním `Validation.Add` a zadáním pole, které se má ověřit, a typu ověření, které má být provedeno.</span><span class="sxs-lookup"><span data-stu-id="c63be-177">You register other types of validation by calling `Validation.Add` and specifying the field to validate and the type of validation to perform.</span></span>

    <span data-ttu-id="c63be-178">Po spuštění stránky ASP.NET provede všechna ověření za vás.</span><span class="sxs-lookup"><span data-stu-id="c63be-178">When the page runs, ASP.NET does all the validation for you.</span></span> <span data-ttu-id="c63be-179">Výsledky můžete kontrolovat voláním `Validation.IsValid`, což vrátí hodnotu true, pokud se jakékoli pole nezdařilo, a NEPRAVDA.</span><span class="sxs-lookup"><span data-stu-id="c63be-179">You can check the results by calling `Validation.IsValid`, which returns true if everything passed and false if any field failed validation.</span></span> <span data-ttu-id="c63be-180">Obvykle zavoláte `Validation.IsValid` před provedením jakéhokoli zpracování na vstupu uživatele.</span><span class="sxs-lookup"><span data-stu-id="c63be-180">Typically, you call `Validation.IsValid` before you perform any processing on the user input.</span></span>
2. <span data-ttu-id="c63be-181">Aktualizujte `<body>` element přidáním tří volání metody `Html.ValidationMessage`, například takto:</span><span class="sxs-lookup"><span data-stu-id="c63be-181">Update the `<body>` element by adding three calls to the `Html.ValidationMessage` method, like this:</span></span>

    [!code-cshtml[Main](4-working-with-forms/samples/sample4.cshtml?highlight=8,13,18)]

    <span data-ttu-id="c63be-182">Chcete-li zobrazit chybové zprávy ověřování, můžete zavolat HTML.`ValidationMessage`</span><span class="sxs-lookup"><span data-stu-id="c63be-182">To display validation error messages, you can call Html.`ValidationMessage`</span></span> <span data-ttu-id="c63be-183">a předejte jí název pole, pro které chcete zprávu.</span><span class="sxs-lookup"><span data-stu-id="c63be-183">and pass it the name of the field that you want the message for.</span></span>
3. <span data-ttu-id="c63be-184">Spusťte stránku.</span><span class="sxs-lookup"><span data-stu-id="c63be-184">Run the page.</span></span> <span data-ttu-id="c63be-185">Pole ponechte prázdné a klikněte na **Odeslat**.</span><span class="sxs-lookup"><span data-stu-id="c63be-185">Leave the fields blank and click **Submit**.</span></span> <span data-ttu-id="c63be-186">Zobrazí se chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="c63be-186">You see error messages.</span></span>

    ![Snímek obrazovky zobrazující chybové zprávy zobrazené v případě, že uživatelský vstup neprojde ověřením](4-working-with-forms/_static/image3.jpg)
4. <span data-ttu-id="c63be-188">Přidejte řetězec (například "ABC") do pole **počet zaměstnanců** a klikněte na **Odeslat** znovu.</span><span class="sxs-lookup"><span data-stu-id="c63be-188">Add a string (for example, "ABC") to the **Employee Count** field and click **Submit** again.</span></span> <span data-ttu-id="c63be-189">Tentokrát se zobrazí chyba, která indikuje, že řetězec není ve správném formátu, jmenovitě jako celé číslo.</span><span class="sxs-lookup"><span data-stu-id="c63be-189">This time you see an error that indicates that the string isn't in the right format, namely, an integer.</span></span>

    ![Snímek obrazovky zobrazující chybové zprávy, které se zobrazí, pokud uživatelé zadají do pole zaměstnanci řetězec.](4-working-with-forms/_static/image4.jpg)

<span data-ttu-id="c63be-191">Webové stránky ASP.NET poskytují více možností pro ověřování vstupu uživatele, včetně možnosti automaticky provádět ověřování pomocí skriptu klienta, aby uživatelé získali okamžitou zpětnou vazbu v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="c63be-191">ASP.NET Web Pages provides more options for validating user input, including the ability to automatically perform validation using client script, so that users get immediate feedback in the browser.</span></span> <span data-ttu-id="c63be-192">Další informace najdete v části [Další materiály](#Additional_Resources) níže.</span><span class="sxs-lookup"><span data-stu-id="c63be-192">See the [Additional Resources](#Additional_Resources) section later for more information.</span></span>

## <a name="restoring-form-values-after-postbacks"></a><span data-ttu-id="c63be-193">Obnovení hodnot formuláře po Postbackech</span><span class="sxs-lookup"><span data-stu-id="c63be-193">Restoring Form Values After Postbacks</span></span>

<span data-ttu-id="c63be-194">Když jste otestovali stránku v předchozí části, možná jste si všimli, že pokud jste provedli chybu ověření, všechno, co jste zadali (ne jen neplatná data), bylo pryč a museli jste znovu zadat hodnoty pro všechna pole.</span><span class="sxs-lookup"><span data-stu-id="c63be-194">When you tested the page in the previous section, you might have noticed that if you had a validation error, everything you entered (not just the invalid data) was gone, and you had to re-enter values for all the fields.</span></span> <span data-ttu-id="c63be-195">To ukazuje důležitý bod: Když odešlete stránku, zpracujete ji a pak ji znovu vykreslíte, stránka se znovu vytvoří od začátku.</span><span class="sxs-lookup"><span data-stu-id="c63be-195">This illustrates an important point: when you submit a page, process it, and then render the page again, the page is re-created from scratch.</span></span> <span data-ttu-id="c63be-196">Jak jste viděli, znamená to, že všechny hodnoty, které byly na stránce při odeslání, jsou ztraceny.</span><span class="sxs-lookup"><span data-stu-id="c63be-196">As you saw, this means that any values that were in the page when it was submitted are lost.</span></span>

<span data-ttu-id="c63be-197">Můžete to ale snadno opravit.</span><span class="sxs-lookup"><span data-stu-id="c63be-197">You can fix this easily, however.</span></span> <span data-ttu-id="c63be-198">Máte přístup k hodnotám, které byly odeslány (v objektu `Request.Form`, takže je možné tyto hodnoty vyplnit do polí formuláře při vykreslování stránky.</span><span class="sxs-lookup"><span data-stu-id="c63be-198">You have access to the values that were submitted (in the `Request.Form` object, so you can fill those values back into the form fields when the page is rendered.</span></span>

1. <span data-ttu-id="c63be-199">V souboru *Form. cshtml* nahraďte `value` atributy `<input>` prvků pomocí atributu `value`.:</span><span class="sxs-lookup"><span data-stu-id="c63be-199">In the *Form.cshtml* file, replace the `value` attributes of the `<input>` elements using the `value` attribute.:</span></span> 

    [!code-cshtml[Main](4-working-with-forms/samples/sample5.cshtml?highlight=13,19,25)]

    <span data-ttu-id="c63be-200">Atribut `value` prvků `<input>` byl nastaven na dynamické čtení hodnoty pole z objektu `Request.Form`.</span><span class="sxs-lookup"><span data-stu-id="c63be-200">The `value` attribute of the `<input>` elements has been set to dynamically read the field value out of the `Request.Form` object.</span></span> <span data-ttu-id="c63be-201">Při prvním vyžádání stránky jsou všechny hodnoty v objektu `Request.Form` prázdné.</span><span class="sxs-lookup"><span data-stu-id="c63be-201">The first time that the page is requested, the values in the `Request.Form` object are all empty.</span></span> <span data-ttu-id="c63be-202">To je v pořádku, protože tento formulář je prázdný.</span><span class="sxs-lookup"><span data-stu-id="c63be-202">This is fine, because that way the form is blank.</span></span>
2. <span data-ttu-id="c63be-203">Otevřete stránku v prohlížeči, vyplňte pole formuláře nebo je nechte prázdné a klikněte na **Odeslat**.</span><span class="sxs-lookup"><span data-stu-id="c63be-203">Launch the page in your browser, fill in the form fields or leave them blank, and click **Submit**.</span></span> <span data-ttu-id="c63be-204">Zobrazí se stránka, která zobrazuje odeslané hodnoty.</span><span class="sxs-lookup"><span data-stu-id="c63be-204">A page that shows the submitted values is displayed.</span></span>

    ![forms-5](4-working-with-forms/_static/image5.jpg)

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="c63be-206">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="c63be-206">Additional Resources</span></span>

- [<span data-ttu-id="c63be-207">1 001 způsoby získání vstupu od webových uživatelů</span><span class="sxs-lookup"><span data-stu-id="c63be-207">1,001 Ways to Get Input from Web Users</span></span>](https://msdn.microsoft.com/library/ms971057.aspx)
- <span data-ttu-id="c63be-208">[Používání formulářů a zpracování vstupu uživatele](https://msdn.microsoft.com/library/ms525182(VS.90).aspx)</span><span class="sxs-lookup"><span data-stu-id="c63be-208">[Using Forms and Processing User Input](https://msdn.microsoft.com/library/ms525182(VS.90).aspx)</span></span>
- [<span data-ttu-id="c63be-209">Ověřování uživatelských vstupů na webech s webovými stránkami ASP.NET</span><span class="sxs-lookup"><span data-stu-id="c63be-209">Validating User Input in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=253002)
- <span data-ttu-id="c63be-210">[Použití automatického dokončování ve formulářích HTML](https://msdn.microsoft.com/library/ms533032(VS.85).aspx)</span><span class="sxs-lookup"><span data-stu-id="c63be-210">[Using AutoComplete in HTML Forms](https://msdn.microsoft.com/library/ms533032(VS.85).aspx)</span></span>
