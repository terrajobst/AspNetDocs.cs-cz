---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: Principy filtrů akcí (VB) | Microsoft Docs
author: microsoft
description: Cílem tohoto kurzu je vysvětlit filtry akcí. Filtr akcí je atribut, který můžete použít pro akci kontroleru, nebo pro celý kontroler...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: 263658231ccaa7863508c691a3570bc00b9e8039
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/28/2019
ms.locfileid: "74590036"
---
# <a name="understanding-action-filters-vb"></a><span data-ttu-id="49551-104">Principy filtrů akcí (VB)</span><span class="sxs-lookup"><span data-stu-id="49551-104">Understanding Action Filters (VB)</span></span>

<span data-ttu-id="49551-105">od [Microsoftu](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="49551-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="49551-106">Stáhnout PDF</span><span class="sxs-lookup"><span data-stu-id="49551-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="49551-107">Cílem tohoto kurzu je vysvětlit filtry akcí.</span><span class="sxs-lookup"><span data-stu-id="49551-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="49551-108">Filtr akcí je atribut, který můžete použít pro akci kontroleru, nebo pro celý kontroler, který mění způsob, jakým se akce provádí.</span><span class="sxs-lookup"><span data-stu-id="49551-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="49551-109">Porozumění filtrům akcí</span><span class="sxs-lookup"><span data-stu-id="49551-109">Understanding Action Filters</span></span>

<span data-ttu-id="49551-110">Cílem tohoto kurzu je vysvětlit filtry akcí.</span><span class="sxs-lookup"><span data-stu-id="49551-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="49551-111">Filtr akcí je atribut, který můžete použít pro akci kontroleru, nebo pro celý kontroler, který mění způsob, jakým se akce provádí.</span><span class="sxs-lookup"><span data-stu-id="49551-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="49551-112">Rozhraní ASP.NET MVC obsahuje několik filtrů akcí:</span><span class="sxs-lookup"><span data-stu-id="49551-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="49551-113">OutputCache – tento filtr akcí ukládá do mezipaměti výstup akce kontroleru po zadanou dobu.</span><span class="sxs-lookup"><span data-stu-id="49551-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="49551-114">HandleError – tento filtr akcí zpracovává chyby vyvolané při spuštění akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="49551-115">Autorizovat – tento filtr akcí vám umožňuje omezit přístup k určitému uživateli nebo roli.</span><span class="sxs-lookup"><span data-stu-id="49551-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="49551-116">Můžete také vytvořit vlastní filtry akcí.</span><span class="sxs-lookup"><span data-stu-id="49551-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="49551-117">Například můžete chtít vytvořit vlastní filtr akcí, abyste mohli implementovat vlastní ověřovací systém.</span><span class="sxs-lookup"><span data-stu-id="49551-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="49551-118">Nebo můžete chtít vytvořit filtr akcí, který upraví zobrazení dat vrácených akcí kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="49551-119">V tomto kurzu se naučíte, jak vytvořit filtr akcí od základu.</span><span class="sxs-lookup"><span data-stu-id="49551-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="49551-120">Vytvoříme filtr akcí protokolu, který bude protokolovat různé fáze zpracování akce v okně výstupu sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="49551-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="49551-121">Použití filtru akcí</span><span class="sxs-lookup"><span data-stu-id="49551-121">Using an Action Filter</span></span>

<span data-ttu-id="49551-122">Filtr akcí je atribut.</span><span class="sxs-lookup"><span data-stu-id="49551-122">An action filter is an attribute.</span></span> <span data-ttu-id="49551-123">Většinu filtrů akcí můžete použít buď na akci samostatného kontroleru, nebo na celý kontroler.</span><span class="sxs-lookup"><span data-stu-id="49551-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="49551-124">Například řadič dat v seznamu 1 zpřístupňuje akci s názvem `Index()`, která vrací aktuální čas.</span><span class="sxs-lookup"><span data-stu-id="49551-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="49551-125">Tato akce je upravena pomocí filtru akcí `OutputCache`.</span><span class="sxs-lookup"><span data-stu-id="49551-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="49551-126">Tento filtr způsobí, že hodnota vrácená akcí bude ukládána do mezipaměti po dobu 10 sekund.</span><span class="sxs-lookup"><span data-stu-id="49551-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="49551-127">**Výpis 1 – `Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="49551-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="49551-128">Pokud `Index()` akci opakovaně vyvoláte tak, že zadáte adresu URL/Data/Index do panelu Adresa v prohlížeči a několikrát kliknete na tlačítko Aktualizovat, bude se vám po dobu 10 sekund zobrazovat stejný čas.</span><span class="sxs-lookup"><span data-stu-id="49551-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="49551-129">Výstup akce `Index()` je uložen do mezipaměti po dobu 10 sekund (viz obrázek 1).</span><span class="sxs-lookup"><span data-stu-id="49551-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="49551-130">[čas ![v mezipaměti](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="49551-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="49551-131">**Obrázek 01**: čas v mezipaměti ([kliknutím zobrazíte obrázek v plné velikosti](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="49551-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>

<span data-ttu-id="49551-132">V seznamu 1 se pro metodu `Index()` používá filtr akcí `OutputCache` –.</span><span class="sxs-lookup"><span data-stu-id="49551-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="49551-133">Pokud potřebujete, můžete pro stejnou akci použít více filtrů akcí.</span><span class="sxs-lookup"><span data-stu-id="49551-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="49551-134">Můžete například chtít, aby se pro stejnou akci mohla použít filtr akcí `OutputCache` i `HandleError`.</span><span class="sxs-lookup"><span data-stu-id="49551-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="49551-135">V seznamu 1 se pro akci `Index()` použije filtr akce `OutputCache`.</span><span class="sxs-lookup"><span data-stu-id="49551-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="49551-136">Tento atribut můžete použít také pro třídu `DataController` samotné.</span><span class="sxs-lookup"><span data-stu-id="49551-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="49551-137">V takovém případě by výsledek vrácený všemi akcemi vystavenými řadičem byl uložen do mezipaměti po dobu 10 sekund.</span><span class="sxs-lookup"><span data-stu-id="49551-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="49551-138">Různé typy filtrů</span><span class="sxs-lookup"><span data-stu-id="49551-138">The Different Types of Filters</span></span>

<span data-ttu-id="49551-139">Rozhraní ASP.NET MVC podporuje čtyři různé typy filtrů:</span><span class="sxs-lookup"><span data-stu-id="49551-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="49551-140">Autorizační filtry – implementuje atribut `IAuthorizationFilter`.</span><span class="sxs-lookup"><span data-stu-id="49551-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="49551-141">Filtry akcí – implementuje atribut `IActionFilter`.</span><span class="sxs-lookup"><span data-stu-id="49551-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="49551-142">Filtry výsledků – implementuje atribut `IResultFilter`.</span><span class="sxs-lookup"><span data-stu-id="49551-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="49551-143">Filtry výjimek – implementuje atribut `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="49551-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="49551-144">Filtry se spouštějí v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="49551-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="49551-145">Například filtry autorizace jsou vždy spouštěny před filtry akcí a filtry výjimek jsou vždy provedeny po každém jiném typu filtru.</span><span class="sxs-lookup"><span data-stu-id="49551-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="49551-146">Filtry autorizace slouží k implementaci ověřování a autorizace pro akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="49551-147">Například filtr autorizace je příkladem autorizačního filtru.</span><span class="sxs-lookup"><span data-stu-id="49551-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="49551-148">Filtry akcí obsahují logiku, která se spustí před a po spuštění akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="49551-149">Filtr akcí můžete například použít k úpravě dat zobrazení, která akce kontroleru vrátí.</span><span class="sxs-lookup"><span data-stu-id="49551-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="49551-150">Filtry výsledků obsahují logiku, která se spustí před a po provedení výsledku zobrazení.</span><span class="sxs-lookup"><span data-stu-id="49551-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="49551-151">Například můžete chtít upravit výsledek zobrazení přímo před tím, než se zobrazení vykreslí do prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="49551-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="49551-152">Filtry výjimek jsou poslední typ filtru, který má být spuštěn.</span><span class="sxs-lookup"><span data-stu-id="49551-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="49551-153">Filtr výjimek můžete použít ke zpracování chyb vyvolaných akcemi kontroleru nebo výsledky akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="49551-154">Filtry výjimek můžete použít také k protokolování chyb.</span><span class="sxs-lookup"><span data-stu-id="49551-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="49551-155">Každý jiný typ filtru se spustí v určitém pořadí.</span><span class="sxs-lookup"><span data-stu-id="49551-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="49551-156">Pokud chcete řídit pořadí, ve kterém se spouštějí filtry stejného typu, můžete nastavit vlastnost Order objektu Filter.</span><span class="sxs-lookup"><span data-stu-id="49551-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="49551-157">Základní třídou pro všechny filtry akcí je třída `System.Web.Mvc.FilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="49551-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="49551-158">Pokud chcete implementovat konkrétní typ filtru, je nutné vytvořit třídu, která dědí z třídy základního filtru a implementuje jedno nebo více rozhraní IAuthorizationFilter, IActionFilter, IResultFilter nebo ExceptionFilter.</span><span class="sxs-lookup"><span data-stu-id="49551-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="49551-159">Základní třída ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="49551-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="49551-160">Aby bylo snazší implementovat vlastní filtr akcí, rozhraní ASP.NET MVC zahrnuje základní třídu `ActionFilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="49551-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="49551-161">Tato třída implementuje rozhraní `IActionFilter` i `IResultFilter` a dědí z třídy `Filter`.</span><span class="sxs-lookup"><span data-stu-id="49551-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="49551-162">Tato terminologie není zcela konzistentní.</span><span class="sxs-lookup"><span data-stu-id="49551-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="49551-163">Technicky, třída, která dědí z třídy ActionFilterAttribute, je filtr akcí a filtr výsledků.</span><span class="sxs-lookup"><span data-stu-id="49551-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="49551-164">Ale ve volném smyslu se k odkazování na libovolný typ filtru v rozhraní ASP.NET MVC používá filtr akcí Wordu.</span><span class="sxs-lookup"><span data-stu-id="49551-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="49551-165">Základní třída ActionFilterAttribute má následující metody, které lze přepsat:</span><span class="sxs-lookup"><span data-stu-id="49551-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="49551-166">OnActionExecuting – Tato metoda je volána před provedením akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="49551-167">OnActionExecuted – Tato metoda se volá po provedení akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="49551-168">OnResultExecuting – Tato metoda je volána před provedením výsledku akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="49551-169">OnResultExecuted – Tato metoda se volá poté, co se spustí výsledek akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="49551-170">V další části uvidíte, jak můžete implementovat každou z těchto různých metod.</span><span class="sxs-lookup"><span data-stu-id="49551-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="49551-171">Vytvoření filtru akce protokolu</span><span class="sxs-lookup"><span data-stu-id="49551-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="49551-172">Pro ilustraci, jak můžete vytvořit vlastní filtr akcí, vytvoříme vlastní filtr akcí, který bude protokolovat fáze zpracování akce kontroleru v okně výstupu sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="49551-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="49551-173">Náš `LogActionFilter` je obsažený v seznamu 2.</span><span class="sxs-lookup"><span data-stu-id="49551-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="49551-174">**Výpis 2 – `ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="49551-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="49551-175">V výpisu 2 jsou `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`a `OnResultExecuted()` metody všechny volat metodu `Log()`.</span><span class="sxs-lookup"><span data-stu-id="49551-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="49551-176">Název metody a data aktuální trasy jsou předány metodě `Log()`.</span><span class="sxs-lookup"><span data-stu-id="49551-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="49551-177">Metoda `Log()` zapisuje zprávu do okna výstupu sady Visual Studio (viz obrázek 2).</span><span class="sxs-lookup"><span data-stu-id="49551-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="49551-178">[![zápis do okna výstup aplikace Visual Studio](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="49551-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="49551-179">**Obrázek 02**: zápis do okna výstup aplikace Visual Studio ([kliknutím zobrazíte obrázek v plné velikosti](understanding-action-filters-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="49551-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>

<span data-ttu-id="49551-180">Domovský kontroler v seznamu 3 ukazuje, jak můžete použít filtr akce protokolu na celou třídu kontroleru.</span><span class="sxs-lookup"><span data-stu-id="49551-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="49551-181">Vždy, když jsou vyvolány jakékoli akce vystavené domovským řadičem – buď metoda `Index()`, nebo metoda `About()` – fáze zpracování akce jsou protokolovány do okna výstup sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="49551-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="49551-182">**Výpis 3 – `Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="49551-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="49551-183">Přehled</span><span class="sxs-lookup"><span data-stu-id="49551-183">Summary</span></span>

<span data-ttu-id="49551-184">V tomto kurzu jste se zavedli k ASP.NETm filtrům akcí MVC.</span><span class="sxs-lookup"><span data-stu-id="49551-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="49551-185">Seznámili jste se se čtyřmi různými typy filtrů: filtry autorizace, filtry akcí, filtry výsledků a filtry výjimek.</span><span class="sxs-lookup"><span data-stu-id="49551-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="49551-186">Zjistili jste také základní třídu `ActionFilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="49551-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="49551-187">Nakonec jste zjistili, jak implementovat jednoduchý filtr akcí.</span><span class="sxs-lookup"><span data-stu-id="49551-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="49551-188">Vytvořili jsme filtr akcí protokolu, který zapisuje fáze zpracování akce kontroleru do okna výstup sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="49551-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="49551-189">[Předchozí](asp-net-mvc-routing-overview-vb.md)
> [Další](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="49551-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
